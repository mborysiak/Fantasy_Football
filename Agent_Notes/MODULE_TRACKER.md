# Module Tracker

| Module | Scope | Status | Current Baseline | Next Step |
| --- | --- | --- | --- | --- |
| Data generation | Projection, ADP, salary, injury, and compiled model-input tables | active | Numbered scripts under `Scripts/Data_Generation/` populate SQLite model inputs and downstream context; salary predictions include bootstrap-averaged empirical residual quantiles in `Salaries_Pred`, keeper-adjusted calibration, and rolling validation pools. The current v5 surface retains v4 causal keeper-market inputs and additive `$1`-floor calibration while reducing the fitted surface from 155 nonconstant legacy features to 12. `s5_Auction_Selection_Premium.py` now runs one premium-free annual Target seed under the live keeper-aware organic construction policy and publishes a strictly prior-origin half-strength selection reserve without altering v5 market prices; the current seed method is `app_target_selection_only_keeper_portfolio_v3` | Refresh the static reserve after material salary/keeper/template/construction-policy changes and reassess its strength alongside any future remaining-risk guardrail |
| Stacking model | Residual models and final prediction tables | active | `s1_Stacking_Model.py` exposes strict-prior-origin empirical residual calibration, `s2_RunAll.py` writes target/cutoff provenance to `Model_Validations_Resid`, and `s3_Model_Ensemble.py` persists current and historical final ensembles; the existing 42,351 validation rows were backfilled into 6,006 `Final_Validations_Resid` rows with horizon-aware donor embargoes | Use `Final_Validations_Resid` for historical ensemble features and interval audits; never evaluate terminal next targets when `resid_target_available = 0` |
| Managed auction app | Auction draft optimizer, weekly managed roster scoring, waiver baselines | active | Fantasy_Football_App loads `League_Keepers`, normalizes salary draws, requires fully paired Buy/Pass nomination trials, validates nominee position and full-roster constraints, and uses a decision-price-invariant globally anchored adaptive isotonic Max Bid search; managed scorers consume a separate played-week mask so source-observed zero/negative outcomes are not replaced with hindsight; each organic Target outer roster repeats the constraint-safe exact-scored expected-profile swap until no improvement remains, capped at 12 swaps, while forced Buy/Pass and Current Nomination remain unrefined. Target defaults to 320 organic trials in eight balanced 40-trial logical blocks, then uses four broad paired pilot scenarios over eligible players outside 20 protected heuristic candidates and appends four discoveries. The resulting 24-player cohort receives 64 fresh preliminary pairs. Fresh 96-pair confirmation preserves the ten evidence-priority candidates and appends any missing members of the four highest-market-salary usable preliminary candidates, producing 10-14 unique confirmations; confirmed market anchors remain visible in the 30-row board. Pilot values never enter evidence. Both forced stages retain eight logical scenario blocks independent of the one-to-eight process count. A position-neutral leave-one-player-out random-effects model combines only comparable forced stages with logical-block-aware uncertainty, and excess preliminary/confirmation disagreement adds continuous between-stage variance. Every usable forced row ranks on posterior LCB80 regardless of evidence label; confirmed, preliminary, mixed, negative, conflicting, and low-fit-pivot labels do not replace the score. Forced rows precede a separate Organic Gain watchlist with explicit pair coverage. Target Pass dynamically promotes Top-N rank N+1 when it excludes an original Top-N candidate. The compact table shows eight decision fields and places separate preliminary/confirmation gains, confirmation route, posterior support, disagreement, forced-Buy roster mix, WR/TE decision-dollar change, completion-player, and price diagnostics under collapsed Evidence Details. Current Nomination remains the exact-price authority. UI dropdowns expose starter, bench, position-max, and waiver PPG settings with a default two-TE roster cap, collapsed semantic sidebar groups, pre-solve feasibility checks, and a cached exact Weekly Template Comp Explorer with donor weights, outcomes, participation, and position-specific match context. Target and Nomination add the active static half-strength selection reserve only to unresolved-player affordability, while keepers, entered purchases, and explicit nominee prices remain deterministic. Candidate-rebased Target markets remove only sampled market price from league money, never the personal reserve. Market, reserve, and decision dollars remain separate, and a default-on runtime checkbox zeros the reserve everywhere for baseline comparisons | Benchmark rank stability and missed regret across representative draft states and paired seeds; use Current Nomination for exact bid decisions and do not interpret the targeted reserve as complete salary-risk coverage |
| Sequential auction Target Board | Non-anticipating auction-path target prioritization | active | Fantasy_Football_App defaults to a history-only sequential policy; the prior staged full-roster Target remains available as `Legacy Oracle`. At budget 320, 24 heuristic candidates expand to a 64-player exact discovery pool with capped same-position peers plus position-relative Premium/Mid/Value coverage; all receive exact managed-season evidence. Confirmed and discovery rows share one global LCB80 comparison table with explicit stage labels and filters; discovery rows are shaded, retain exploratory PROMISING/WATCH/LOW labels, and cannot issue TARGET or max-bid guidance. Fresh confirmation allocates 18 players across leaders, peers, position/tier sentinels, and market anchors; the three highest-salary personally affordable/position-legal remaining players are protected exact confirmations and stay on the main board even when labeled `INCOMPLETE`. They consume existing slots. The leading ten usable confirmations receive low/high price curves. Production v6 draws construction, salary, nomination, and weekly evidence on the full canonical player universe before masking the live pool; player templates and candidate seeds are player-keyed. Four partial-crossed evidence blocks each build a point-forecast policy from 32 stratified joint historical residual/weekly-path donors. Discovery uses 8 paths by 16 seasons per block; confirmation uses 12 paths by 64 seasons per block, retaining 3,072 confirmation cells and 256 distinct seasons. A random-effects action SE combines within-block crossed uncertainty with between-block policy disagreement, and the UI shows block-positive share/range. Four spawned workers preserve exact parity. A 16-root replay of the prior evidence engine raised AJ-on/off Bijan correlation from 0.25 to 0.993, held the AJ-state delta to +0.37 mean / 0.96 SD, and reduced seed SD from 21.07 to 7.99-8.15. A paired 2022-2025 replay found that blindness removes a `$14.4` full-surface scenario discount but leaves a similar `$17.3` actual-minus-point initial-plan residual; live recourse is legal but strands `$45.4` on average | Tune history-only target ceilings and late-draft reinvestment to reduce stranded salary without exposing future prices; then rerun representative seed-stability, rank-regret, and historical-price benchmarks |
| Managed auction bench policy | Bench opportunity cost, waiver replacement, breakout upside, and keeper value | active | Production keeps the estimated waiver PPG as the weekly replacement floor, removes the generic 0.25 bench-P90 bonus and raw next-year blend, and uses current-season managed value before the separate soft keeper portfolio. No age, role, or designated lottery-ticket quota is imposed | Monitor prospective roster composition and latency; do not raise waiver PPG as an upside proxy or reintroduce a broad additive bench bonus |
| Best-ball weekly templates | Historical weekly profile tables and current-player template pools | active | `Scripts/Modeling/s4_Best_Ball_Weekly.py` builds league-aware templates with best-ball `week_*`, auction `managed_week_*`, and `played_week_*` masks; QB participation and managed scores are captured before the greater-than-15-play best-ball filter and audited through distinct `played_games`/`active_games` counts. Matching uses uncapped experience, absolute PPG, projection/market disagreement, and full-universe workload-room share/rank/gap/concentration. Position-specific absolute-distance kernels blend toward uniform for weak matches, apply a fixed 12-season recency sampling half-life, reapply the 5% donor cap, and report ESS plus donor-age diagnostics. The redundant `projection_x_exp` interaction is diagnostic only; direct PPG and experience remain independently weighted. Ordinary zero-active outcomes remain eligible; declared structural exclusions remain auditable, with Bell's 2018 holdout currently the only excluded donor. A 1,620-target strictly prior-donor replay supports pool centering: recent PPG/contribution bias was -0.01/+0.89 with 80.8% P10-P90 coverage. Context and feature ablations reject global draft-capital/supporting-cast weights and selected the active recency/interaction specification at every 2023-2025 nested origin, while position guardrails rejected dropping component ranks. A 36-method local sensitivity replay supports leaving all remaining relative weights unchanged. Beta and DK source/app tables were rebuilt and schema/probability checked on 2026-07-23 | Monitor prospective calibration and comp composition under the active matcher; keep flatter within-pool sampling as a separate future hypothesis |
| Simulation database export | App-facing SQLite handoff | active | `copy_simulation_db_to_apps()` requires complete played/managed fields, synchronizes only generated best-ball tables to the auction app (preserving app-owned keeper/salary scenarios), and copies the complete source DB to Snake. `create_snake_nffc_preview.py` separately builds an isolated, explicitly uncalibrated NFFC setup database from the stable DK app copy while s2 is in flight | Replace the preview clone with normal NFFC s3/s4 outputs, confirm app copies after schema changes, and update app contracts |
| Validation/audits | Calibration, ADP, and template-quality checks | active | Audit tables plus study outputs support review; `Best_Ball_ADP_Audit` flags draftable players with missing/fallback/suspicious ADP context; `Salary_Backtest_Predictions` and `Salary_Validations_Resid` retain raw/normalized salary predictions, causal cutoffs, candidate coverage, and method provenance; selected-roster diagnostics retain unique-player and selection-weighted residual views, exact scenario-center shifts, and roster-gap decomposition. The selection-surcharge study persists rolling coefficients, player corrections, fixed-roster gap checks, feasible-only quality, and 4,000 paired policy cells | Add a genuinely nested method holdout if model-selection claims require it, and evaluate current 2026 surcharge stability before production promotion |
| Managed keeper portfolio | One-year keeper call option across bench slots | active | Organic Target construction now scores expected best positive first-year `price + $10` surplus across every nominal bench player. Next-year residual draws are separate from current draws with player common-factor `rho=0.25`. A six-player greedy shortlist may re-solve the entire roster around accumulated bench anchors only when the complete live construction bank preserves both current mean and p10 and keeper utility improves at least `$1`; there are no age, role, or option-count quotas. Legacy forced Buy/Pass and Current Nomination remain current-year estimands. Sequential Target now adds keeper-led discovery and paired whole-bench Buy-minus-Pass keeper labels without blending keeper value into current-season rank or auction policy; `LOTTERY` allows at most a two-point current mean and season-p10 loss. The v3 annual premium-free seed completed 1,000/1,000 rosters and republished 166 source/app premium rows with selection rates summing to 13 | Monitor prospective keeper concentration, starter spend, option additions, current holdout EV/p10, and real-world keeper eligibility; do not add tenure, age, or ticket-count restrictions without a separate use case |
