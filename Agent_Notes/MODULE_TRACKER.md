# Module Tracker

| Module | Scope | Status | Current Baseline | Next Step |
| --- | --- | --- | --- | --- |
| Canonical ADP governance | Historical/current provider selection, NFFC/DK distributions, model consensus, beta template context, and app publication | active in production | Policy `canonical_market_adp_family_v2` uses MFL only through 2024 and one vote per FantasyPros redraft, FantasyPros best-ball `AVG`, direct DK, and NFFC family thereafter. NFFC is built from literal Overall `ADP.tsv` plus $25/$50 `ADP (1).tsv`; paired centers/bounds are equal means and SD pools within/between-feed variance. Single-feed fringe rows retain their observed distribution with `source_count=1` and null `feed_gap`. Beta template matching prefers V2 canonical ADP while ETR remains eligibility rank/last-resort fallback. Release stage `20260804T193416Z_4136d153` is live at 348/382/326 DK/NFFC/beta players; all ten installed digests and durable backups were verified | Monitor prospective source coverage and NFFC pair agreement rather than restoring MFL or correlated raw-provider votes |
| Conditional-PPG ensemble research | Additional model-family signal around the locked Lasso/RF/LightGBM current-season blend | Extra Trees remains a research shadow; Ridge replacement, HistGBM, CatBoost, and expanded boosting grids rejected; production unchanged | Extra Trees equal-four improves reused 2023-2025 RMSE by 0.003461 DK and 0.006270 beta, with all six scoring-season and non-QB position cells favorable, but lacks new-origin/downstream confirmation. Ridge `alpha=10` failed its full corrected-lineage downstream decision: annual 2017-2025 RMSE improves only 0.001401/0.001264 with intervals crossing zero and 4/6 recent wins; player distributions and weekly templates pass, but fixed-roster CRPS improves only DK development and beta temporal worsens 0.527% versus the 0.5% gate. HistGBM equal-four worsens by 0.003646/0.001022; replacing LightGBM wins only 3/6 season cells. Expanded LightGBM and CatBoost do not improve the locked blend | Keep the current equal-third Lasso/RF/LightGBM blend in production for 2026. Ridge is no longer a promotion candidate on existing data. Extra Trees requires a separately frozen full downstream test and prospective/new-origin evidence; do not combine shadows, widen grids, or tune ensemble weights on reused 2023-2025 outcomes |
| Annual model parameter cache | Current- and next-season V2 annual model selection by season, league, runner, and model | active in 2026 production | Random forest fits use four internal workers. Exact data/model-spec fingerprints govern 36 cached 2026 selections across DK, NFFC, and beta; cache hits skip hyperparameter search but always refit and predict. The promoted cache has 7/7 current and 5/5 next hit receipts in every league V2 database and is backed up and rollback-protected with the release | Monitor fingerprint misses on future input changes; review and register the next season rather than copying 2026 entries |
| Upside objective validation | Rare player outcomes and roster championship utility for weekly templates, Auction, and Snake | validation policy active; production and app objectives unchanged | The causal q90 event combines +5 active PPG over the held-out projection with managed contribution above a five-year strictly prior position threshold; q95 is the severity sensitivity. PPG/contribution CRPS remain calibration gates, while rare-event Brier/log loss, average precision/top-decile lift, tail-utility CRPS, and paired 12-team championship Brier/log loss are secondary objectives. The WR `wr_ppg225_both025` arm improved all four player-level q90 calibration/tail cells but failed roster championship transport in three of four league-period cells. DK absolute tail probability is materially underpredicted. The flatter-distance arm also remains rejected | For future matcher tests, require expected-score/calibration non-inferiority and cross-league/temporal tail replication. Before any app promotion, causally recalibrate DK tail probability and run common-bank historical Auction Buy/Pass and Snake forced-pick replays. Prototype a lexicographic decision rule: among actions within 0.25% of the best expected roster score, prefer the strongest paired championship-probability lower bound |
| PFF advanced-stat screening | Leakage-safe prior-season receiving/rushing features for projection and weekly-template challengers | full confirmation complete; production unchanged | Raw PFF IDs join through canonical `player_identity`; season `t` uses only `t-1` values, neutral position-season priors, and fixed sample reliability. The full selected-grid Lasso/RF/LightGBM replay confirmed TE avoided tackles per reception as a projection-only signal: TE-routed RMSE and q90 Brier improved versus production and a prior-PFF-opportunity control in both DK/beta and both 2017-2022/2023-2025. TE YAC/route passed as a separate projection sensitivity. Template evidence did not transport: primary avoided-tackles matching failed development PPG/upside, while the YAC finalist passed the player-level screen but worsened all three DK roster metrics in both periods and was mixed in beta | Keep PFF rates out of weekly-template matching. Treat TE avoided tackles per reception as a projection-only implementation candidate, routed after the blend to preserve exact non-TE parity; require explicit promotion governance or a genuinely new-origin confirmation because screening and confirmation reused the same historical origins |
| Production refresh orchestration | Canonical downstream build, validation, app candidates, and promotion | schema-6 FTN-adjusted release promoted | Governed stage `20260820T025601Z_a672217a` completed all 30 steps with 347/382/327/327 DK/NFFC/beta/NV projections, exact weekly-map and app-database parity, 1,000/1,000 auction trials, and zero Auction/Snake smoke errors or exceptions. Its report records 20 additions and 14 removals versus the prior release. Explicit promotion completed at `2026-08-20T15:29:51Z`; all 11 installed hashes match staging and durable rollback files are complete | Keep managed-template validation in every refresh, close overlapping digest readers before app preparation on Windows, and register 2027 evidence rather than copying 2026 |
| Repository object integrity | Git loose-object storage, reachability, and durable database-blob recovery | repaired; strict full graph clean | Four corrupt loose database objects were preserved outside `.git`. The only branch/reflog-reachable object was reconstructed from an exact-hash governed backup; the other three were unreachable and quarantined. Full strict fsck/connectivity, committed blob reads, status/diff, and whitespace checks pass without changing the live databases | Retain the recovery copies until the next verified clone/backup cycle; use exact object hashes and governed artifacts for any future large-blob repair |
| NV auction league path | Independent scoring, V2 handoff, weekly templates, salaries, and Auction UI | active in promoted release | NV matches beta except passing TDs score 4 rather than 5. The promoted release publishes 326 NV projections, salaries, and 80-donor weekly maps, zero keepers, a top-156 salary total of `$3,576`, and Auction defaults of 2 QB/2 RB/2 WR/1 TE/1 FLEX plus five bench slots under a `$298` cap. The managed-template follow-up has zero horizon-bound violations and a clean NV app smoke | Add `keepers_2026_nv.csv` later without a code change and rerun the governed salary path; retain the 2-QB replacement and managed-center gates |
| Data generation | Projection, ADP, salary, injury, and compiled model-input tables | active; refreshed FTN inputs promoted | Numbered scripts under `Scripts/Data_Generation/` populate SQLite model inputs and downstream context. `ADP_Ranks` validates and atomically replaces source-scoped slices; the live source has 42 restored 2025 FantasyPros QBs, one 32-team 2026 DST snapshot, and no duplicate or nonstandard FantasyPros positions. FantasyPoints now exports `projections.season.csv` with grouped labels on row one and actual headers on row two; its validated loader maps the new names and repeated stat families to the unchanged `FantasyPoints_Projections` contract. FTN's grouped-header export is validated and stored separately in `FTN_Projections`; the 2026 top-of-pool slice has 111 rows (11/43/48/9 QB/RB/WR/TE) and participates in both legacy and V2 component consensus. FFToday retrieval retries by position, discovers the projection table structurally, rejects shallow QB/RB/WR/TE slices below 40/80/100/40, and verifies exact post-commit counts; the repaired 2026 slice is 50/95/131/50. FantasyPros redraft, NFFC, DraftKings, FFToday, and FTN saves emit post-commit confirmations. `4_Data_Compile.py` computes period-three player rolling means/maxima from lags, avoiding the pandas 2.3.1 grouped-window corruption. ESPN salary ingestion is structural and atomically repairs only staged governed slices after marker/row/uniqueness validation. The live FTN-adjusted release publishes 327 beta/NV salary predictions and 313 selection premiums | At annual rollover review provider filenames/header shapes plus every governed salary count or boundary policy and nullable-team allowlist rather than copying 2026; keep weekly donor tails diagnostic and reassess the separate remaining-risk guardrail |
| Projection V2 foundation | Identity, outcomes, preseason source evidence, controlled feature mart, and locked current/following-season model evidence | FTN-adjusted release active in production | The live release shares a 13,936-row, 161-field foundation across DK/NFFC/beta/NV, including audited FTN component evidence. Historical `FFA_RawStats`, `FFA_Projections`, and `FantasyPros_Best_Ball_ADP` team labels through 2025 are discarded while their values remain, and `TEAM_MAP` aliases are canonicalized before consensus. Policy `canonical_market_adp_family_v2` and its hash are identical across league foundations; current raw markets contain 442 DK, 525 NFFC aggregate, and 244 ETR rows, while resolved app publication contains 442 DK, 458 NFFC, and 244 ETR keys. All four leagues publish 772 current and 772 next-season shadow rows; live handoffs are 347 DK, 382 NFFC, 327 beta, and 327 NV with exact weekly/salary key coverage. Haynes King and J'Mari Taylor were removed from the NFFC market-only exclusion set after complete current/next evidence arrived. Governed identity aliases, effective source seasons, beta QB sack scoring, and the FFToday quarantine remain auditable. DK retains its legacy/preseason historical-center policy; beta/NV use scoring-matched context and NFFC uses its separately validated expert center. The final nested test rejects normalized expert-rank level for production; raw-provider rank, its raw-log representation, and logged cross-source disagreement/scale variants are also rejected | Monitor prospective calibration, ADP source resolution, NFFC pair agreement, beta/NV fringe-QB provider coverage, trusted-source team conflicts, and keeper appearance calibration; never substitute mutable latest-team, legacy, DK, quarantined, or fabricated evidence for missing governed context |
| NFFC Snake offense path | Independent NFFC scoring, projection handoff, ADP, weekly templates, and Snake selection | active in production as offense-only 3RR; not complete contest support | The 2026 cycle registers `v2_conditional_ppg_2026_candidate_nffc_v1`, separate NFFC current/next scoring hashes, the core plus top-360 canonical offensive NFFC ADP eligibility union, and 17-week templates drawn from 2021-forward donors. Historical/current scoring-sensitive matcher context is authoritative from the NFFC-scored V2 preseason consensus; DK-scored `Model_Inputs` context is audit-only, and receiver matching uses QB1 passing points rather than total QB fantasy points. Historical donors use `nffc_scored_expert_consensus`. The promoted surface has 1,509 templates, a 382-player map, 80 donors per player, 17 populated weeks, and zero Snake smoke errors. Canonical `TK`/`TDSP` rows remain market audit entities but are filtered from model and app populations. The selector is explicitly offense-only 3RR; K/DST, official 30-round roster composition, and alternate NFFC contest formats are not implemented | Retain the expert donor center, monitor live use, and do not call the adapter complete NFFC contest support |
| Stacking model | Residual models and final prediction tables | active | `s1_Stacking_Model.py` exposes strict-prior-origin empirical residual calibration, `s2_RunAll.py` writes target/cutoff provenance to `Model_Validations_Resid`, and `s3_Model_Ensemble.py` persists current and historical final ensembles; the existing 42,351 validation rows were backfilled into 6,006 `Final_Validations_Resid` rows with horizon-aware donor embargoes | Use `Final_Validations_Resid` for historical ensemble features and interval audits; never evaluate terminal next targets when `resid_target_available = 0` |
| Managed auction app | Auction draft optimizer, weekly managed roster scoring, waiver baselines | active; source-owned managed center promoted | Fantasy_Football_App loads league-specific keepers/settings and source-owned managed weekly-template centers. Current databases consume `managed_profile_ppg`, `managed_residual_center_ppg`, and `managed_active_ppg_resid` directly; only legacy databases receive the old runtime repair. This prevents near-zero predicted PPG from amplifying partial-season backup-QB paths and avoids double-rescaling current data. The remaining paired Buy/Pass, globally anchored Max Bid, evidence-block, roster-refinement, reserve, and Current Nomination behavior is unchanged | Benchmark rank stability after the corrected centers; keep legacy compatibility isolated and use Current Nomination for exact bid decisions |
| Sequential auction Target Board | Non-anticipating auction-path target prioritization | active | Fantasy_Football_App defaults to a history-only sequential policy; the prior staged full-roster Target remains available as `Legacy Oracle`. At budget 320, 24 heuristic candidates expand to a 64-player exact discovery pool with capped same-position peers plus position-relative Premium/Mid/Value coverage; all receive exact managed-season evidence. Confirmed and discovery rows share one global LCB80 comparison table with explicit stage labels and filters; discovery rows are shaded, retain exploratory PROMISING/WATCH/LOW labels, and cannot issue TARGET or max-bid guidance. Fresh confirmation allocates 18 players across leaders, peers, position/tier sentinels, and market anchors; the three highest-salary personally affordable/position-legal remaining players are protected exact confirmations and stay on the main board even when labeled `INCOMPLETE`. They consume existing slots. The leading ten usable confirmations receive low/high price curves. Production v6 draws construction, salary, nomination, and weekly evidence on the full canonical player universe before masking the live pool; player templates and candidate seeds are player-keyed. Four partial-crossed evidence blocks each build a point-forecast policy from 32 stratified joint historical residual/weekly-path donors. Discovery uses 8 paths by 16 seasons per block; confirmation uses 12 paths by 64 seasons per block, retaining 3,072 confirmation cells and 256 distinct seasons. A random-effects action SE combines within-block crossed uncertainty with between-block policy disagreement, and the UI shows block-positive share/range. Four spawned workers preserve exact parity. A 16-root replay of the prior evidence engine raised AJ-on/off Bijan correlation from 0.25 to 0.993, held the AJ-state delta to +0.37 mean / 0.96 SD, and reduced seed SD from 21.07 to 7.99-8.15. A paired 2022-2025 replay found that blindness removes a `$14.4` full-surface scenario discount but leaves a similar `$17.3` actual-minus-point initial-plan residual; live recourse is legal but strands `$45.4` on average | Tune history-only target ceilings and late-draft reinvestment to reduce stranded salary without exposing future prices; then rerun representative seed-stability, rank-regret, and historical-price benchmarks |
| Managed auction bench policy | Bench opportunity cost, waiver replacement, breakout upside, and keeper value | active | Production keeps the estimated waiver PPG as the weekly replacement floor, removes the generic 0.25 bench-P90 bonus and raw next-year blend, and uses current-season managed value before the separate soft keeper portfolio. No age, role, or designated lottery-ticket quota is imposed | Monitor prospective roster composition and latency; do not raise waiver PPG as an upside proxy or reintroduce a broad additive bench bonus |
| Best-ball weekly templates | Historical weekly profile tables and current-player template pools | DK, NFFC, beta, and NV slices active; managed contract promoted | Current beta/DK/NFFC/NV maps retain 80 donors/player and their original league horizons. The builder additionally persists an auction-only managed center, policy, and residual. Nonpositive-active donors use a positive conditional V2 center when available or a governed historical fallback; build/export/release validation requires exact denominator/path/residual consistency and caps weekly/season multipliers at the horizon. Legacy `active_ppg_resid` and `week_*` remain unchanged for Snake. All related audits and non-managed template fields were identical across the correction rebuild | Monitor prospective comp composition and calibration. Preserve Snake semantics, beta scoring units, the 39 beta 2018 QB exclusions, and NFFC expert donor center; never weaken the managed horizon gate |
| Simulation database export | App-facing SQLite handoff | active | The governed cutover synchronizes 20 source-owned generated tables to Auction while preserving six app-owned tables and copies the complete source database to Snake. Both staged app artifacts are vacuumed before parity checks and smoke tests; receipts retain before/after page usage, require zero freelist pages, and fail above GitHub's 100 MiB blob limit. Every generated Auction table matches staging, all six app-owned tables remain unchanged, and every Snake table matches source logically even though compaction changes byte layout | Confirm source/Auction/Snake content invariants and compacted artifact sizes after every handoff or schema rebuild |
| Validation/audits | Calibration, ADP, and template-quality checks | active | Audit tables plus study outputs support review; `Best_Ball_ADP_Audit` flags draftable players with missing/fallback/suspicious ADP context; `Salary_Backtest_Predictions` and `Salary_Validations_Resid` retain raw/normalized salary predictions, causal cutoffs, candidate coverage, and method provenance; selected-roster diagnostics retain unique-player and selection-weighted residual views, exact scenario-center shifts, and roster-gap decomposition. The selection-surcharge study persists rolling coefficients, player corrections, fixed-roster gap checks, feasible-only quality, and 4,000 paired policy cells | Add a genuinely nested method holdout if model-selection claims require it, and evaluate current 2026 surcharge stability before production promotion |
| Managed keeper portfolio | One-year keeper call option across bench slots | active | Organic Target construction scores expected best positive first-year `price + $10` surplus across every nominal bench player. V2 next-year residual draws are conditional on appearance with player common-factor `rho=0.25`; a separate Bernoulli `pred_appear_ny` draw zeros future market value before surplus. Current-year uncertainty remains the joint donor residual/path and never enters keeper residual sampling | Monitor prospective keeper concentration, appearance calibration, current holdout EV/p10, and real-world eligibility; do not consume conditional PPG without the appearance draw |
