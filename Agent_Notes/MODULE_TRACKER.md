# Module Tracker

| Module | Scope | Status | Current Baseline | Next Step |
| --- | --- | --- | --- | --- |
| Database storage and recovery | Source inputs, generated databases, Git tracking, and artifact retention | display-only breakout tables retired; source Simulation below 100 MiB | Source/Auction/Snake are 80.64/80.64/80.62 MiB with 26 tables each. All retained table content/schema matches pre-retirement snapshots; expert/stat databases remain tracked and compiled Model_Inputs/Validations remain untracked locally. Schema 8 removes inherited breakout data before compaction and rejects its reintroduction | Keep external backup and historical Git cleanup as separate follow-ups; preserve snapshot receipts and all weekly scoring tables. See `docs/runbooks/database_storage.md` |
| Paired breakout and keeper templates | Causal season-N archetypes paired to same-player N+1 appearance/performance for research | retired from production on 2026-09-05; research builder retained | Removed the display, four Simulation tables, refresh build/validation step, and app publication requirement. The donor surface never affected projections, salaries, scoring, rosters, or bids. CLI now requires a separate research output DB and has no app sync option; historical snapshots and table contract are retained | Use only for optional research pending rolling calibration and roster-level validation; do not restore production storage for a display-only feature |
| Canonical ADP governance | Historical/current provider selection, NFFC/DK distributions, model consensus, beta template context, and app publication | active in production | Policy `canonical_market_adp_family_v2` uses MFL only through 2024 and one vote per FantasyPros redraft, FantasyPros best-ball `AVG`, direct DK, and NFFC family thereafter. NFFC is built from literal Overall `ADP.tsv` plus $25/$50 `ADP (1).tsv`; paired centers/bounds are equal means and SD pools within/between-feed variance. Single-feed fringe rows retain their observed distribution with `source_count=1` and null `feed_gap`. Beta template matching prefers V2 canonical ADP while ETR remains eligibility rank/last-resort fallback. Release `20260826T212331Z_dbd5cac4` is live at 348/382/324/324 DK/NFFC/beta/NV players with zero weekly ADP defaults or review rows | Monitor prospective source coverage and NFFC pair agreement rather than restoring MFL or correlated raw-provider votes |
| Conditional-PPG ensemble research | Additional model-family signal around the locked Lasso/RF/LightGBM current-season blend | Extra Trees remains a research shadow; Ridge replacement, HistGBM, CatBoost, and expanded boosting grids rejected; production unchanged | Extra Trees equal-four improves reused 2023-2025 RMSE by 0.003461 DK and 0.006270 beta, with all six scoring-season and non-QB position cells favorable, but lacks new-origin/downstream confirmation. Ridge `alpha=10` failed its full corrected-lineage downstream decision: annual 2017-2025 RMSE improves only 0.001401/0.001264 with intervals crossing zero and 4/6 recent wins; player distributions and weekly templates pass, but fixed-roster CRPS improves only DK development and beta temporal worsens 0.527% versus the 0.5% gate. HistGBM equal-four worsens by 0.003646/0.001022; replacing LightGBM wins only 3/6 season cells. Expanded LightGBM and CatBoost do not improve the locked blend | Keep the current equal-third Lasso/RF/LightGBM blend in production for 2026. Ridge is no longer a promotion candidate on existing data. Extra Trees requires a separately frozen full downstream test and prospective/new-origin evidence; do not combine shadows, widen grids, or tune ensemble weights on reused 2023-2025 outcomes |
| Annual model parameter cache | Current- and next-season V2 annual model selection by season, league, runner, and model | active in 2026 production | Random forest fits use four internal workers. Exact data/model-spec fingerprints govern 48 cached 2026 selections across DK, NFFC, beta, and NV; cache hits skip hyperparameter search but always refit and predict. The August 23 release reused all 7/7 current-model selections per league, while changed next-year fingerprints correctly reselected five grids per league. The promoted cache is backed up and rollback-protected with the release | Monitor fingerprint misses on future input changes; review and register the next season rather than copying 2026 entries |
| Upside objective validation | Rare player outcomes and roster championship utility for weekly templates, Auction, and Snake | 2022-2025 Auction objective replays complete; production and app objectives unchanged | The causal q90 event is not a sufficient roster objective. The best-available waiver proxy remains promising, while guarded win/excess/power selection retains the exact-mean control. Frozen 2022-2024 replays reject direct expected-excess optimization: pure excess loses actual score `5.88/48.84/64.33`; 50/50 gains `25.99` in 2022 but loses `59.56/83.23`. Both lose independent holdout mean and P90 in every new origin. Including the previously inspected positive 2025 outcome, actual deltas average `-7.20` pure and `-13.97` 50/50. The aggressive arms do not consistently implement a young-upside tilt, and youth alone was not a reliable historical q90 discriminator | Preserve expected-score/calibration non-inferiority and paired uncertainty control. Keep win probability and expected winning margin diagnostic. Do not promote the current pure or 50/50 estimator. Next use dense legal local swaps and cross-fitting within each origin, add roster-marginal value over accessible same-position waiver and playoff sensitivity, and require genuinely prospective evidence. Do not deploy an age tax or youth bonus |
| PFF advanced-stat screening | Leakage-safe prior-season receiving/rushing features for projection and weekly-template challengers | full confirmation complete; production unchanged | Raw PFF IDs join through canonical `player_identity`; season `t` uses only `t-1` values, neutral position-season priors, and fixed sample reliability. The full selected-grid Lasso/RF/LightGBM replay confirmed TE avoided tackles per reception as a projection-only signal: TE-routed RMSE and q90 Brier improved versus production and a prior-PFF-opportunity control in both DK/beta and both 2017-2022/2023-2025. TE YAC/route passed as a separate projection sensitivity. Template evidence did not transport: primary avoided-tackles matching failed development PPG/upside, while the YAC finalist passed the player-level screen but worsened all three DK roster metrics in both periods and was mixed in beta | Keep PFF rates out of weekly-template matching. Treat TE avoided tackles per reception as a projection-only implementation candidate, routed after the blend to preserve exact non-TE parity; require explicit promotion governance or a genuinely new-origin confirmation because screening and confirmation reused the same historical origins |
| Production refresh orchestration | Canonical downstream build, validation, app candidates, and promotion | August 28 schema-7 release plus August 29 isolated Beta salary refresh promoted | Governed stage `20260828_beta_keeper_refresh_v4` / run `20260828T215922Z_c9cd883e` completed all 31 steps with 341/383/323/323 DK/NFFC/beta/NV projections. The isolated August 29 follow-up updates the 14-player Beta keeper ledger to `$597`, reruns the locked salary model and a fresh 1,000/1,000 reserve, and republishes the keeper-aware paired-breakout map. Live model/Auction/Snake parity, SQLite integrity, focused tests, and app smokes pass; both current keeper files remain required governed inputs and rollback copies are retained | Keep managed/paired-template validation in every refresh and register 2027 evidence rather than copying 2026; use the schema-7 synchronizer for paired-breakout tables in future salary-only promotions |
| Repository object integrity | Git loose-object storage, reachability, and durable database-blob recovery | repaired; strict full graph clean | Four corrupt loose database objects were preserved outside `.git`. The only branch/reflog-reachable object was reconstructed from an exact-hash governed backup; the other three were unreachable and quarantined. Full strict fsck/connectivity, committed blob reads, status/diff, and whitespace checks pass without changing the live databases | Retain the recovery copies until the next verified clone/backup cycle; use exact object hashes and governed artifacts for any future large-blob repair |
| NV auction league path | Independent scoring, V2 handoff, weekly templates, salaries, and Auction UI | active in promoted release | NV matches beta except passing TDs score 4 rather than 5. The active release publishes 324 NV projections/salaries and 80-donor weekly maps. Its 16 keepers spend `$453`, leaving 140 market slots and `$3,123`; Auction defaults remain 2 QB/2 RB/2 WR/1 TE/1 FLEX plus five bench slots under a `$298` cap. The 240-record ESPN copy uses a variable terminal-`$0` boundary, all keeper/salary tables have exact source/app parity, and live NV smoke is clean | Treat `keepers_2026_nv.csv` as a required annual input; rerun the governed salary path after any keeper change and retain the 2-QB replacement and managed-center gates |
| Data generation | Projection, ADP, salary, injury, and compiled model-input tables | active; completed 2026 Beta/NV results published | Numbered scripts under `Scripts/Data_Generation/` populate SQLite model inputs and downstream context. `ADP_Ranks` validates and atomically replaces source-scoped slices. FantasyPoints and FTN grouped-header exports are schema-validated. FFToday retrieval remains depth-gated and atomic. `4_Data_Compile.py` avoids the pandas grouped-window corruption. ESPN salary ingestion is structural and repairs only staged governed slices: frozen 2025 Beta/NV remain exact at 200/160, while active 2026 Beta/NV use variable terminal-`$0` boundaries and parse 180/240. The live predicted surface publishes 323 Beta/NV rows, 14 Beta preseason keepers spending `$597`, 16 NV keepers spending `$453`, and 309 Beta selection premiums. Beta's 142 open slots reconcile exactly to `$2,979`. Completed 2026 results contribute 180 unique rows in each league: Beta spends `$3,581` with 15 result keepers at `$608`, while NV spends `$3,495` with 16 keeper flags. Josh Jacobs' exact Beta `$26` result is retained for hindsight but the `Josh Jacobs, 2026` player-season is excluded from future salary-model fitting because suspension risk caused an approximately `$31` non-market discount | Reconcile the four NV realized/preseason keeper-price differences before using 2026 keeper-budget context in the 2027 build. At annual rollover confirm the governed Josh Jacobs exclusion is applied, review provider filenames/header shapes plus every salary count or boundary policy, keeper file, and nullable-team allowlist rather than copying 2026, and keep weekly donor tails diagnostic |
| Projection V2 foundation | Identity, outcomes, preseason source evidence, controlled feature mart, and locked current/following-season model evidence | August 26 release active in production | The active release uses the refreshed August 26 provider and market inputs. Historical `FFA_RawStats`, `FFA_Projections`, and `FantasyPros_Best_Ball_ADP` team labels through 2025 are discarded while their values remain, and `TEAM_MAP` aliases are canonicalized before consensus. Policy `canonical_market_adp_family_v2` and its hash are identical across league foundations. Live handoffs are 348 DK, 382 NFFC, 324 beta, and 324 NV with exact weekly/salary key coverage. Protected-market rows without a governed current center remain explicit exclusions rather than receiving fabricated evidence: Jayden Higgins is excluded in DK/NFFC and Zamir White and Theo Wease Jr. in NFFC; stale Austin Ekeler and now-complete Jaydn Ott exclusion entries were retired. Governed identity aliases, effective source seasons, beta QB sack scoring, and the FFToday quarantine remain auditable. DK retains its legacy/preseason historical-center policy; beta/NV use scoring-matched context and NFFC uses its separately validated expert center | Monitor prospective calibration, ADP source resolution, NFFC pair agreement, beta/NV fringe-QB provider coverage, trusted-source team conflicts, and keeper appearance calibration; never substitute mutable latest-team, legacy, DK, quarantined, or fabricated evidence for missing governed context |
| NFFC Snake offense path | Independent NFFC scoring, projection handoff, ADP, weekly templates, and Snake selection | active in production as offense-only 3RR; not complete contest support | The 2026 cycle registers `v2_conditional_ppg_2026_candidate_nffc_v1`, separate NFFC current/next scoring hashes, the core plus top-360 canonical offensive NFFC ADP eligibility union, and 17-week templates drawn from 2021-forward donors. Historical/current scoring-sensitive matcher context is authoritative from the NFFC-scored V2 preseason consensus; DK-scored `Model_Inputs` context is audit-only, and receiver matching uses QB1 passing points rather than total QB fantasy points. Historical donors use `nffc_scored_expert_consensus`. The promoted surface has 1,509 historical templates, a 385-player map, 80 donors per player, 17 populated weeks, and zero Snake smoke errors. Canonical `TK`/`TDSP` rows remain market audit entities but are filtered from model and app populations. The selector is explicitly offense-only 3RR; K/DST, official 30-round roster composition, and alternate NFFC contest formats are not implemented | Retain the expert donor center, monitor live use, and do not call the adapter complete NFFC contest support |
| Stacking model | Residual models and final prediction tables | active | `s1_Stacking_Model.py` exposes strict-prior-origin empirical residual calibration, `s2_RunAll.py` writes target/cutoff provenance to `Model_Validations_Resid`, and `s3_Model_Ensemble.py` persists current and historical final ensembles; the existing 42,351 validation rows were backfilled into 6,006 `Final_Validations_Resid` rows with horizon-aware donor embargoes | Use `Final_Validations_Resid` for historical ensemble features and interval audits; never evaluate terminal next targets when `resid_target_available = 0` |
| Managed auction app | Auction draft optimizer, weekly managed roster scoring, waiver baselines | active; actual-salary hindsight published for 2026 Beta/NV | Fantasy_Football_App loads league-specific settings and source-owned managed weekly-template centers. League Settings defaults to predicted salaries and enables Actual mode only for a canonical completed-draft slice. The 2026 NV/Beta slices each contain 156 drafted offensive players totaling `$3,435`/`$3,541`. Actual mode uses exact prices, zero salary uncertainty, no market rescaling or selection reserve, excludes undrafted players, and bypasses the predicted premium join. When app-side raw results exist, realized keeper membership is authoritative; Beta therefore exposes all 15 result keepers, including result-only Jayden Daniels `$11`, while predicted mode retains the 14-player preseason context. Older actual slices fall back to `League_Keepers` plus recorded-price overlay. The remaining paired Buy/Pass, globally anchored Max Bid, evidence-block, roster-refinement, and Current Nomination behavior is unchanged | Use variation 0 as the baseline hindsight view; publish later completed drafts through `Scripts/Modeling/publish_actual_salaries.py`, copy the raw result slice when realized keeper membership differs, and reconcile result-file keeper errors before publication |
| Historical Auction replay | Isolated prior-year projection, salary, keeper, rank, and weekly-template surfaces for current App review | 2025 Beta replay active as a local staged artifact; live 2026 unchanged | The replay builder copies all SQLite inputs, publishes 305 current-method rolling-origin rows plus four complete saved-preseason fallback rows, normalizes 309 predicted salaries to the exact `$3,169` open budget, keys 238 saved ETR ranks, preserves 15 keepers/`$407` and 156 offensive actual-price rows, and builds 309 player maps against 4,993 replay-only donors capped at 2024. Next-year keeper discovery and selection premiums are disabled. Both predicted and actual app simulations load 16-week profiles and finite draws | Use the staged database for 2025 review and keep the `model_spec_asof_year=2026` caveat visible. Do not promote it over live `Simulation.sqlite3`; register another season explicitly rather than assuming the 2025 contract generalizes |
| Sequential auction Target Board | Non-anticipating auction-path target prioritization | local Auction main App v21; not pushed | Fantasy_Football_App defaults to the history-only Sequential policy; `Legacy Oracle` remains optional. Both the 64-player discovery screen and 18-player confirmation stage use additive completion, while the exact full-bank leave-one-out swap remains research-only. The four highest positive confirmed LCB anchors contribute their completed market-price Buy rollouts, every retained outcome is grouped by position spend/shape, and one central conditional completion per anchor remains visible. Add Evidence accumulates the conditional outcomes and reranks anchors without another roster solve. Family summaries report already-scored managed EV, typical median/P10-P90 open-slot budgets, the secondary full envelope, and cap-scaled `<$40`/`<$50` total-roster WR audits including fixed players. The audit compares share, within-family EV, and total RB count without imposing a constraint. Commit `0ae0c02`; 106 tests, worker parity, accumulation, and rendered smoke pass | Use the low-WR audit prospectively before adding any rule. If a recurring low-spend subset is materially lower-EV and RB-heavy, test a starter-quality WR coverage rule rather than a raw salary floor. Preserve current beta shape/waivers and keep exact joint refinement, multi-swap convergence, cross-position rollout upgrades, rollout-time solves, the final-two-slot guard, and direct salary-spend rewards research-only or rejected |
| Managed auction bench policy | Bench opportunity cost, waiver replacement, breakout upside, and keeper value | active | Production keeps the estimated waiver PPG as the weekly replacement floor, removes the generic 0.25 bench-P90 bonus and raw next-year blend, and uses current-season managed value before the separate soft keeper portfolio. No age, role, or designated lottery-ticket quota is imposed. In the keeper-correct Brown/Tuten beta replay, constructing with every waiver baseline raised `1.5` loses `10.34` common-holdout mean points, leaves p10 essentially flat, and still averages six RBs | Monitor prospective roster composition and latency; do not raise waiver PPG as an upside proxy or reintroduce a broad additive bench bonus |
| Best-ball weekly templates | Historical weekly profile tables and current-player template pools | DK, NFFC, beta, and NV slices active; managed contract promoted | Current beta/DK/NFFC/NV maps retain 80 donors/player and their original league horizons. The builder additionally persists an auction-only managed center, policy, and residual. Nonpositive-active donors use a positive conditional V2 center when available or a governed historical fallback; build/export/release validation requires exact denominator/path/residual consistency and caps weekly/season multipliers at the horizon. Legacy `active_ppg_resid` and `week_*` remain unchanged for Snake. The fresh 2,647-target-per-league role-tiered replay rejects replacing or supplementing positional ADP rank with overall log ADP at weight 0.50: effects are tiny and fail DK/beta replication even though Brock Bowers' premium-TE donor share rises. The 2026-08-29 DK/beta asymmetric-provider replay rejects both generic `max - median` PPG and the prespecified stabilized fraction `(max - median) / sqrt(median^2 + 5^2)`: pooled point gains remain unstable and QB-heavy, strictly-prior +3/+5 models worsen, and fixed-weight template behavior does not replicate | Monitor prospective comp composition and calibration. Preserve Snake semantics, beta scoring units, the 39 beta 2018 QB exclusions, and NFFC expert donor center; keep overall log ADP and every tested one-sided provider-gap form diagnostic-only unless materially different prospective evidence earns a new hypothesis |
| Simulation database export | App-facing SQLite handoff | active | The governed cutover synchronizes 20 source-owned generated tables to Auction while preserving six app-owned tables and copies the complete source database to Snake. Both staged app artifacts are vacuumed before parity checks and smoke tests; receipts retain before/after page usage, require zero freelist pages, and fail above GitHub's 100 MiB blob limit. Every generated Auction table matches staging, all six app-owned tables remain unchanged, and every Snake table matches source logically even though compaction changes byte layout | Confirm source/Auction/Snake content invariants and compacted artifact sizes after every handoff or schema rebuild |
| Validation/audits | Calibration, ADP, and template-quality checks | active | Audit tables plus study outputs support review; `Best_Ball_ADP_Audit` flags draftable players with missing/fallback/suspicious ADP context; `Salary_Backtest_Predictions` and `Salary_Validations_Resid` retain raw/normalized salary predictions, causal cutoffs, candidate coverage, and method provenance; selected-roster diagnostics retain unique-player and selection-weighted residual views, exact scenario-center shifts, and roster-gap decomposition. The selection-surcharge study persists rolling coefficients, player corrections, fixed-roster gap checks, feasible-only quality, and 4,000 paired policy cells | Add a genuinely nested method holdout if model-selection claims require it, and evaluate current 2026 surcharge stability before production promotion |
| Managed keeper portfolio | One-year keeper call option across bench slots | active | Organic Target construction scores expected best positive first-year `price + $10` surplus across every nominal bench player. V2 next-year residual draws are conditional on appearance with player common-factor `rho=0.25`; a separate Bernoulli `pred_appear_ny` draw zeros future market value before surplus. Current-year uncertainty remains the joint donor residual/path and never enters keeper residual sampling | Monitor prospective keeper concentration, appearance calibration, current holdout EV/p10, and real-world eligibility; do not consume conditional PPG without the appearance draw |
