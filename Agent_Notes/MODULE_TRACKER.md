# Module Tracker

| Module | Scope | Status | Current Baseline | Next Step |
| --- | --- | --- | --- | --- |
| Annual model parameter cache | Current- and next-season V2 annual model selection by season, league, runner, and model | active in 2026 production | Random forest fits use four internal workers. Exact data/model-spec fingerprints govern 36 cached 2026 selections across DK, NFFC, and beta; cache hits skip hyperparameter search but always refit and predict. The promoted cache has 7/7 current and 5/5 next hit receipts in every league V2 database and is backed up and rollback-protected with the release | Monitor fingerprint misses on future input changes; review and register the next season rather than copying 2026 entries |
| Upside objective validation | Rare player outcomes and roster championship utility for weekly templates, Auction, and Snake | validation policy active; production and app objectives unchanged | The causal q90 event combines +5 active PPG over the held-out projection with managed contribution above a five-year strictly prior position threshold; q95 is the severity sensitivity. PPG/contribution CRPS remain calibration gates, while rare-event Brier/log loss, average precision/top-decile lift, tail-utility CRPS, and paired 12-team championship Brier/log loss are secondary objectives. The WR `wr_ppg225_both025` arm improved all four player-level q90 calibration/tail cells but failed roster championship transport in three of four league-period cells. DK absolute tail probability is materially underpredicted. The flatter-distance arm also remains rejected | For future matcher tests, require expected-score/calibration non-inferiority and cross-league/temporal tail replication. Before any app promotion, causally recalibrate DK tail probability and run common-bank historical Auction Buy/Pass and Snake forced-pick replays. Prototype a lexicographic decision rule: among actions within 0.25% of the best expected roster score, prefer the strongest paired championship-probability lower bound |
| PFF advanced-stat screening | Leakage-safe prior-season receiving/rushing features for projection and weekly-template challengers | research screen complete; production unchanged | Raw PFF IDs join through canonical `player_identity`; season `t` uses only `t-1` values, neutral position-season priors, and fixed sample reliability. The locked blend, including expert projection and ADP, is the baseline; each rate is also compared with a PFF opportunity-only control. Across 124 DK/beta position-metric rows, TE missed tackles per reception was the only DK candidate whose season-bootstrap intervals cleared zero for both PPG RMSE and q90 Brier. It improved both objectives directionally in beta, won 6/8 seasons per objective and scoring format, and won every 2023-2025 season. Its persistence is moderate (0.330) and selected-feature redundancy low (max absolute Spearman 0.094). TE YAC/route is second tier; YPRR, first downs/route, targets/route, and route grade are highly overlapping and mostly redundant with prior production | Prespecify TE missed tackles per reception as the primary candidate in a full locked-model and template-matcher validation, with TE YAC/route as a separate sensitivity rather than a bundled feature. Retain neutral/reliability handling and opportunity controls; do not promote from the broad screen |
| Production refresh orchestration | Canonical downstream build, validation, app candidates, and promotion | schema-5 2026 cycle active in production | `Scripts/V2/refresh_production.py --year 2026` starts after the manual `1_Update_Projections.py` ingest and stages eight source/model databases plus both app bases. It rebuilds current/next Model_Inputs, separate DK/NFFC/beta V2 objectives and shadows, annual keepers, the idempotent handoff, three weekly surfaces/audits, salary/validation outputs, and a mandatory fresh 1,000-trial reserve. Manifest schema 5 retains immutable hash-checked bases, stages the annual parameter cache, isolates V2 foundation/feature processes, and uses bounded retries only for classified Windows native crashes. `production_cycle.py` binds exact annual runners, versions, floors, horizons, template eras, and source-feed contracts; its receipt/hash is immutable through resume and promotion. The 24-step run `20260801T194518Z_0473c7e7` passed every gate and promoted ten model/app databases with durable backups and no cleanup warnings. Current season 2027 fails closed until separately registered | For 2027 add new annual inputs, validation runners, versions, exclusions/floors, and one approved cycle entry rather than copying 2026 evidence |
| Data generation | Projection, ADP, salary, injury, and compiled model-input tables | active; ADP provider slices repaired; Windows grouped-rolling fault removed | Numbered scripts under `Scripts/Data_Generation/` populate SQLite model inputs and downstream context. `ADP_Ranks` now validates and atomically replaces source-scoped slices; the live source has 42 restored 2025 FantasyPros QBs, one 32-team 2026 DST snapshot, and no duplicate or nonstandard FantasyPros positions. `4_Data_Compile.py` computes period-three player rolling means/maxima from lags, avoiding the pandas 2.3.1 grouped-window corruption; all 18 generated 2026 tables match the prior successful build within `1e-12`. The live beta salary method is `current_locked_spec_v6_v2_population_11f`: exactly 328 canonical keys, 326 `ProjOnly` plus Stefon Diggs/Deebo Samuel V2 fallbacks, 14 keyed keepers, and a top-142 non-keeper total of `$3,071`. The fresh 1,000/1,000 reserve covers 314 non-keepers with expected roster reserve `$8.5598` and explicitly transfers historical v5 calibration to current v6 | Start a fresh governed V2 refresh so the corrected historical `adp_fpros` vote changes fingerprints and derived artifacts; keep weekly donor tails diagnostic and reassess the separate remaining-risk guardrail |
| Projection V2 foundation | Identity, outcomes, preseason source evidence, controlled feature mart, and locked current/following-season model evidence | expanded population active in production | DK/beta builds share 6,655 identities, 55,914 aliases, 31,798 projection values, 28,801 market/rank values, and 13,909 feature keys. NFFC contributes one composite ADP vote; its four contest rows remain identity/candidate evidence only. The canonical current feeds contain 416 DK, 497 NFFC (431 offense plus 33 TK/33 TDSP), and 243 ETR rows; the latest local NFFC/ETR exports are dated 2026-07-27. Locked current RMSE is 3.1076 DK and 2.8841 beta. The locked V2 current/next shadows are authoritative for 351 DK (56/101/143/51) and 328 beta (50/95/133/50) rows; legacy current/next values are audit-only. DK is the 326-player core plus top-280 market union after eight governed no-center exclusions; beta is core plus top-180 ETR overall-rank ordering and keepers. The 1,490-row eligibility audit, canonical Tetairoa McMillan/Amon-Ra identities, unchanged hashes across all eight governed handoff tables, and 100% keyed coverage are live. Governed identity aliases, effective source seasons, beta QB sack scoring, and the 50-row FFToday quarantine remain auditable. DK/beta historical donor residuals still use the validated legacy OOS center, including 39 governed beta 2018 QB diagnostic absences; NFFC uses its separately validated scoring-matched expert center. Normalized expert-rank level is an unpromoted challenger; the scoring-matched raw percentile-plus-coverage candidate failed cross-league gates, raw log was not distinguishable from normalized rank in the paired study, and all raw-rank variants remain audit-only. The expert-minus-projection gap is rejected | Monitor prospective calibration, normalized expert-rank confirmation, beta fringe-QB provider coverage, and keeper appearance calibration; only reconsider raw log under an independently prespecified future-origin test, and never substitute legacy current/next, DK, quarantined evidence, or zero sacks for an unavailable locked center |
| NFFC Snake offense path | Independent NFFC scoring, projection handoff, ADP, weekly templates, and Snake selection | active in production as offense-only 3RR; not complete contest support | The 2026 cycle registers `v2_conditional_ppg_2026_candidate_nffc_v1`, separate NFFC current/next scoring hashes, the core plus top-360 canonical offensive NFFC ADP eligibility union, and 17-week templates drawn from 2021-forward donors. Historical/current scoring-sensitive matcher context is authoritative from the NFFC-scored V2 preseason consensus; DK-scored `Model_Inputs` context is audit-only, and receiver matching uses QB1 passing points rather than total QB fantasy points. Historical donors use `nffc_scored_expert_consensus`. The promoted surface has 1,509 templates, a 383-player map, 80 donors per player, 17 populated weeks, and zero Snake smoke errors. Canonical `TK`/`TDSP` rows remain market audit entities but are filtered from model and app populations. The selector is explicitly offense-only 3RR; K/DST, official 30-round roster composition, and alternate NFFC contest formats are not implemented | Retain the expert donor center, monitor live use, and do not call the adapter complete NFFC contest support |
| Stacking model | Residual models and final prediction tables | active | `s1_Stacking_Model.py` exposes strict-prior-origin empirical residual calibration, `s2_RunAll.py` writes target/cutoff provenance to `Model_Validations_Resid`, and `s3_Model_Ensemble.py` persists current and historical final ensembles; the existing 42,351 validation rows were backfilled into 6,006 `Final_Validations_Resid` rows with horizon-aware donor embargoes | Use `Final_Validations_Resid` for historical ensemble features and interval audits; never evaluate terminal next targets when `resid_target_available = 0` |
| Managed auction app | Auction draft optimizer, weekly managed roster scoring, waiver baselines | active | Fantasy_Football_App loads `League_Keepers`, normalizes salary draws, requires fully paired Buy/Pass nomination trials, validates nominee position and full-roster constraints, and uses a decision-price-invariant globally anchored adaptive isotonic Max Bid search; managed scorers consume a separate played-week mask so source-observed zero/negative outcomes are not replaced with hindsight; each organic Target outer roster repeats the constraint-safe exact-scored expected-profile swap until no improvement remains, capped at 12 swaps, while forced Buy/Pass and Current Nomination remain unrefined. Target defaults to 320 organic trials in eight balanced 40-trial logical blocks, then uses four broad paired pilot scenarios over eligible players outside 20 protected heuristic candidates and appends four discoveries. The resulting 24-player cohort receives 64 fresh preliminary pairs. Fresh 96-pair confirmation preserves the ten evidence-priority candidates and appends any missing members of the four highest-market-salary usable preliminary candidates, producing 10-14 unique confirmations; confirmed market anchors remain visible in the 30-row board. Pilot values never enter evidence. Both forced stages retain eight logical scenario blocks independent of the one-to-eight process count. A position-neutral leave-one-player-out random-effects model combines only comparable forced stages with logical-block-aware uncertainty, and excess preliminary/confirmation disagreement adds continuous between-stage variance. Every usable forced row ranks on posterior LCB80 regardless of evidence label; confirmed, preliminary, mixed, negative, conflicting, and low-fit-pivot labels do not replace the score. Forced rows precede a separate Organic Gain watchlist with explicit pair coverage. Target Pass dynamically promotes Top-N rank N+1 when it excludes an original Top-N candidate. The compact table shows eight decision fields and places separate preliminary/confirmation gains, confirmation route, posterior support, disagreement, forced-Buy roster mix, WR/TE decision-dollar change, completion-player, and price diagnostics under collapsed Evidence Details. Current Nomination remains the exact-price authority. UI dropdowns expose starter, bench, position-max, and waiver PPG settings with a default two-TE roster cap, collapsed semantic sidebar groups, pre-solve feasibility checks, and a cached exact Weekly Template Comp Explorer with donor weights, outcomes, participation, and position-specific match context. Target and Nomination add the active static half-strength selection reserve only to unresolved-player affordability, while keepers, entered purchases, and explicit nominee prices remain deterministic. Candidate-rebased Target markets remove only sampled market price from league money, never the personal reserve. Market, reserve, and decision dollars remain separate, and the runtime checkbox now defaults off so reserves start at zero but remain available as an explicit opt-in comparison | Benchmark rank stability and missed regret across representative draft states and paired seeds; use Current Nomination for exact bid decisions and do not interpret the targeted reserve as complete salary-risk coverage |
| Sequential auction Target Board | Non-anticipating auction-path target prioritization | active | Fantasy_Football_App defaults to a history-only sequential policy; the prior staged full-roster Target remains available as `Legacy Oracle`. At budget 320, 24 heuristic candidates expand to a 64-player exact discovery pool with capped same-position peers plus position-relative Premium/Mid/Value coverage; all receive exact managed-season evidence. Confirmed and discovery rows share one global LCB80 comparison table with explicit stage labels and filters; discovery rows are shaded, retain exploratory PROMISING/WATCH/LOW labels, and cannot issue TARGET or max-bid guidance. Fresh confirmation allocates 18 players across leaders, peers, position/tier sentinels, and market anchors; the three highest-salary personally affordable/position-legal remaining players are protected exact confirmations and stay on the main board even when labeled `INCOMPLETE`. They consume existing slots. The leading ten usable confirmations receive low/high price curves. Production v6 draws construction, salary, nomination, and weekly evidence on the full canonical player universe before masking the live pool; player templates and candidate seeds are player-keyed. Four partial-crossed evidence blocks each build a point-forecast policy from 32 stratified joint historical residual/weekly-path donors. Discovery uses 8 paths by 16 seasons per block; confirmation uses 12 paths by 64 seasons per block, retaining 3,072 confirmation cells and 256 distinct seasons. A random-effects action SE combines within-block crossed uncertainty with between-block policy disagreement, and the UI shows block-positive share/range. Four spawned workers preserve exact parity. A 16-root replay of the prior evidence engine raised AJ-on/off Bijan correlation from 0.25 to 0.993, held the AJ-state delta to +0.37 mean / 0.96 SD, and reduced seed SD from 21.07 to 7.99-8.15. A paired 2022-2025 replay found that blindness removes a `$14.4` full-surface scenario discount but leaves a similar `$17.3` actual-minus-point initial-plan residual; live recourse is legal but strands `$45.4` on average | Tune history-only target ceilings and late-draft reinvestment to reduce stranded salary without exposing future prices; then rerun representative seed-stability, rank-regret, and historical-price benchmarks |
| Managed auction bench policy | Bench opportunity cost, waiver replacement, breakout upside, and keeper value | active | Production keeps the estimated waiver PPG as the weekly replacement floor, removes the generic 0.25 bench-P90 bonus and raw next-year blend, and uses current-season managed value before the separate soft keeper portfolio. No age, role, or designated lottery-ticket quota is imposed | Monitor prospective roster composition and latency; do not raise waiver PPG as an upside proxy or reintroduce a broad additive bench bonus |
| Best-ball weekly templates | Historical weekly profile tables and current-player template pools | DK, NFFC, and beta slices active; role-tier validator active | Live current maps contain 350 DK, 383 NFFC, and 328 beta players with 80 donors each; context joins are key-first and canonical ADP team fills are provenance-labeled. DK/beta use 2008-2025 16-week donors; NFFC uses 1,509 2021-2025 templates and 17 weeks with `nffc_scored_expert_consensus`. Yardage bonuses and sacks flow through weekly `active_ppg` and optimizer selection. All three current maps have complete identity and point-center coverage, and their production template audits have zero unmatched rows. Core matcher validation prioritizes active-PPG CRPS, contribution for one-SE near-ties, aggregate availability, and replacement-aware roster CRPS; rejected role and distance challengers remain unpromoted | Use the role-tiered validator for future matcher studies; monitor prospective calibration and comp composition; retain the 39 explicit beta 2018 QB diagnostic absences and NFFC expert donor center |
| Simulation database export | App-facing SQLite handoff | active | The governed cutover synchronizes 20 source-owned generated tables to Auction while preserving six app-owned tables and copies the complete source database to Snake. Both staged app artifacts are vacuumed before parity checks and smoke tests; receipts retain before/after page usage, require zero freelist pages, and fail above GitHub's 100 MiB blob limit. Every generated Auction table matches staging, all six app-owned tables remain unchanged, and every Snake table matches source logically even though compaction changes byte layout | Confirm source/Auction/Snake content invariants and compacted artifact sizes after every handoff or schema rebuild |
| Validation/audits | Calibration, ADP, and template-quality checks | active | Audit tables plus study outputs support review; `Best_Ball_ADP_Audit` flags draftable players with missing/fallback/suspicious ADP context; `Salary_Backtest_Predictions` and `Salary_Validations_Resid` retain raw/normalized salary predictions, causal cutoffs, candidate coverage, and method provenance; selected-roster diagnostics retain unique-player and selection-weighted residual views, exact scenario-center shifts, and roster-gap decomposition. The selection-surcharge study persists rolling coefficients, player corrections, fixed-roster gap checks, feasible-only quality, and 4,000 paired policy cells | Add a genuinely nested method holdout if model-selection claims require it, and evaluate current 2026 surcharge stability before production promotion |
| Managed keeper portfolio | One-year keeper call option across bench slots | active | Organic Target construction scores expected best positive first-year `price + $10` surplus across every nominal bench player. V2 next-year residual draws are conditional on appearance with player common-factor `rho=0.25`; a separate Bernoulli `pred_appear_ny` draw zeros future market value before surplus. Current-year uncertainty remains the joint donor residual/path and never enters keeper residual sampling | Monitor prospective keeper concentration, appearance calibration, current holdout EV/p10, and real-world eligibility; do not consume conditional PPG without the appearance draw |
