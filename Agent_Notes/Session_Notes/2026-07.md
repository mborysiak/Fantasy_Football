# 2026-07 Session Notes

## 2026-07-06 - Agent Note System Bootstrap

- Request:
  - review the Daily Fantasy Sims V2 and NBA Sims agent-note structures and set
    up an appropriate system for Fantasy Football plus the Snake app
- Key decisions:
  - used the NBA-style `Agent_Notes/` pattern as the baseline because these repos
    are hand-built and should not get a heavy process layer
  - added focused `docs/data_contracts/`, `docs/runbooks/`, and
    `research/studies/` folders for app-facing schemas and real investigations
  - documented cross-repo ownership: this repo builds `Simulation.sqlite3`, while
    the Snake app consumes the copied DB
- Files changed:
  - `AGENTS.md`
  - `Agent_Notes/README.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/CROSS_REPO_CONTEXT.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
  - `docs/data_contracts/best_ball_weekly_tables.md`
  - `docs/runbooks/best_ball_weekly_build.md`
  - `research/README.md`
- Verification:
  - reviewed reference note systems in `Daily_Fantasy_Sims_V2` and `NBA_Sims`
  - no model code executed; documentation/bootstrap task
- Follow-ups:
  - keep future durable best-ball decisions in `DECISION_LOG.md`
  - move reusable calibration and audit outputs into dated research studies

## 2026-07-06 - Best-Ball Durable Decision Notes

- Request:
  - identify and add any missing notes based on the recent best-ball modeling
    and app work
- Key decisions:
  - promoted the template-matching, role-feature, QB context, non-QB
    zero-active filtering, weighted sampling, and ADP audit decisions into the
    durable decision log
  - expanded the module tracker and table contract so future app/model work has
    the relevant context without reading the full implementation first
- Files changed:
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
  - `docs/data_contracts/best_ball_weekly_tables.md`
  - `docs/runbooks/best_ball_weekly_build.md`
- Verification:
  - documentation-only update; no model code executed
- Follow-ups:
  - keep app-facing schema decisions mirrored in the Snake repo notes

## 2026-07-07 - League-Aware Best-Ball Tables

- Request:
  - make `s4_Best_Ball_Weekly.py` and the best-ball weekly tables support
    side-by-side league settings such as dk, beta, nffc, and nv
- Key decisions:
  - added league metadata to historical weekly templates and template pools
  - offset template IDs by league while preserving a `template_local_id` for
    within-league diagnostics
  - changed best-ball writes to preserve non-active league slices instead of
    replacing the entire table set on each run
  - updated the Snake app weekly-template join to include template league context
    when the copied DB exposes `Best_Ball_Weekly_Templates.league`
- Files changed:
  - `Scripts/Modeling/s4_Best_Ball_Weekly.py`
  - `docs/data_contracts/best_ball_weekly_tables.md`
  - `docs/runbooks/best_ball_weekly_build.md`
  - Snake app `app/zSim_Helper.py`
  - Snake app data contract/runbook
- Verification:
  - `python -m py_compile Scripts/Modeling/s4_Best_Ball_Weekly.py`
  - `python -m py_compile Fantasy_Football_Snake/app/zSim_Helper.py`
  - import-level checks for `s4_Best_Ball_Weekly.py` and Snake `zSim_Helper.py`
  - ran `Scripts/Modeling/s4_Best_Ball_Weekly.py` for active config
    `LEAGUE='beta'`
  - inspected source and copied `Simulation.sqlite3` tables: `beta` and `dk`
    slices coexist, and all pool rows join to same-league templates
  - Snake weekly-template cache read against both copied `beta` and `dk` slices
  - in-memory player-pool audit check for the new league-aware template merge

## 2026-07-07 - Auction Salary Residual Quantiles

- Request:
  - replace spline/truncated-normal salary uncertainty in
    `s4_Salaries_Injuries.py` with historical residual quantile calibration
    while keeping the app-facing legacy salary columns
- Key decisions:
  - kept the point salary model flow intact and calibrated uncertainty from
    historical out-of-fold residuals only
  - added `salary_resid_5/10/25/75/90/95` to `Salaries_Pred`
  - derived `std_dev`, `min_score`, and `max_score` from the residual quantiles
    for backward compatibility with `Fantasy_Football_App`
  - kept known keepers deterministic in the salary prediction output
- Files changed:
  - `Scripts/Modeling/s4_Salaries_Injuries.py`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`
  - installed missing runtime packages in the active Python environment:
    `sklearn-quantile`, `catboost`, `hyperopt`, `optuna`; pinned
    `setuptools<81` and `scikit-learn==1.7.2` to avoid dependency/runtime issues
  - ran `Scripts/Modeling/s4_Salaries_Injuries.py` for active
    `YEAR=2026`, `LEAGUE='beta'`
  - inspected `Salaries_Pred`: new columns present, 2026 `betapred` rows have
    no null residuals, monotonic quantiles, and valid `min_score <= salary <= max_score`
  - OOF residual coverage: all-position p5-p95 0.887, p10-p90 0.796,
    p25-p75 0.509
  - copied source `Simulation.sqlite3` to `Fantasy_Football_App` and confirmed
    the app's legacy salary query still reads 2026 `betapred`
- Follow-ups:
  - port Fantasy_Football_App salary sampling from truncated normal to the new
    salary residual quantile columns

## 2026-07-08 - Auction Salary Bootstrap Residual Quantiles

- Request:
  - make salary residual quantile calibration mirror the `s1` projection
    residual bootstrap approach
- Key decisions:
  - updated `apply_salary_resid_quantiles` to average residual quantile
    predictions across bootstrap resamples with replacement
  - kept the first iteration on the full OOF residual set, then used
    `bootstrap_iters=50`, `bootstrap_frac=1.0`, `bootstrap_replace=True`,
    and `random_state=42`
  - preserved the existing position fallback behavior and the current
    salary-specific tuning values `min_n=50`, `max_bins=15`
- Files changed:
  - `Scripts/Modeling/s4_Salaries_Injuries.py`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`
  - synthetic helper-level bootstrap test confirmed no null residuals,
    monotonic quantiles, and bootstrap metadata on bucket diagnostics
  - attempted full active salary rebuild, but it timed out after 30 minutes;
    stopped the orphaned `Scripts/Modeling/s4_Salaries_Injuries.py` process
- Follow-ups:
  - rerun the full salary rebuild with a longer timeout or from the notebook
    before treating `Salaries_Pred` as refreshed with bootstrap residuals

## 2026-07-08 - Auction App Residual Sampling Port

- Request:
  - update the Fantasy_Football_App auction ILP to use residual quantiles for
    both projections and salaries, and remove unused upside/top probability
    code
- Key decisions:
  - changed the auction app prediction input from `Final_Predictions` to
    `Final_Predictions_Resid`
  - sampled current-year projections, next-year projections, and auction
    salaries from empirical residual quantile columns
  - kept manual salary overrides on the base salary and legacy salary bounds;
    residual columns continue to describe model uncertainty
  - removed `prob_top`, `prob_upside`, `upside_frac`, and truncated-normal
    sampling paths from the app auction flow
- Files changed:
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `Fantasy_Football_App/app/ffapp.py`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/zSim_Helper.py Fantasy_Football_App/app/ffapp.py`
  - smoke-tested `FootballSimulation` against the copied app
    `Simulation.sqlite3`: residual columns loaded without nulls, projection and
    salary sample matrices built, salary samples clipped to at least 1, and a
    3-iteration ILP run completed
  - confirmed no remaining app references to `prob_top`, `prob_upside`,
    `upside_frac`, `pred_prob`, or `trunc_normal`
- Follow-ups:
  - rerun the full salary rebuild with bootstrap residuals before treating the
    copied salary residual columns as fully refreshed

## 2026-07-08 - Auction App Year/League Selectors

- Request:
  - add Fantasy_Football_App UI controls for selecting the auction year and
    league
- Key decisions:
  - populate the sidebar selectors from the copied `Simulation.sqlite3`
    intersection of `Final_Predictions_Resid` and `Salaries_Pred`
  - include salary source as a selector so current-year predicted salary rows
    and historical actual salary rows can be routed through the same app path
  - reset stored simulation results and focus-player state when the selected
    year, league, or salary source changes
- Files changed:
  - `Fantasy_Football_App/app/ffapp.py`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/ffapp.py`
  - Streamlit `AppTest` from the app directory rendered `Year`, `League`, and
    `Salary Source` select boxes with no exceptions
  - live Streamlit server on port 8502 returned HTTP 200 after the change

## 2026-07-08 - Managed Auction Weekly Scorer

- Request:
  - begin converting the auction app from static projected-points optimization
    toward managed weekly lineup value with waiver fallback
- Key decisions:
  - start with a standalone roster scorer before replacing the auction ILP
    objective
  - reuse the existing weekly template tables and weighted template sampling
    from the best-ball pipeline
  - score lineups as managed starts: `1QB`, `2RB`, `2WR`, `1TE`, and `2FLEX`
  - choose starters from preseason/learned decision scores, then score the
    selected starters using sampled weekly outcomes
  - skip real players with sampled zero-output weeks when choosing starters so
    waiver baselines can cover bye/inactive slots
  - include visible waiver baseline constants as placeholders for future
    calibration/UI controls
- Files changed:
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `docs/data_contracts/best_ball_weekly_tables.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/zSim_Helper.py Fantasy_Football_App/app/ffapp.py`
  - smoke-tested `2026 beta` template loading from the copied app
    `Simulation.sqlite3`
  - smoke-tested a salary-sorted 13-player roster with waiver fill-ins and a
    balanced 13-player roster with only one waiver start across 16 sampled
    weeks
- Follow-ups:
  - calibrate or expose waiver baselines
  - convert sampled managed roster value into the auction ILP objective

## 2026-07-08 - Managed Auction ILP And Waiver Baselines

- Request:
  - continue from the managed weekly scorer by adding waiver baseline controls
    and updating the auction ILP objective
- Key decisions:
  - estimate waiver baselines from likely undrafted players in the current
    copied app database, then expose `QB`, `RB`, `WR`, and `TE` overrides in
    the sidebar
  - default the managed roster to `1QB`, `2RB`, `2WR`, `1TE`, `2FLEX`, and
    five bench slots
  - replace static PPG objective coefficients with candidate marginal managed
    weekly value over the current roster plus waiver baseline
  - keep the ILP linear by using per-candidate marginal values, with a bench
    upside weight term for high-tail weekly deltas
  - use managed position min/max constraints and default `QB` max of one to
    reflect waiver-streaming behavior in a 1QB managed league
- Files changed:
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `Fantasy_Football_App/app/ffapp.py`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/zSim_Helper.py Fantasy_Football_App/app/ffapp.py`
  - direct managed ILP smoke test for active `2026 beta` context solved a
    13-player roster under the salary cap and position constraints
  - Streamlit `AppTest` rendered year/league/source, lineup, bench, waiver, and
    bench-upside controls with no exceptions
  - wrapper-level `ffapp.run_sim` smoke test completed three managed ILP
    iterations
- Follow-ups:
  - validate waiver baseline levels and bench-upside weight against historical
    managed-league expectations
  - consider a second-pass roster interaction refinement because the ILP still
    uses linearized per-candidate marginal values

## 2026-07-08 - Managed Auction Summary Panel

- Request:
  - remove bench budget allocation and add a small managed lineup summary panel
- Key decisions:
  - removed the Streamlit `Bench Salary` reserve because the optimizer now
    directly drafts and values bench slots
  - kept the full auction cap as the simulation salary cap
  - added managed simulation summary tracking for season EV, weekly EV,
    season p10/p90, waiver starts, bench starts, salary spend, and trial count
  - define bench starts as starts by rostered players outside the preseason
    selected starting lineup
- Files changed:
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `Fantasy_Football_App/app/ffapp.py`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/zSim_Helper.py Fantasy_Football_App/app/ffapp.py`
  - Streamlit `AppTest` rendered the app without the old `Bench Salary` input
    and with no exceptions
  - direct helper and `ffapp.run_sim` smoke tests completed three managed ILP
    iterations and returned populated summary dictionaries

## 2026-07-09 - Managed Auction Value Batching

- Request:
  - improve auction ILP efficiency by batching managed-value objective samples
- Key decisions:
  - added a managed-value sample matrix helper so expensive weekly-template
    marginal values are built once per refresh block
  - defaulted to 25 managed-value objective columns per 100-iteration refresh
    block, with short runs capped to the remaining iteration count
  - each ILP iteration now samples one cached managed-value column while salary
    samples continue to vary per iteration
- Files changed:
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/zSim_Helper.py Fantasy_Football_App/app/ffapp.py`
  - direct managed ILP smoke test completed with `managed_value_options=2`
  - wrapper-level `ffapp.run_sim` smoke test completed three managed ILP
    iterations and returned populated summary metrics
  - Streamlit `AppTest` rendered with no exceptions; live app on port 8502
    returned HTTP 200

## 2026-07-09 - Managed Auction Marginal Value Vectorization

- Request:
  - vectorize managed auction marginal value calculation after batching
- Key decisions:
  - replaced the candidate-by-candidate managed lineup rescoring loop with a
    vectorized position/week threshold calculation
  - preserve the existing managed lineup semantics: starters are chosen by
    decision score, real zero-output weeks are unavailable, waiver players fill
    gaps, and skill-position candidates can add value through starter slots or
    flex
  - keep the slower loop only in external smoke-test comparison, not in app
    runtime
- Files changed:
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/zSim_Helper.py Fantasy_Football_App/app/ffapp.py`
  - compared vectorized output to a local copy of the previous loop for both
    empty-roster and active-base-player cases; max difference was under
    `1e-4`
  - focused timing check on active `2026 beta` sample showed roughly 75x faster
    managed marginal value calculation
  - direct managed ILP smoke test completed three iterations with populated
    summary metrics
  - Streamlit `AppTest` rendered with no exceptions; live app on port 8502
    returned HTTP 200

## 2026-07-09 - Managed Auction ILP Matrix Reuse

- Request:
  - improve auction ILP efficiency by reusing static managed ILP matrices
    instead of filtering the player pool
- Key decisions:
  - kept the full candidate pool intact to avoid strategic filtering risk
  - added a managed static-matrix builder for roster-size equality, fixed-player
    lower bounds, position min/max constraints, and optional top-N salary
    constraint
  - rebuild only the sampled salary row and objective vector inside each
    managed ILP iteration
  - left the legacy non-managed path unchanged
- Files changed:
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/zSim_Helper.py Fantasy_Football_App/app/ffapp.py`
  - direct managed ILP smoke test completed five iterations with populated
    summary metrics
  - wrapper-level `ffapp.run_sim` smoke test completed five managed ILP
    iterations
  - Streamlit `AppTest` rendered with no exceptions; live app on port 8502
    returned HTTP 200

## 2026-07-09 - League Keeper Table

- Request:
  - persist the keeper inputs from `s4_Salaries_Injuries.py` so the auction app
    can read the same league/year keeper context
- Key decisions:
  - added `League_Keepers(year, league, player, keeper_salary)` with a primary
    key on the league/year/player slice
  - replace only the active `YEAR`/`LEAGUE` slice and clean player names before
    writing
  - keep the app-side table read as a follow-up after the salary script rebuild
- Files changed:
  - `Scripts/Modeling/s4_Salaries_Injuries.py`
  - `docs/data_contracts/auction_salary_tables.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/MODULE_TRACKER.md`
- Verification:
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`
- Follow-ups:
  - save the complete keeper dictionary, rerun the active salary build, inspect
    the resulting table, and port the app keeper load to `League_Keepers`

## 2026-07-09 - Managed Auction One-Swap Refinement

- Request:
  - reduce fixed-base additive marginal-value bias with one interaction-aware
    swap pass after each managed auction ILP solve
- Key decisions:
  - retain the weekly scores and managed decision scores associated with each
    batched objective column
  - use the ILP as the fast global candidate generator, then evaluate every
    feasible incoming player against each removable selected player through
    vectorized rebasing
  - accept at most one swap and only when exact managed season points improve
    on the matched weekly scenario
  - preserve fixed players, manual fixed-player salaries, salary cap, position
    minimums/maximums, and the optional top-N salary constraint
  - keep the first-pass bench-upside term while scoring the refinement by full
    managed lineup points, where bench option value is realized through starts
- Files changed:
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile Fantasy_Football_App/app/zSim_Helper.py Fantasy_Football_App/app/ffapp.py`
  - helper-level test preserved fixed players and manual salaries while meeting
    cap, position, roster-size, and top-N constraints
  - vectorized refinement matched the best exact score from a brute-force
    one-swap search on the active 180-player pool
  - integrated 20-trial managed simulation completed all trials; all 20 made an
    improving swap with a mean matched-scenario gain of about 71 season points
  - Streamlit `AppTest` rendered with no exceptions; live app on port 8502
    returned HTTP 200

## 2026-07-09 - Keeper-Adjusted Salary Calibration

- Request:
  - correct historical OOF salary reconciliation so keeper dollars are not
    added to non-keeper predictions before residual fitting
- Key decisions:
  - clean current keeper names before model joins and table writes
  - preserve keeper count and spend before model-input joins can remove rows
  - reconcile historical OOF predictions to actual spend for the exact
    represented non-keeper rows
  - reconcile current non-keeper predictions to league budget minus keeper
    spend across roster slots minus keeper count
  - scale salary dollars above the `$1` floor proportionally instead of adding
    a flat rounded amount to every player
  - report leave-one-year-out residual interval coverage, residual bias by
    position/salary band, and current point-versus-sampled market totals
- Files changed:
  - `Scripts/Modeling/s4_Salaries_Injuries.py`
  - `docs/data_contracts/auction_salary_tables.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/DECISION_LOG.md`
- Verification:
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`
  - helper tests reconciled both historical non-keeper-only and current
    keeper-inclusive examples to their intended targets
  - real-data helper test reconciled represented 2023-2025 OOF totals, set the
    2026 available point market to `$3,109`, and made Jaxon Smith-Njigba a
    deterministic `$42` keeper
- Follow-ups:
  - rerun the full salary model and review the new notebook audits before
    updating the auction app to consume `League_Keepers` and normalize salary
    draws against remaining league money

## 2026-07-09 - Auction App Keeper And Salary Market

- Request:
  - fix the removed hardcoded keeper variable and make auction salary handling
    consistent with the refreshed keeper-aware salary model
- Key decisions:
  - load the selected year/league keeper rows from `League_Keepers`
  - prefill keeper costs, mark keeper rows in the editor, and remove unowned
    keepers from the ILP candidate pool
  - exclude keepers from the displayed non-keeper auction-pace calculation and
    remove the old global spending-rate salary adjustment
  - calculate remaining league money/slots from all keeper commitments and
    entered non-keeper results
  - normalize each sampled salary vector proportionally above the `$1` floor so
    its top remaining-slot market equals the available league budget
  - rebuild the top-N constraint pool after unavailable keeper rows are removed
- Files changed:
  - `Fantasy_Football_App/app/ffapp.py`
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `docs/data_contracts/auction_salary_tables.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile app/ffapp.py app/zSim_Helper.py`
  - confirmed 14 keepers and `$467` of keeper spend load into the app
  - confirmed the initial remaining market is 142 slots and `$3,109`
  - confirmed normalized sampled top-142 salaries total exactly `$3,109`
  - direct three-iteration managed ILP smoke test completed all trials
  - Streamlit `AppTest` rendered 14 keeper rows with no exceptions

## 2026-07-10 - Current Nomination Buy Versus Pass

- Request:
  - replace Focus Player output with an actionable Current Nomination evaluator
- Key decisions:
  - compare forced Buy and Pass branches using the same projection residuals,
    salary residuals, weekly templates, and decision-score scenarios
  - remove the same decision price and league roster slot in both branches so
    the comparison isolates whether the player belongs on the user's roster
  - construct Buy and Pass rosters from managed values averaged across cached
    scenarios so neither branch can select players using the scored outcome
  - do not apply the scenario-specific one-swap refinement in nomination
    analysis because it would reintroduce outcome hindsight
  - score each completed roster with exact managed weekly lineup points on the
    paired outcome after roster construction
  - calculate Roster Max Bid as the highest integer price with non-negative mean
    Buy Edge, treating cap-infeasible prices as Pass outcomes
  - calculate Fit Rate with a third unconstrained solve at the entered price
  - replace the Focus Player grid checkbox with a searchable Current Nominee
    selector and relabel Target Board selection frequency as Fit Rate
- Files changed:
  - `Fantasy_Football_App/app/ffapp.py`
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `docs/data_contracts/auction_salary_tables.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile app/ffapp.py app/zSim_Helper.py`
  - repeated fixed-seed nomination evaluations returned identical outputs
  - nomination smoke tests completed for current and next-year-weighted paths
  - default 100-trial Streamlit evaluation completed in about 10 seconds after
    removing scenario-specific refinement
  - 100-trial Target Board Streamlit run completed without exceptions

## 2026-07-10 - Nomination Confidence And Opportunity Board

- Request:
  - distinguish strong auction opportunities from small positive EV, show
    broader per-player EV, and quantify how alternatives leaving changes value
- Key decisions:
  - summarize paired edge with season points per week and a bootstrap 90%
    confidence interval for the mean
  - replace binary recommendation output with `STRONG BUY`, `BUY`, `LEAN BUY`,
    `NEUTRAL`, and `PASS` tiers based on mean edge, confidence, and win rate
  - classify Gibbs at `$115` as `LEAN BUY`: `+11.4` season points, `+0.71` per
    week, and a mean-edge interval of about `-6.6` to `+28.9`
  - rerun the nominee after separately removing the two most common alternatives
    at Market Price to expose scarcity and remaining-budget effects
  - add a fast all-player Opportunity Board at Market Price using one shared
    expected managed-value bank and 25 paired outcome contexts
  - label board EV as approximate and retain the 100-trial Current Nomination
    price search as the exact decision surface
- Files changed:
  - `Fantasy_Football_App/app/ffapp.py`
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `docs/data_contracts/auction_salary_tables.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile app/ffapp.py app/zSim_Helper.py`
  - 166-player Opportunity Board completed in about 11 seconds in Streamlit
  - 100-trial Gibbs nomination plus two sensitivity scenarios completed in
    about 11 seconds with no Streamlit exceptions
  - Gibbs sensitivity increased Buy Edge to about `+29.3` after Jeanty was
    removed and `+35.9` after Hampton was removed in the current clean state

## 2026-07-10 - Target Board Expected Roster Gain

- Request:
  - remove the noisy fast Opportunity Board and give Target Board selection
    frequency a direct roster-EV interpretation
- Key decisions:
  - for each selected non-fixed player, choose the best feasible replacement
    using the same draft-time managed-value context and all active constraints
  - score the selected and replacement rosters on a separate weekly holdout
    draw to avoid outcome hindsight
  - define Expected Roster Gain as Fit Rate multiplied by mean Gain When
    Selected, and sort the Target Board by this combined metric
  - retain Current Nomination as the exact price-specific Buy/Pass evaluator
- Files changed:
  - `Fantasy_Football_App/app/ffapp.py`
  - `Fantasy_Football_App/app/zSim_Helper.py`
  - `docs/data_contracts/auction_salary_tables.md`
  - durable agent notes
- Verification:
  - 100-trial direct managed simulation completed in about 5.4 seconds
  - contribution identity matched to floating-point precision
  - 100-trial Streamlit AppTest rendered the new Target Board with no exceptions

## 2026-07-10 - Auction EV Alignment And Price Curves

- Request:
  - add more Current Nomination chart points, investigate parallel execution,
    and reduce Target players ranking highly before negative nomination results
- Key decisions:
  - retain sparse binary search for initial price discovery, then evaluate every
    dollar around the crossing plus a bridge to the entered decision price
  - fit non-increasing Buy Edge and Win Rate curves; use fitted Buy Edge for
    Roster Max Bid because raw paired estimates can have positive noise islands
  - build each Target roster from a five-context bootstrap average rather than
    one realized weekly scenario or one full-bank deterministic mean
  - replace one-player Target counterfactuals with candidate-rebased forced-Buy
    and Pass ILP roster rebuilds scored on independent holdout weekly outcomes
  - keep Target Expected Roster Gain as sampled-build optionality and Current
    Nomination as the forced decision at one actual price
  - defer parallel execution to process-level workers; GLPK calls are too small
    for per-solve threading and shared global simulation randomness must first
    be isolated for deterministic workers
  - decouple nomination objective-bank size from salary-trial count, using 250
    unique managed contexts for roster construction and five weekly scoring
    contexts per salary trial
- Verification:
  - Barkley test produced a 19-point decision-relevant curve instead of the
    original sparse seven-point curve
  - monotone fitted Buy Edge rejected isolated positive high-price noise and
    moved the supported crossing to the local sustained boundary
  - 100-trial Target Board with full Buy/Pass rebuilds completed in about 10.3
    seconds in the active clean beta context
  - the prior Barkley `$76` result moved from about `-20` to approximately
    neutral (`+1.4`, interval spanning zero) after objective/scoring stabilization

## 2026-07-10 - Target Board Fit Rate Regression

- Request:
  - investigate Target Board Fit Rates above 80% after EV alignment changes
- Finding:
  - selection counting was correct, but averaging all 25 managed contexts into
    one objective made the Target roster nearly deterministic across trials
- Fix:
  - bootstrap five context columns with replacement per trial
  - use the same sampled context indices for the open ILP and candidate-rebased
    Buy/Pass contribution branches
- Verification:
  - 100 trials: maximum Fit Rate `45.0%`, no player above `80%`
  - 500 trials: maximum Fit Rate `53.8%`, no player above `60%`; RJ Harvey fell
    from the reported `84.6%` to `53.8%`
  - Expected Roster Gain identity remained exact to floating-point precision

## 2026-07-11 - Target Managed Context Refresh

- Request:
  - reduce repeated reuse of 25 managed weekly contexts across each 100 Target
    trials by using 50 contexts for 50 trials
- Key decisions:
  - separate managed-context refresh from the 100-trial projection/salary market
    refresh
  - generate a fresh 50-context bank every 50 trials while continuing to
    bootstrap five contexts with replacement for each ILP objective
  - lazily calculate candidate-rebased Buy values only for context columns that
    a selected player actually uses
- Verification:
  - 100 trials completed in about 13 seconds with two independent context banks
  - 500 trials completed in about 64 seconds with ten context banks; maximum Fit
    Rate was `42.8%`, with no player above `60%`
  - Expected Roster Gain identity remained exact to floating-point precision

## 2026-07-11 - Nomination Scoring Efficiency

- Request:
  - profile Current Nomination and Target Board for obvious runtime gains
- Findings:
  - Current Nomination spent about 54% of profiled runtime in managed lineup
    scoring and scored roughly 3,500 Buy/Pass/open ILP rosters whose scores were
    discarded before the separate five-context evaluation
  - Target runtime is dominated by required candidate-rebased marginal values
    and Buy/Pass ILPs; 100 trials performed about 4,045 marginal-value calls and
    2,700 ILP solves
- Changes:
  - allow managed scenario solves to return selected rosters without exact
    scoring when the caller will score them separately
  - avoid constructing weekly lineup-detail frames for Pass roster evaluations
  - add a batched managed-lineup scorer that evaluates every weekly contest for
    all five shared contexts in one NumPy path while retaining waiver,
    inactivity, position, FLEX, and tracked-start behavior
  - use the batched scorer for Current Nomination Buy and Pass roster evaluation
- Verification:
  - primary 100-trial Barkley nomination fell from about 27.9 to 24.1 seconds
    with identical fixed-seed edge, Fit Rate, Max Bid, starts, and curve points
  - end-to-end Streamlit nomination including alternative sensitivity completed
    in about 29.7 seconds with no exceptions
  - synthetic scalar-versus-batch checks had zero score difference and identical
    tracked starts; the isolated five-context scoring kernel was about 15.6x
    faster
  - after vectorization, the primary 100-trial evaluation completed in about
    13.9 seconds and two 50-trial sensitivity evaluations took about 1.5 seconds
    each; the full Streamlit interaction completed in about 17.2 seconds with no
    exceptions

## 2026-07-11 - Parallel Target Simulation

- Request:
  - parallelize Target Board simulation across as many as 16 workers so 800 or
    1,000 trials are practical
- Changes:
  - split Target execution into independently seeded 50-trial process blocks,
    preserving the managed-context refresh unit while giving every block a fresh
    projection and salary draw bank
  - initialize a private SQLite connection, simulation object, adjusted player
    data, and weekly-template cache in each spawned worker
  - return raw selection counts, selected salaries, player contributions, and
    managed season samples from workers; calculate rates, means, percentiles,
    quantiles, and Expected Roster Gain only after merging in the parent process
  - add a 1-16 Parallel Workers control; the existing simulation control already
    supports up to 1,000 trials
- Verification:
  - same-seed 100-trial serial-block and two-worker results matched exactly for
    every player and managed summary statistic; Expected Roster Gain identity
    error was zero
  - benchmark runs completed 500 trials on 10 workers in about 13.5 seconds, 800
    trials on 16 workers in about 22.3 seconds, and 1,000 trials on 16 workers in
    about 24.4 seconds, with every requested trial successful
  - Streamlit AppTest completed a two-worker Target run with 100 summary trials,
    30 result rows, and no exceptions

## 2026-07-11 - Target Contribution Holdout Averaging

- Request:
  - reduce run-to-run player contribution variance by scoring each selected
    player's fixed Buy and Pass rosters over five holdout weekly seasons
- Changes:
  - sample one legacy-compatible holdout plus four independently seeded holdouts
    for each successful Target iteration
  - batch the four extra weekly-template draws with one pass over the player pool
    and score all five contexts through the vectorized managed-lineup scorer
  - average paired Buy-minus-Pass season points across the five shared holdouts;
    do not rerun either candidate ILP for additional holdouts
  - isolate extra holdout random draws from the construction stream and continue
    using only the first holdout for managed lineup summary outcomes
- Verification:
  - the first holdout and post-holdout construction RNG matched the one-context
    path exactly, and all five sampled weekly contexts were distinct
  - same-seed one- versus five-holdout checks had identical roster selections,
    selected salaries, and managed-summary samples while contribution estimates
    changed as intended
  - 500 trials on 10 workers completed in about 17.8 seconds; 800 trials on 16
    workers completed in about 19.5 seconds with all requested trials successful
  - Streamlit AppTest completed a 100-trial, two-worker Target run with 30 rows
    and no exceptions

## 2026-07-11 - GLPK Runtime Profile

- Request:
  - decompose GLPK and surrounding ILP runtime in Current Nomination and Target,
    then identify shared optimization opportunities
- Study:
  - `research/studies/2026-07-11_glpk_runtime_profile/`
- Findings:
  - a 100-trial nomination evaluated 17 unique prices and made 3,500 GLPK calls;
    GLPK consumed 9.66 profiled seconds, pandas salary-row construction 3.99,
    five-context scoring 2.26, and salary normalization 1.59
  - 10,506 CVXOPT matrix conversions consumed only 0.23 seconds, so broad matrix
    object reuse is low priority
  - an exact NumPy salary row was about 288x faster than the pandas implementation;
    copying each 166x1,003 prediction frame and Pass subset cost about 2.09 ms
  - repeated scenario/roster scores were potentially reusable for 65.6% of Pass
    evaluations and 23.0% of Buy evaluations across prices
  - sparse matrices and redundant-row removal gave small isolated GLPK gains but
    no end-to-end benefit; global best-projection backtracking made a 50-trial
    nomination 27% slower despite helping one captured Pass problem
  - a 0.5% MIP gap changed rosters/objectives, and HiGHS was slower and unstable
    operationally in this environment; retain exact GLPK
  - Target spent more time in managed marginal-value calculations than GLPK, so
    solver tuning is not its primary next optimization
- Operational note:
  - two abandoned Target process pools retained several GB and each had one
    full-core worker; they were terminated without touching Streamlit or notebook
    processes, and process cancellation/cleanup remains a separate audit item
## 2026-07-11 - Current Nomination Runtime Optimization

- Request:
  - implement the exact speedups identified by the detailed GLPK profile
- Changes:
  - added reusable NumPy salary-market normalization and salary-row helpers
  - removed repeated full projection-frame copies from Current Nomination price
    scenarios
  - cached exact managed-roster scores by scenario and roster across prices
  - skipped the paired Pass solve whenever the Buy branch was infeasible
- Verification:
  - 50-trial benchmark dropped from 7.406 seconds to 4.326 seconds (41.6%)
  - every value on the full 18-price curve matched the saved baseline exactly
  - call audit avoided 48 Pass solves and reduced exact scoring calls by 47.5%
  - 100-trial nomination completed in 7.402 seconds with the same previously
    profiled Buy Edge, max bid, fit rate, and win rate
  - managed Target, fixed-player/next-year nomination, syntax, Streamlit AppTest,
    and live server checks passed

## 2026-07-11 - Target Runtime Profile

- Request:
  - perform the same detailed runtime profiling for the Target Board process
- Findings:
  - one uninstrumented 50-trial block completed in 5.771 seconds
  - candidate Buy/Pass contribution evaluation consumed 82.4% of block time
  - candidate marginal values consumed 1.866 seconds, contribution GLPK 1.501
    seconds, and 650 full prediction-frame copies consumed 0.695 seconds
  - candidate/context caching avoided 1,208 marginal calculations and should be
    retained
  - 51.2% of holdout roster score calls were reusable within a trial; Buy
    rosters were 92.3% reusable
  - candidate copies moved about 876 MB per block
  - worker startup/shutdown increased from 2.94 seconds at eight workers to 9.41
    seconds at 16 workers
  - ten workers was the best general cap across the current 500-1,000 trial UI
    range; fixed-worker outputs matched exactly
- Recommended order:
  - cap workers at 10
  - port the NumPy candidate salary workspace and reuse paired salary rows
  - cache exact per-trial roster scores
  - vectorize `managed_base_lineup_state` with strict equivalence checks
- Artifacts:
  - `research/studies/2026-07-11_target_runtime_profile/`

## 2026-07-11 - Target Runtime Optimization

- Request:
  - implement all four Target profile recommendations using eight workers by
    default
- Changes:
  - reduced the Target worker default and cap from 16 to eight
  - replaced per-candidate full DataFrame copies and pandas salary normalization
    with lean NumPy salary arrays
  - reused paired Buy/Pass salary rows when historical fixed-player ordering
    guarantees equality
  - cached exact holdout roster scores within each trial
  - vectorized managed base-lineup threshold selection across weeks
- Verification:
  - the seeded 30-row Target board matched the saved pre-change output exactly
  - 500 captured lineup-state fixtures and 500 randomized fixed-player salary
    cases matched exactly
  - repeated 50-trial runtime averaged 3.783 seconds versus a 5.560-second
    baseline, a 32.0% reduction
  - eight-worker runtime was 8.811 seconds for 500 trials, 12.043 for 800, and
    14.445 for 1,000
  - fixed-roster/next-year smoke, syntax, Streamlit AppTest, worker-control, and
    live-server checks passed

## 2026-07-11 - Batched Managed Marginal Values

- Request:
  - batch candidate marginal-value calculations and replace the weekly P90
    percentile when the numerical difference is negligible
- Changes:
  - added a shared multi-base managed marginal-value evaluator
  - grouped Target candidate cache misses by managed context so all missing
    candidate-rebased objective rows are calculated together
  - calculated Current Nomination Pass and Buy objective rows in one batch
  - replaced full weekly percentile sorting with partial selection while
    matching NumPy's dtype and linear interpolation behavior exactly
  - retained the existing per-base threshold selector after a fully tensorized
    prototype changed equal-decision displacement due to unstable sort ordering
- Verification:
  - 500 randomized reference comparisons matched the prior scalar marginal
    implementation exactly, including tied decisions and 1-20 week inputs
  - the durable verification script passed 500 lineup fixtures, 200 batched
    marginal cases, 500 salary cases, and the seeded 30-row Target board
  - five repeated 50-trial runs averaged 3.299 seconds versus 3.783 seconds,
    a further 12.8% reduction
  - contribution evaluation fell from 2.840 to 2.343 seconds; eight-worker
    500-trial runtime fell from 8.811 to 7.726 seconds
  - Current Nomination completed a seeded 50-trial, 18-price smoke run in 4.00
    seconds
  - syntax, Streamlit AppTest, live HTTP 200, and diff checks passed

## 2026-07-12 - Nomination And Target Board Cleanup

- Request:
  - implement the seven identified Current Nomination and Target Board
    correctness/presentation cleanups
- Changes:
  - keyed Target results to draft contents, simulation controls, lineup rules,
    waiver baselines, budget/slot state, and runtime settings; stale results are
    hidden behind an explicit refresh notice
  - changed nomination display to separate Decision Price, adjusted Market
    salary, Roster Max Bid, and Bid Cushion; Fit Rate is explicitly at the
    evaluated price
  - used `UpdateSal` for nomination defaults, nomination market context,
    alternative sensitivity sale prices, and Target Market salary while
    retaining the original prediction as Model salary
  - preserved Target ordering on unrounded Expected Roster Gain, removed fixed
    players before the 30-row limit, and added a stable visible rank
  - added contribution evaluation count and conditional P10 to the simulation
    result contract and Target table
  - added currency formatting and prioritized adjusted Market salary ahead of
    Model salary in the visible table
- Verification:
  - durable Target verification passed 500 lineup fixtures, 200 batch marginal
    cases, 500 salary cases, and the seeded 30-row board
  - pure table tests confirmed filter-before-limit, raw-value ordering,
    UpdateSal mapping, sequential rank, state-key changes, and contribution
    counts
  - a five-fixed-player 50-trial run returned 30 non-fixed candidates with valid
    P10/sample-count fields
  - a real nomination test separated model $115, market $122, decision $119,
    and max-bid cushion correctly
  - Streamlit stale-state and full 100-trial AppTests passed; live browser review
    confirmed the final table and nomination layouts without overlap
  - syntax, app diff check, and live HTTP 200 passed

## 2026-07-12 - Candidate Review Responsiveness

- Request:
  - defer alternative sensitivity and reduce exact nomination chart-price solves
- Changes:
  - split primary nomination analysis from the two optional 50-trial alternative
    scenarios and added an explicit `Run Alternative Sensitivity` action
  - reduced the exact local fair-bid neighborhood and bridge-price grid
  - retained exact GLPK evaluations for the decision and roster max bid, while
    interpolating only monotone chart display points with a five-dollar maximum
    visual gap
- Verification:
  - helper test confirmed one primary evaluator call and two calls only after
    requesting sensitivity
  - deterministic 100-trial evaluator benchmark completed in 7.76 seconds with
    14 exact price points, down from the profiled 10.91 seconds and 19 points
  - full Streamlit AppTest completed the primary review in 7.85 seconds with 11
    exact price points, then built two sensitivity rows in a separate 4.86-second
    action; no app exceptions occurred
  - syntax, diff check, and live HTTP 200 passed

## 2026-07-13 - Auction EV Methodology Review And Nomination Corrections

- Request:
  - review Fantasy_Football_App EV methodology for a real-time managed auction
    tool, then correct the nomination position/full-roster and Max Bid issues
  - retain intentional alternate-keeper comparisons and the small fallback
    value used when manually copied ESPN auction values omit deep players
- Changes:
  - based nomination position maxima on the roster before the nominee is added,
    so only an already-entered violation is grandfathered
  - removed the fully fixed roster shortcut and validate cap, position, roster,
    and top-N constraints through GLPK for every branch
  - solve Buy and Pass independently in every scenario, report their completion
    counts, and exclude partial paired samples from EV and recommendation
  - replaced raw-edge binary search with 13 global exact anchors, non-increasing
    isotonic fitting, and adaptive adjacent-dollar refinement of both the EV
    crossing and the full/partial feasibility boundary
  - kept the entered decision-price solve outside the deterministic Max Bid fit
    unless adaptive search independently selects the same price, so Max Bid is
    invariant to the UI price used for the current recommendation
  - require complete Open-roster coverage before displaying Fit Rate
  - made the displayed recommendation respect the fitted Roster Max Bid and
    stopped treating interpolated chart prices as feasible evidence
  - surface incomplete trial coverage as `INFEASIBLE` with Buy/Pass counts in
    the Streamlit result
- Verification:
  - full-roster test rejected a second forced QB with `0/3` Buy and `3/3` Pass
    completions
  - a pre-existing two-QB roster remained valid for an RB nomination, while a
    third QB nomination was rejected
  - an injected Open-roster failure left paired EV valid but correctly withheld
    Fit Rate at `2/3` completion
  - isotonic noise tests produced the intended crossings for both a recoverable
    positive island and a rejected high-price island
  - 20-trial runs at decision prices `$25` and `$76` had identical exact search
    rows and the same `$73` Max Bid; the `$76` diagnostic remained outside the
    search fit
  - seeded 100-trial Saquon Barkley evaluation completed in 8.87 seconds across
    23 exact prices, returned a `$80` Max Bid with the next exact price negative,
    completed Fit Rate on `100/100` Open solves, and excluded `$284` (`97/100`)
    and `$286` (`1/100`) partial-pair prices
  - `python -m py_compile app/zSim_Helper.py app/ffapp.py`, app diff checks, and
    Streamlit `AppTest` passed
- Follow-up:
  - alternate keeper-table scenarios remain intentional, but removing a player
    only from `League_Keepers` does not create a full non-keeper salary-model
    counterfactual while that player's `Salaries_Pred` row remains deterministic
    at the keeper price

## 2026-07-13 - Target Roster Interaction Refinement

- Request:
  - correct the Target Board's fixed-base additive roster construction bias
    without making the real-time auction workflow materially slower
- Changes:
  - retained the fast outer ILP, then applied one best feasible one-player swap
    before contribution holdouts, summaries, and selection tallying
  - averaged the full cached construction bank into one expected 16-week
    profile for the swap search; a rejected prototype that optimized only the
    five bootstrapped ILP seasons improved them in-sample but reduced paired
    holdout EV
  - batched every removable-player base roster, used exact marginal identities
    to shortlist swaps, exact-scored complete finalists, and bounded rounded/tied
    decision handling to 16 finalists
  - preserved fixed players and manual salaries, salary cap, roster size,
    position minimums/maximums, and the top-N constraint
  - skipped the unused weekly P90 calculation when bench-upside weight is zero
    and bumped the Target cache version
- Verification:
  - synthetic redundant-position roster swapped `ExtraRB -> StrongWR` and
    improved exact managed points from 192 to 228 while preserving every
    constraint
  - 30 deliberately rounded/tied-decision cases matched exhaustive feasible
    one-swap search; 500 lineup fixtures, 200 batched marginal cases, and 500
    salary workspace cases retained their prior outputs
  - a seeded 50-trial serial block improved 48 rosters by 30.6 points on the
    expected construction profile and increased paired holdout season EV by
    15.7 points, with runtime increasing from about 3.2 to 3.5 seconds
  - a seeded 500-trial, eight-worker production run increased paired holdout
    season EV from 1655.67 to 1667.40 while runtime increased from about 8.5 to
    9.0 seconds
  - syntax checks and a 100-trial, two-worker Streamlit `AppTest` completed
    without exceptions
- Study:
  - `research/studies/2026-07-13_target_roster_refinement/`

## 2026-07-13 - Managed Played-Week Availability

- Request:
  - remove managed-season hindsight caused by treating a realized zero or
    negative fantasy score as proof that the player was unavailable
- Changes:
  - persisted `played_week_1` through `played_week_16` from source player-week
    evidence, separately from score multipliers; QB evidence is captured before
    the existing greater-than-15-play performance-profile filter
  - added `played_games` alongside unchanged `active_games`; mask sums validate
    against `played_games`, active evidence must be a subset of played evidence,
    and only QB templates may have a positive difference
  - added `managed_week_1` through `managed_week_16` so short-QB scores remain
    available to the auction app without changing the workload-qualified
    `week_*` profiles consumed by Snake
  - rebuilt both retained `beta` and `dk` weekly-template slices and synchronized
    the generated tables to the auction app while preserving its intentional
    keeper and deterministic salary overrides
  - changed the durable export path to preserve those auction-owned scenario
    tables, skip partial multi-league played/managed schema migrations, and
    continue copying the full source database to Snake
  - carried the played mask through sampled contexts, learned decision scores,
    scalar and multi-context lineups, marginal values, Target refinement and
    holdouts, contribution scoring, summaries, and Current Nomination
  - kept a per-cell legacy score-threshold fallback for older or unreconstructed
    database slices and rejected partial played-mask schemas
  - excluded missed weeks from subsequent learned PPG while retaining played
    zero and negative performances
  - bumped Target and Nomination cache versions
- Verification:
  - 5,298 templates in each of `beta` and `dk` have complete 0/1 masks whose
    row sums equal `played_games`; all mask/count, active-subset, and non-QB
    equality audits pass
  - the `beta` source contains 2,586 exact-zero scores, 653 negative scores, and
    317 short-QB participation weeks excluded from the best-ball profile; all
    are treated as played, and 295 non-zero short-QB outcomes totaling 713.98
    points are retained in the managed profile
  - focused builder, scalar, multi-context, learning, marginal, loader, and
    template-pairing fixtures passed
  - existing checks passed for 500 lineups, 200 batched marginal cases, and 500
    salary workspaces
  - a paired 100-trial Target diagnostic removed 22.39 season points of legacy
    score-threshold EV while retaining Chase Brown as the top player; a
    warmed four-pair alternating runtime check had 0.32% median overhead, and a
    two-worker 100-trial corrected run completed in 5.92 seconds
  - a 20-trial Saquon Barkley nomination completed all Buy and Pass branches;
    a 100-trial/two-worker Streamlit AppTest completed with no exceptions
- Study:
  - `research/studies/2026-07-13_managed_played_mask/`
- Remaining limitation:
  - the mask proves a qualifying play-by-play row, not comprehensive snap or
    pre-kickoff status, so zero-opportunity appearances remain incomplete
    outside available snap-count coverage
  - historical weekly scoring is still aggregated by cleaned player name,
    position, season, and week; review confirmed 19 overlapping TE weeks from
    two different Zach Millers, so stable player identity/team resolution is
    the next template-integrity fix

## 2026-07-13 - Managed Roster Settings Dropdowns

- Request:
  - cap completed auction rosters at two TEs by default and make roster and
    waiver assumptions easier to edit in the live app
- Changes:
  - changed the shared managed-position default from three TEs to two
  - converted QB/RB/WR/TE/FLEX starter counts, bench slots, and QB/RB/WR/TE
    waiver-replacement PPG inputs from number fields to dropdowns
  - exposed QB/RB/WR/TE finished-roster maxima as dropdowns; each maximum is
    bounded below by its direct starter requirement
  - preserved the existing rule that a live over-cap roster is grandfathered
    while preventing another acquisition at that position
  - added pre-solve validation for total roster capacity, FLEX-eligible
    capacity, and starter configurations that exceed the scorer's synthetic
    waiver-player supply
  - versioned converted widget keys to avoid carrying incompatible live
    number-input state into the new dropdown widgets
- Verification:
  - syntax checks passed for `ffapp.py` and `zSim_Helper.py`
  - Streamlit AppTest rendered the new controls with zero exceptions and default
    `TE Max = 2`; dropdown mutations persisted across reruns
  - invalid aggregate maxima and unsupported starter combinations displayed
    clear errors and disabled simulation before any ILP call
  - focused GLPK fixtures selected at most two TEs and made a forced third-TE
    Buy branch infeasible while the two-TE Pass branch remained feasible
  - a 100-trial/two-worker Target AppTest completed with zero exceptions, two
    dataframes, and eleven metrics under the new default

## 2026-07-13 - Collapsible Managed Sidebar Controls

- Request:
  - reduce sidebar clutter without changing the managed-auction inputs
- Changes:
  - grouped league selection into a collapsed `League Settings` expander
  - grouped all starter and bench controls into a collapsed `Starter & Bench
    Settings` expander
  - grouped all per-position roster maxima into a separate collapsed `Roster
    Position Maximums` expander
  - grouped next-year weighting and runtime controls into `Simulation & Value
    Settings`
  - grouped the Top-N constraint controls and player preview into `Top N Salary
    Constraint`
  - grouped waiver PPG and bench-upside assumptions into `Managed Season
    Scoring`
  - replaced the nested Top-N help expander with a caption for compatibility
    with the app environment's Streamlit version
- Verification:
  - syntax and whitespace checks passed
  - Streamlit AppTest rendered all six sibling expanders collapsed by default
    with no app exception and retained the default `TE Max = 2`

## 2026-07-13 - Managed Auction Rolling-Origin Replay

- Request:
  - run a historical replay of the managed-season auction simulation choices and
    identify the next major methodological opportunities
- Study:
  - added `research/studies/2026-07-13_managed_auction_rolling_replay/`
  - used frozen preseason inputs for 2022-2025, raw FastR target-season weeks,
    pre-origin-only weekly donors, independent construction/evaluation context
    banks, and 250 paired trials across a 16-cell factorial
  - compared one versus five salary draws, Top-N off/on, projected versus
    prior-empirical waiver baselines, and bench-upside weight 0 versus 0.25
  - scored recorded played-zero/negative weeks normally and removed historical
    keepers with deterministic recorded spend from the empty-roster replay
- Verification:
  - completed all 16,000 cells in 717 seconds; all solves were optimal, every
    roster had 13 unique players, forecast spend stayed within $298, position and
    Top-N rules held, and every construction donor preceded the origin
  - independently recomputed all 4,000 exact-profile and 32,000 factorial pairs,
    output aggregations, source hashes, joins, and roster counters
  - first/second trial halves agreed on the salary, waiver, and aggregate Top-N
    directions; bench-weight direction reversed
- Findings:
  - the exact current profile fit historical final prices in 195/1,000 trials;
    mean forecast spend was $292.85 versus $322.89 realized, exposing selected-
    roster optimizer's curse despite nearly unbiased overall salary means
  - one draw improved CRPS/WIS and raised 80% coverage from 31.1% to 65.1%, but
    reduced roster feasibility from 19.5% to 5.8%; averaging five draws shrank
    forecast SD to about 45% without improving salary MAE
  - prior-empirical waivers lost 22.5 points on average and lost in every season;
    Top-N off lost 4.9 overall and 27.7 in the 2025 check; bench zero versus 0.25
    was unstable
- Decisions:
  - retain five draws, projected waivers, Top-N on, and bench weight 0.25 as
    short-term production defaults; do not interpret the five-draw distribution
    as calibrated uncertainty or switch directly to one draw
  - prioritize a walk-forward replay of the current salary-residual method using
    a $250-$290 cap-haircut grid, coherent normalized market scenarios, and
    roster-level chance/CVaR or feasible recourse rules
  - after salary risk, validate candidate-level Target Gain/Fit, add live
    nomination-state logging, correct the refinement OR-across-context played
    mask, and replace hindsight-filtered frictionless waiver eligibility
- Limits:
  - final prices are exogenous, no auction order/state log exists, jointly
    feasible pairs are sparse, $1 actual-price fallbacks make feasibility
    optimistic, and four seasons remain only four independent outcomes

## 2026-07-14 - Nominal Salary Buffer Replay

- Request:
  - test whether a second roster constraint on summed predicted salaries prevents
    the optimizer from assembling too many cheap salary draws, including both one
    draw and the current average of five draws
- Study:
  - added `research/studies/2026-07-14_nominal_salary_buffer_replay/`
  - crossed one/five salary draws with no nominal constraint or buffers of `$0`,
    `$5`, `$10`, `$15`, and `$25` above the sampled-price `$298` cap
  - normalized one point-predicted salary market per origin to keeper-adjusted
    remaining league money and slots; retained Top-N, projected waivers, and bench
    weight `0.25`
  - added optional sampled and nominal salary rows to the shared optimizer and
    enforced both through the bounded refinement pass; fixed acquired-player
    salary overrides to map by player rather than input row order
- Verification:
  - completed all 12,000 cells in 559.7 seconds; all solves were optimal and all
    sampled/nominal caps, unique-roster, position, Top-N, donor-timing, actual-
    overage, and normalized-market invariants passed
  - exactly reproduced all 2,000 unconstrained parent controls with zero roster,
    score, salary, feasibility, or waiver-start mismatches
  - independently recomputed all 10,000 buffer pairs, 6,000 draw pairs, and 5,000
    draw-by-buffer interactions; maximum numeric discrepancy was `2.27e-13`
  - five-draw `+$10` overage reductions were stable in both trial halves; an
    independent result review reached the same policy recommendation
- Findings:
  - five-draw `+$10` lost 6.1 points over 2022-2024 while improving realized-
    price feasibility by 2.4 percentage points and reducing mean overage by
    `$3.52`; in 2025 it gained 1.2 points, held feasibility flat, and reduced
    overage by `$1.06`
  - five-draw `+$5` bought another 2.1 development feasibility points versus
    `+$10` but cost another 7.2 season points; a zero buffer was too aggressive
  - at `+$10`, one draw was point-neutral versus five across all seasons but was
    5.8 feasibility points worse and `$4.05` further over cap
  - the `+$10` point cost was concentrated in the unusually under-scaled 2022
    frozen salary surface; 2023-2025 averaged +1.0 point, +1.1 feasibility points,
    and `$0.80` less overage
- Decision:
  - retain five draws; use `cap + $5` as the provisional affordability guardrail
    and expose `cap + $10` as the looser, point-preserving option
  - count acquired players at deterministic paid salaries in both price rows
  - keep the choice provisional until the current residual-quantile salary method
    is rebuilt walk-forward and compared with coherent scenarios/chance risk
- Limits:
  - even five-draw `$0` reached only 27.6% development and 19.2% 2025 historical-
    price feasibility, so the point row does not solve optimizer's curse
  - historical final prices are exogenous, `$1` missing-price fallbacks are
    optimistic, feasible-only effects select on future prices, and the frozen
    2023-2025 salary laws mainly predate the current residual-quantile method

## 2026-07-14 - Sequential Price-Recourse Buffer Replay

- Request:
  - replay `+$5` versus `+$10` as a live, non-anticipating auction policy before
    deciding which nominal predicted-salary buffer to use
- Study:
  - added `research/studies/2026-07-14_sequential_price_recourse_replay/`
  - parsed the raw 12-roster auction exports, removed K/DST and league keepers,
    retained unmatched skill sales as passive tape events, and used deterministic
    recorded paid prices for acquisitions
  - evaluated four synthetic order stresses, recorded clearing price `p` and
    outbid stress `p + 1`, strict and operational policy modes, both buffers,
    eight common trials, and four frozen origins
  - hid future tape prices/order and target outcomes from each receding-horizon
    Buy/Pass decision; scored outcomes only after a legal 13-player roster
- Verification:
  - completed the exact 1,024-path grid with 821 legal rosters; all ledgers,
    `$298` paid-price caps, branch salary rows, position limits, raw roster counts,
    keeper counts/spend, source hashes, donor timing, and prefix invariance passed
  - incomplete paths retained null outcome fields, strict paths used no fallback,
    runtime generic repair stayed disabled, and no synthetic contract player was
    acquired
  - independently audited the 2022-2024 checkpoints and recomputed the primary
    decision metrics from raw paths
  - long 2025 CVXOPT/GLPK processes exited intermittently without Python errors;
    reran the same full plans as 32 isolated order/trial shards, validated all 256
    cells and repeated audit hashes, then committed a hash-checked checkpoint
  - widened only the hypothetical branch-row numerical audit tolerance to
    `$0.001` for a maximum GLPK excess of `$0.0004223`; completed paid-price
    rosters remain capped at `$298` within `1e-8`
- Findings:
  - primary strict `p + 1` development completion was 44/72 for `$5` and 42/72
    for `$10`; only 38/72 pairs both completed, 10/72 were discordant, and 15/72
    were paired-clean
  - no primary order family had clean observations in all three origins, so the
    equal-origin point effect and randomization SE are undefined; available signs
    conflict by year/order and reverse under zero-valued failure sensitivity
  - recorded-price `p` improved completion to 70/72 versus 69/72 and 67/72 paired,
    but still left five discordant and only 33 clean pairs
  - operational mode exactly matched strict throughout development; 2025 reversed
    the aggregate completion direction and also had mixed point signs
- Decision:
  - select neither buffer from this replay; keep the earlier static `+$5` result
    provisional only and do not treat `+$10`'s conditional point lean as causal
  - next rebuild the current residual-quantile salary method walk-forward and
    compare fixed keeper states with coherent salary-market or direct chance-risk
    recourse before wiring a universal live guardrail
- Limits:
  - nomination order and losing bids are unavailable, opponent response is fixed,
    order families have no identified probabilities, every personal roster starts
    empty after league keepers, and 2025 is a temporal sensitivity rather than a
    fresh holdout

## 2026-07-14 - Salary Validation and Backtest Datasets

- Request:
  - persist historical salary predictions and residuals so the current salary
    method and predicted-salary buffer can be replayed without reconstructing
    notebook-only validation frames
- Changes:
  - added `Salary_Backtest_Predictions` and `Salary_Validations_Resid` builders
    and slice-safe transactional writers to `s4_Salaries_Injuries.py`
  - retained raw and keeper-budget-normalized predictions, residual quantiles,
    target cutoffs, candidate coverage, normalization audit fields, and explicit
    current-method-versus-fresh-holdout provenance
  - removed target-derived `total_spent` and `fraction_spent` from the model
    matrix and calculated rank/gap features among auctionable non-keepers so
    deterministic keepers cannot shift them
  - changed historical pool ownership from the truncated manual ESPN copy to the
    preseason `ProjOnly` universe; uncopied base values use zero and retain a
    `base_salary_observed` flag that is excluded from the model
  - retained keeper contracts missing origin-year projection features as
    deterministic output rows with cross-season position inference
  - added `SALARY_VALIDATION_DATASETS_ONLY=1` to build validation data without
    rewriting live Simulation slices or copying the app database
- Verification:
  - synthetic keeper-separation, rolling-origin, normalization, duplicate-key,
    and idempotent-writer fixtures passed; `py_compile` and focused whitespace
    checks passed
  - exact full-model build completed and stored 1,219 backtest rows for 2022-2025
    and 644 observed non-keeper validation rows for 2021-2025
  - independent SQL checks passed strict model/residual cutoffs, unique keys,
    monotone residual quantiles, candidate-slot coverage, false target-actual
    normalization flags, and exact keeper-adjusted market totals
  - live 2026 beta counts remained 199 `Salaries`, 170 `Salaries_Pred`, and 14
    `League_Keepers`; validation-only mode did not propagate a database copy
- Findings:
  - full replay pools contain 299, 311, 304, and 305 rows for 2022-2025; only
    137, 171, 182, and 170 have manually copied base values
  - overall observed normalized residual MAE/RMSE are 4.307/6.276 versus
    4.187/6.180 raw, so known-budget normalization provides aggregate feasibility
    but should not be described as improving player-level price accuracy
- Next:
  - use these tables for the locked `$5`/`$10` current-method replay and coherent
    salary-market comparisons; consider model-artifact caching separately because
    the exact full build took about 44 minutes

## 2026-07-14 - Current-Method `$5` versus `$10` Replay

- Request:
  - rerun the two provisional predicted-salary guardrails using the newly built
    rolling current-method salary tables
- Study:
  - added `research/studies/2026-07-14_current_salary_buffer_replay/`
  - held five salary draws, sampled `$298` cap, Top-N, projected waivers, bench
    weight `0.25`, and current roster maxima including `TE <= 2` fixed
  - paired no-buffer, `+$5`, and `+$10` rosters on identical salary markets and
    managed-value contexts for 250 trials in each 2022-2025 origin
  - used rolling salary centers/residual quantiles; model-missing frozen players
    used historical ESPN prices when present and prior-only position/price
    residual interpolation, with every fallback recorded
- Verification:
  - completed all 3,000 cells in 83.6 seconds; all solves were optimal and all
    sampled/nominal caps, 13-player uniqueness, position, Top-N, residual-cutoff,
    and template-timing checks passed
  - independently reconstructed direct effects and feasibility flags from raw
    roster rows; first and second trial halves retained the same point,
    feasibility, and overage directions
  - direct minimum-fill and salary-model-fallback selection differences between
    `$5` and `$10` were negligible
- Findings:
  - development `+$5 minus +$10` was `-5.57` season points (`-0.35` team PPG),
    `+6.53` feasibility points, and `-$3.55` mean overage; 2025 was `-3.10`,
    `+6.40`, and `-$3.32`
  - all four seasons agreed: `$5` lowered points, improved feasibility, and
    reduced overage; it changed 83.7% of development and 87.2% of 2025 rosters
  - from no buffer to `+$10`, development lost only 2.95 points while gaining
    6.13 feasibility points and cutting overage by $7.43; the extra tightening to
    `$5` had a worse marginal point/feasibility tradeoff
  - absolute feasibility remained only 23.2% for `$5` and 16.7% for `$10` in
    development, so neither resolves optimizer's curse
- Recommendation:
  - superseded by the subsequent feasibility-first replay: do not select `$5`
    or `$10` from unconditional points on historically unaffordable rosters
  - continue toward coherent-market scenarios or direct chance/recourse risk
    rather than expecting a nominal row alone to make rosters reliably purchasable

## 2026-07-14 - Salary Chance-Constraint Frontier

- Request:
  - replace the misleading unconditional point ranking of historically
    unaffordable rosters with an explicit feasibility-first salary-risk replay
- Study:
  - added `research/studies/2026-07-14_salary_chance_frontier/`
  - required rosters to fit the `$298` cap in 60%, 70%, 80%, or 90% of 20
    normalized five-draw construction markets
  - evaluated managed forecast EV on independent weekly contexts and modeled
    affordability on 200 salary markets not used by the optimizer
  - retained raw historical points only for audit and excluded unaffordable
    rosters from policy point summaries
  - disabled the one-swap refiner because its current scalar salary checks cannot
    preserve a multi-scenario constraint; all thresholds used the same unrefined
    optimizer
- Verification:
  - completed 4,000 optimal cells in 357.8 seconds; all exact chance thresholds,
    13-player uniqueness, position maxima including `TE <= 2`, Top-N, historical
    cap flags, and donor-timing checks passed
  - used a one-cent conservative internal solver cap after reproducing a maximum
    `$0.00232172` GLPK row excess; validation remained against the true `$298` cap
  - independently recomputed every reported aggregate from raw rows; held-out
    probability increased, forecast EV decreased, and historical overage
    decreased in every year and both trial halves
- Findings:
  - 60% to 90% cost 4.88 development forecast season points while adding 24.60
    modeled feasibility points, 6.00 historical feasibility points, and reducing
    historical overage by `$5.78`; 2025 changes were -8.94, +25.94, +6.40, and
    `-$5.62`
  - absolute historical feasibility at 90% was only 18.1% in development and
    14.8% in 2025
  - actual selected-roster spend exceeded independent scenario mean spend by
    about `$29` at every threshold; subsequent residual diagnostics split this
    into point-salary error and a separate selected-roster scenario discount
  - exact chance solves were about 3.2x slower per cell than the simple-buffer
    replay; useful offline but material for live Target/Nomination loops
- Decision:
  - supersede the provisional `+$10` point/affordability recommendation; do not
    choose `$5`/`$10` from unconditional points on unaffordable rosters
  - do not deploy a chance threshold until selected-roster spend and cross-player
    dependence are calibrated with prior-only data; compare a robust salary row,
    dependent scenarios, CVaR, or feasible recourse next

## 2026-07-14 - Selected-Roster Salary Residual Diagnostic

- Request:
  - compare salary residuals for all auctionable players, optimizer-selected
    players, highly projected unselected players, and selected players by
    position, predicted-price tier, and value-over-price rank
- Study:
  - added `research/studies/2026-07-14_selected_roster_salary_residuals/`
  - reconciled the chance study's 930 candidate player-origins and all 52,000
    selected roster slots to the exact `Actual_Salaries` source used by the replay
  - used 518 recorded player-origin prices covering 96.0% of selected slots;
    excluded missing prices from player-residual summaries while retaining the
    replay's `$1` fallback for exact roster-gap reconstruction
  - reconstructed all 1,000 normalized five-draw salary markets per origin to
    measure candidate-level scenario means relative to point-predicted salaries
- Verification:
  - exact roster actual spend, point-salary spend, and missing-price counts
    reproduced all 4,000 chance-frontier cells
  - independent audit recomputed unique-player, selection-weighted, scenario-shift,
    value-rank, and roster-gap summaries; selected residuals were positive and
    selected scenario shifts negative in every year/chance cell
- Findings:
  - all observed auctionable player-origins averaged `-$0.39` residual and unique
    ever-selected players `-$0.40`, but selection-weighted slots averaged `+$1.43`
  - rare 0%-5% selections averaged `-$1.58`, frequent 25%-50% selections `+$2.38`,
    and three core above-50% selections `+$12.32`; selection frequency, not the
    ever-selected flag, exposes the optimizer's curse
  - top-quartile projected players selected at most 5% averaged `-$4.64`, while
    those selected at least 25% averaged `+$2.49`; only two top-quartile players
    were literally never selected
  - selected RB/WR slots averaged `+$2.71`/`+$1.29`; the strongest within-position
    value-over-price quintile averaged `+$4.82` with 72.3% positive residuals
  - the roughly `$29` development actual-minus-scenario gap decomposed into about
    `$16.3-$16.4` actual-minus-point residual plus `$12.8-$12.9` scenario discount;
    2025 components were `$21.5-$23.0` and `$9.4-$9.6`
  - all candidates' scenario means averaged `+$0.23` versus point salary, but
    selection-weighted roster slots averaged `-$0.92`, showing a second selection
    effect inside the scenario layer
- Decision:
  - do not hardcode a `$29` haircut; anchor the next risk rule to the point salary
    row and separately calibrate the scenario-center and actual-price components
  - fit a strictly prior-origin, shrinkage surcharge from preseason selection
    propensity, value rank, position, and price tier; test current versus recentered
    scenarios and a cheap robust row before returning to exact chance binaries

## 2026-07-15 - Optimizer-Aligned Salary Projection Features

- Request:
  - add the final projection ensemble and optimizer-aligned projection-versus-price
    features to the auction salary model
- Changes:
  - reconstructed one historical point ensemble from `Model_Validations_Resid`
    using the same all-current, component, and next-year blend families used by
    `s3_Model_Ensemble.py`
  - joined current `Final_Predictions_Resid` point predictions without changing
    the projection-defined salary candidate universe
  - added ensemble positional strength, ensemble-versus-consensus rank gap,
    ensemble-versus-ESPN-price rank gap, winsorized ensemble PPG per source dollar,
    and position interactions for ensemble PPG, price gap, and per-dollar value
  - calculated rank/value features separately for keeper and auctionable groups;
    unmatched current deep players use `avg_proj_points / 16.5` and the audit-only
    fallback flag is excluded from the model matrix
  - versioned future validation output as
    `current_locked_spec_v2_ensemble_features`, preserving the v1 baseline
- Verification:
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`
  - direct read-only function exercise against the live databases found 2,696 of
    2,696 exact historical validation-era joins, 180 exact 2026 final-ensemble
    joins, and 146 intentional current consensus fallbacks
  - derived feature ranges were valid and the 52-column focused model matrix was
    fully numeric and finite with all twelve requested position interactions
- Follow-up:
  - run a preserved-v1 versus v2 point-salary and optimizer-selected-roster
    ablation before promoting the new feature spec

## 2026-07-15 - Historical Keeper Identity Join

- Request:
  - resolve salary-pipeline position inference failure for historical keeper
    `Mohamed Sanu Sr`
- Changes:
  - cleaned player identities on both historical actual- and source-salary tables
    before their outer merge
  - added duplicate cleaned player-year checks and a one-to-one merge contract
- Verification:
  - all 139 historical keeper rows matched projection position history with zero
    missing positions; `Mohamed Sanu Sr` normalized to `Mohamed Sanu` and WR
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`

## 2026-07-16 - Salary Ensemble-Feature Walk-Forward Ablation

- Request:
  - rerun the prior salary walk-forward analysis after rebuilding the full salary
    pipeline with `current_locked_spec_v2_ensemble_features`
- Study:
  - added `research/studies/2026-07-16_salary_ensemble_feature_ablation/`
  - paired v1 and v2 on 644 common observed player-years, while separately
    recording one new v2-only 2025 minimum-price row
  - projected both methods onto the exact 930-player frozen forecast universe
    from the prior chance replay and exactly reconstructed the v1 salary surface
  - repriced all 4,000 old v1-selected rosters to isolate salary-surface changes
    before optimizer reselection
  - reran the full 4,000-cell 60%-90% chance frontier under v2 with identical
    forecasts, contexts, settings, and random seeds
- Verification:
  - all v1/v2 player rows, 52,000 fixed roster slots, prior point/actual roster
    spend, and 4,000 paired frontier cells independently reconciled
  - all v2 frontier cells were optimal and passed chance, roster, position,
    Top-N, timing, and historical affordability checks
- Findings:
  - v2 improved common-row normalized mean residual from `-$0.71` to `-$0.48`
    and RMSE slightly from `$6.28` to `$6.26`, but worsened MAE from `$4.31` to
    `$4.49`; 2025 MAE worsened from `$3.73` to `$4.20`
  - on the frozen candidate pool, the strongest within-position value quintile's
    old-v1-selection-weighted residual improved from `+$4.82` to `+$3.91`, but
    the all-candidate selection-weighted residual worsened from `+$1.43` to
    `+$1.58`
  - v2 changed 79.1% of development and 80.1% of 2025 optimizer rosters
  - averaged across chance thresholds, development gained 2.08 managed forecast
    points but lost 1.02 modeled affordability points, left historical
    feasibility unchanged, and worsened overage by `$1.09`
  - 2025 lost 1.79 managed forecast points, lost 0.43 modeled affordability
    points, improved historical feasibility by 1.20 points, and reduced overage
    by `$0.46`; directions varied materially by season
- Decision:
  - do not promote v2 or delete its feature set; keep v1 as the current
    comparison/default surface
  - next test a rolling-origin v1/v2 shrinkage blend or restricted correction
    aimed at the optimizer-relevant high-value/high-price tail, requiring both
    player-error and selected-roster affordability gates before another full
    chance replay

## 2026-07-16 - Projection Validation Residual Backfill

- Request:
  - add a reusable s1 residual-calibration helper for future s2 runs and use the
    existing validation rows to build the missing residual schema and final
    historical ensemble without retraining point models
- Changes:
  - added horizon-aware donor filtering and strict-prior-origin cross-fitting in
    `s1_Stacking_Model.py`; s2 now uses it for validation rows and excludes
    not-yet-realized targets from current prediction residual donors
  - added `zProjection_Validation.py` and wired s3 to persist
    `Final_Validations_Resid`
  - switched the salary ensemble feature loader from an ad hoc base-row
    reconstruction to the persisted final validation table
  - backfilled residual quantiles and provenance onto all 42,351 existing
    `Model_Validations_Resid` rows and created 6,006 final player-origin rows
- Validation:
  - created a timestamped pre-migration SQLite backup under the study's ignored
    `artifacts/local/` directory
  - independently reconstructed the prior point ensemble; maximum final mean
    difference was `3.55e-15`
  - found zero donor-cutoff violations, zero residual-order violations, and no
    duplicate final identities
  - 5,237 final rows (87.2%) have usable residual vectors; missing/partial rows
    are concentrated in the earliest origins with insufficient prior donors
  - terminal next rows retain calibrated forecasts but have
    `resid_target_available = 0`, preventing their placeholder outcomes from
    entering evaluation or later donor pools

## 2026-07-16 - Salary Residual and Role-Share Features

- Request:
  - add projection residual features and team/position share features together
    so the salary ensemble can select the useful signals with a smaller search
- Changes:
  - versioned the experiment as
    `current_locked_spec_v3_resid_share_features`
  - joined historical causal residuals from `Final_Validations_Resid` and
    current residuals from `Final_Predictions_Resid`
  - added P75/P90/P95 ceilings, P10 downside, 50%/80%/90% interval widths,
    ceiling positional ranks, price gaps, per-dollar values, and position
    interactions
  - collapsed source-specific projected-point, rush-attempt, reception, and
    receiving-yard team/position shares to row medians plus source disagreement
  - interpolated residuals for deep current players from same-position,
    similar-PPG final-ensemble rows; early seasons without causal intervals use
    neutral residuals, and all fallback flags remain audit-only
  - reduced salary tuning to 15 Optuna iterations and a 45-second timeout per
    model family
- Verification:
  - direct live-table exercise produced 5,624 candidate rows and a 142-column,
    fully numeric and finite model matrix
  - all residual vectors were finite and monotone
  - 2020-2025 historical ensemble rows had complete causal residual coverage
  - 146 deep 2026 candidates used the intended residual tier fallback
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`
- Follow-up:
  - run the v3 validation datasets and compare v1/v2/v3 player error,
    optimizer-selected residuals, and historical roster affordability before
    promotion

## 2026-07-16 - Additive Salary Normalization and Keeper-Market Inputs

- Request:
  - stop broad market normalization from materially diluting high-end raw salary
    predictions and give the salary model clearer keeper-adjusted source prices
- Changes:
  - versioned the next experiment as
    `current_locked_spec_v4_additive_keeper_market_features`
  - replaced proportional-above-`$1` point calibration with one common additive
    shift, floor constrained at `$1`, that exactly reconciles the top open-slot
    non-keeper market to the deterministic remaining budget
  - retained `pred_salary_raw`, set the legacy scale audit to one, and added
    `normalization_method`, `normalization_floor`, and `pred_salary_shift`
  - added keeper market value, contract discount, keeper remaining-pool
    inflation, copied source-market totals, log source salary, keeper-adjusted
    source salary, and fully budget-adjusted source salary features
  - kept missing historical keeper source values neutral by falling back to the
    deterministic contract; projection-only seasons before salary coverage use
    neutral audit-only keeper-market context
  - added schema migration for the rolling salary validation tables and
    documented the v4 fields and formula
- Verification:
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`
  - direct current-market reconstruction changed the old raw top salaries by one
    `-$1.502` shift and exactly matched the `$3,071` top-142 budget
  - the full feature build produced 1,213 training rows and a 167-column numeric,
    finite matrix; all 2021-2025 replay-pool rows were finite
  - current features reconcile to 14 keepers, `$505` spend, `$752` keeper market
    value, `1.0875` keeper-only inflation, and `1.1573` full source-market scale
  - a temporary database test verified additive metadata and keeper-market
    validation columns migrate and append without changing the live database
  - the fixed-v3-raw study under
    `research/studies/2026-07-16_additive_salary_normalization/` compared 527
    observed player-years; additive normalization improved MAE from `$4.363` to
    `$4.293` and RMSE from `$6.221` to `$6.128`
- Next:
  - rerun the full salary pipeline to create v4 OOF/current predictions, then
    repeat player-error, optimizer-selected residual, and managed-roster
    affordability comparisons before promoting v4

## 2026-07-16 - Compact Salary Feature Surface

- Request:
  - identify a smaller salary feature subset that remains associated with
    actual prices and price residuals without retaining the highly redundant
    v3/v4 feature blocks
- Analysis:
  - reconstructed the current causal keeper-adjusted training frame with 887
    labeled historical non-keeper rows and 155 nonconstant legacy features
  - found near-duplicate blocks for source prices, projection
    mean/floor/ceilings, interval widths, value ratios, position interactions,
    and keeper-market context
  - tested compact feature sets across strict 2022-2025 rolling origins with a
    fixed six-model ensemble
  - replacing log ADP with an adjusted-price rank gap reduced its correlation
    with adjusted salary from `0.931` to `0.048`, but worsened rolling MAE by
    about `$0.12`; centering the RB rush-share interaction also slightly
    worsened accuracy
- Changes:
  - versioned the reduced surface as
    `current_locked_spec_v5_compact_salary_features`
  - retained 12 substantive features: budget-adjusted source salary, log ADP,
    ensemble PPG, ensemble-versus-price rank gap, P90 projection residual,
    position points share, RB rush share, experience, rookie status, and QB/RB/TE
    indicators with WR as reference
  - removed adjacent-price gaps, per-dollar ratios, duplicate
    mean/floor/ceiling features, broad interval families, and approximately 60
    position interactions from the live model matrix
  - retained `year` and `game_date` only for rolling splits and explicitly drops
    both before model fitting
  - narrowed random feature fractions to 60%-100% and K-best choices to
    6/8/10/all for the compact matrix
  - added the reproducible study under
    `research/studies/2026-07-16_salary_feature_reduction/`
- Verification:
  - compact matrix has 12 substantive columns plus two split columns and is
    numeric/finite across all 1,213 reconstructed rows
  - the actual pipeline's feature-drop step passed exactly the 12 substantive
    columns to a fitted Ridge smoke test; 2022 predictions were finite
  - across 526 strict rolling player-years, compact versus legacy fixed-ensemble
    MAE improved from `$4.561` to `$4.312`, RMSE from `$6.740` to `$6.461`, and
    R-squared from `0.932` to `0.937`
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py
    research/studies/2026-07-16_salary_feature_reduction/run_feature_audit.py`
- Next:
  - rerun the full v5 salary notebook to persist rolling validation and current
    salary outputs, then compare optimizer-selected residuals and managed-roster
    historical affordability before promotion

## 2026-07-16 - Compact Salary Optuna Feature-Drop Fix

- Issue:
  - `skmodel.default_params()` automatically added
    `feature_drop__drop_cols = ['avg_pick', None]` to the Optuna search space,
    overriding the fixed `['year', 'game_date']` pipeline setting and causing
    `FeatureDrop.transform()` to iterate over `None`
  - the subsequent missing `SciKitModel.y` exception was only the library's
    secondary error path after the original transformer failure
- Fix:
  - removed `feature_drop__drop_cols` from the generated tuning dictionary so
    the structural split-column exclusion cannot be tuned away
- Verification:
  - exact LGBM/Optuna smoke test completed two tuning folds with zero failed
    trials and 526 holdout predictions
  - both returned pipelines retained `['year', 'game_date']` as their fixed
    drop columns
  - `python -m py_compile Scripts/Modeling/s4_Salaries_Injuries.py`

## 2026-07-16 - Salary v5 Full Replay

- Request:
  - repeat the prior salary player-error, selected-residual, and optimizer
    affordability tests after saving the compact v5 predictions
- Study:
  - added `research/studies/2026-07-16_salary_v5_replay/`
  - compared v5 with preserved v1 and feature-rich v3 on rolling player prices
  - reran the identical-seed 4,000-cell 60%-90% chance frontier under v5
  - rebuilt selected-roster residual, value-tail, price-tier, and roster-gap
    diagnostics from the actual v5-selected rosters
- Player-price results:
  - normalized all-year v5 MAE/RMSE were `$4.271`/`$6.197`, versus
    `$4.307`/`$6.276` for v1 and `$4.455`/`$6.252` for v3
  - raw v5 MAE/RMSE were `$4.169`/`$6.176`; the 2025 raw MAE was `$3.549`
  - raw ESPN-source MAE was `$6.442` and mechanically budget-adjusted source
    MAE was `$7.277`, confirming that v5 learns material corrections rather
    than simply reproducing its strongest feature
  - v5 residual intervals achieved 79.5% central-80 coverage and 88.2%
    central-90 coverage across usable rolling rows
- Optimizer results:
  - all 4,000 v5 chance cells were optimal; development rosters changed 89.1%
    and 2025 rosters changed 93.4% versus v1
  - development historical feasibility improved from 15.5% to 19.0%, overage
    fell `$2.47`, and actual spend fell `$2.90`
  - 2025 feasibility improved from 12.0% to 18.5%, overage fell `$4.76`, and
    actual spend fell `$5.60`
  - managed forecast season points fell 3.12 in development and 7.80 in 2025;
    historically conditioned point directions were mixed and cannot identify
    the better scoring policy from four seasons
- Remaining bias:
  - selection-weighted residual improved only from `+$1.43` to `+$1.35`
  - strongest value-quintile residual improved from `+$4.82` to `+$4.18`, and
    core-selection residual from `+$12.32` to `+$8.76`
  - the actual-minus-scenario roster gap fell from roughly `$29-$32` to
    `$25-$27`; scenario means still sit about `$10.4` below selected point
    rosters in development and `$7.0` below them in 2025
- Verification:
  - independent audit reproduced all 4,000 paired frontier cells, all 52,000
    selected slots, period effects, and the roster-gap decomposition
- Decision:
  - keep v5 as the leading/current salary surface and preserve additive budget
    reconciliation
  - next test a shrunk residual model relative to budget-adjusted source salary
    or a strictly prior-origin selection/value surcharge; do not treat the
    remaining affordability gap as solved

## 2026-07-16 - Optimizer Selection Surcharge Replay

- Request:
  - test a simple second-stage correction relating actual-minus-v5 salary
    residuals to position, predicted salary, and optimizer selection frequency
- Study:
  - added
    `research/studies/2026-07-16_optimizer_selection_surcharge/`
  - fit a heavily regularized ridge model strictly on completed prior origins;
    2022 is a zero-surcharge seed, 2023 trains on 2022, 2024 on 2022-2023, and
    2025 on 2022-2024
  - kept v5 point and sampled salaries unchanged as market estimates and applied
    positive predicted residuals only as a separate decision-price reserve
  - replayed 250 paired five-draw trials per origin for baseline `$298`, blanket
    `$285`, half surcharge, and full surcharge policies
- Results:
  - half surcharge reduced selection-weighted residual bias from `+$1.61` to
    `+$1.04` in 2023-2024 and from `+$1.44` to `+$0.67` in 2025
  - on fixed baseline chance-frontier rosters, half surcharge reduced the mean
    actual-minus-price gap from `$19.69` to `$12.68` in development and from
    `$18.63` to `$8.65` in 2025; full reduced it to `$5.66` and `-$1.33`
  - half surcharge improved historical feasibility by 6.8 points in both
    development and 2025, reduced mean overage by `$7.02`/`$8.06`, and changed
    raw season points by `-1.31`/`-0.05`
  - full surcharge improved feasibility by 15.6/21.2 points and overage by
    `$11.69`/`$14.05`, versus 13.6/14.4 points and `$12.41`/`$9.62` for the flat
    `$285` policy; its season-point effect was `-3.50`/`-5.26`
  - absolute feasibility remained low: 12.8%/14.8% for half and 21.6%/29.2%
    for full in development/2025
- Verification:
  - project-environment syntax and all-origin smoke tests passed
  - the full replay completed all 4,000 cells, reconciled all 930 selection-rate
    rows without fallback, enforced all salary/position/Top-N constraints, and
    preserved identical 2022 baseline/targeted rosters
- Decision:
  - preserve v5 as the coherent market price
  - retain half shrinkage as the low-cost reserve candidate and full shrinkage
    as the aggressive affordability candidate, but do not make either the
    default until a current 2026 seed pass and remaining-risk policy are tested

## 2026-07-16 - Annual Optimizer-Selection Premium Workflow

- Request:
  - turn the validated ridge-style selection correction into a simple static
    table that can be refreshed at the beginning of each season or after a
    material simulation update
- Implementation:
  - added `Scripts/Modeling/s5_Auction_Selection_Premium.py`
  - bootstrapped durable 2022-2025 premium-free Target-style selection seeds,
    carries later seed rows forward, and attaches actual non-keeper auction
    prices only after their season is complete
  - fits the alpha-100 ridge strictly on prior origins and publishes positive
    residual estimates with a `$10` full cap and default 50% shrinkage
  - added `Salary_Selection_Seeds` and `Salary_Selection_Calibrator` to
    `Validations.sqlite3` and `Salary_Selection_Premium` to source and app
    `Simulation.sqlite3`
  - integrated the reserve into Target, Target Buy/Pass contribution, and
    Current Nomination affordability after live salary normalization; fixed
    roster salaries, keepers, and explicit nominee prices remain deterministic
  - exposed Market `$`, Reserve `$`, and Decision `$` separately on the Target
    Board and documented the annual refresh command/data contract
- 2026 beta refresh:
  - completed 1,000/1,000 premium-free Target builds across 166 non-keepers
  - all 2022-2026 seed-rate sums reconcile to the 13-player roster size
  - trained through 2025 on 518 observed player-origins
  - selection-weighted applied premium was `$0.34` per roster slot and the
    expected roster reserve was `$4.37`; RJ Harvey received the maximum `$2.54`
    at a 57.6% seed selection rate
  - increased the default from 250 to 1,000 after an independent-seed check:
    aggregate reserve was already stable, but individual premiums were noisy;
    at 1,000, seed-to-seed rate/premium correlations improved to 0.951/0.919
    with only `$0.046` all-player premium MAE
- Verification:
  - compiled the workflow plus both app modules in Python 3.12
  - source/app premium schemas and 166-row active slices match
  - normal Target and explicit-price Nomination smoke solves completed

## 2026-07-16 - Selection Reserve App Toggle

- Request:
  - expose a simple app setting that turns the static optimizer-selection
    reserve off and treats every premium as `$0`
- Implementation:
  - added a default-on `Use Selection Reserve` checkbox under Simulation &
    Value Settings
  - disabled mode zeros the displayed reserve and bypasses it in Target,
    Target candidate Buy/Pass, Current Nomination, and alternative sensitivity
    without rewriting `Salary_Selection_Premium`
  - included the setting in Target and Nomination cache keys so stale results
    cannot cross the on/off boundary

## 2026-07-16 - Target Forced-Candidate Shortlist

- Request:
  - surface players such as Jonathan Taylor whose exact Current Nomination Buy
    result is positive even though ordinary Target roster construction rarely
    selects them
- Implementation:
  - retained the normal parallel Target build as the organic stage
  - selected a 24-player shortlist by round-robin coverage of organic Target
    rank, current/next weighted projection, sufficiently sampled conditional
    contribution, and market salary; players blocked by fixed-position maxima
    are excluded
  - evaluated the shortlist over 25 shared paired Buy/Pass trials at sampled,
    normalized Target decision prices, reusing batched candidate marginal values
  - ranked sufficiently covered shortlist players on forced-screen gain while
    retaining Organic Gain, Fit Rate, conditional contribution, forced P10,
    forced win rate, and both sample sizes in the UI
  - labeled rows `Organic`, `Organic + screen`, or `Forced screen`; bumped the
    Target cache version so old boards cannot cross the method boundary
- Verification:
  - Python 3.12 syntax checks and `git diff --check` passed
  - a 100-trial/two-worker later-draft smoke test completed in 8.5-8.6 seconds
    and rendered the combined Target columns
  - in a fixed-seed 50-trial reproduction, Jonathan Taylor moved from organic
    rank 167 with `-1.61` Organic Gain to rank 10 with `+4.66` Forced Gain over
    25/25 paired trials and a 64% forced win rate
  - the fixed 24-player/25-trial stage added about 6.5 seconds to that serial
    later-draft diagnostic; its absolute cost does not grow with the main 500 or
    1,000 Target trial count
- Limitation:
  - the 25-trial screen is a discovery/ranking layer rather than the final bid
    surface; use the 100-trial Current Nomination evaluation to confirm a player
    and exact price before bidding

## 2026-07-16 - Adaptive Target Confirmation

- Request:
  - spend less compute on already-stable organic roster counts and more on the
    consequential forced candidates whose 25-trial ranks changed materially
    between runs
- Implementation:
  - changed the app default from 500 to 300 `Organic Simulations`
  - reduced the preliminary forced shortlist from 24 to 20 players while
    retaining organic, projection, conditional-contribution, and market-price
    coverage
  - selected eight confirmation candidates by round-robin coverage of
    standardized preliminary evidence, projection strength, market price,
    organic rank, organic/forced conflict, and raw forced upside
  - added 75 independent paired trials for those eight, producing approximately
    100 combined forced evaluations per confirmed player
  - preliminary 25-trial results remain visible but cannot change the main rank;
    only sufficiently complete confirmation-stage results use Forced Gain for
    ranking
  - replaced discovery labels with explicit preliminary/confirmed provenance,
    added an approximate mean-edge CI90, clarified Organic Trials, and bumped
    the Target cache version
- Verification:
  - Python syntax checks passed in both the app and modeling environments
  - two 300-organic/eight-worker adaptive smoke runs completed in 19.7-20.1
    seconds; each produced 20 preliminary candidates, eight confirmations, and
    100/100 paired results for displayed confirmed rows
  - the refined confirmation selector concentrated its latest eight slots on
    Drake London, Jahmyr Gibbs, Ashton Jeanty, Tet McMillan, Brian Thomas,
    Bijan Robinson, Kenneth Walker, and Derrick Henry rather than low-end
    one-off conditional contributors
- Interpretation:
  - confirmed Target Gain is still evaluated at sampled normalized decision
    prices; Current Nomination remains the exact current-price bid and Max Bid
    surface

## 2026-07-16 - Parallel Adaptive Target Stages

- Request:
  - apply the Target worker setting to the preliminary and confirmation solves,
    which were still running serially after the organic worker pool closed
- Implementation:
  - split each later-stage scenario set into balanced independently seeded
    blocks and evaluate every candidate within each block, preserving paired
    Buy/Pass comparisons and shared scenario context
  - reuse one eight-process pool across the 25-trial preliminary and 75-trial
    confirmation stages; merge raw contribution/salary samples before summary
  - bumped the Target cache version because the stochastic block mapping changed
- Verification:
  - app-environment syntax and whitespace checks passed
  - a 100-organic end-to-end smoke test completed in 8.7 seconds
  - the comparable 300-organic test completed in 11.5 seconds versus 19.7-20.1
    seconds for the prior serial-later-stage implementation
  - every confirmed player retained exactly 100 attempts/evaluations and every
    preliminary-only evaluated player retained exactly 25

## 2026-07-16 - 400-Trial Organic Default

- Request:
  - promote 400 organic Target trials after parallelizing the later forced stages
- Decision:
  - changed the app default from 300 to 400 organic trials
  - 400 maps exactly to eight 50-trial organic blocks, using all eight workers
    in one wave while reducing organic Monte Carlo standard error by about 13%
    versus 300 trials
  - retained the 20-by-25 preliminary and eight-by-75 confirmation settings

## 2026-07-16 - Bayesian Preliminary Target Ranking

- Request:
  - let preliminary forced trials improve the main Target order without treating
    a noisy forced mean as fully equivalent to confirmed evidence
- Implementation:
  - rebalanced the forced allocation to 20 players by 40 preliminary trials and
    eight players by 60 additional trials, retaining 100 total confirmations
  - for every sufficiently paired screened player, rank on Forced Gain shrunk
    toward organic conditional Gain if Drafted rather than Organic Gain
  - use organic contribution N as prior strength, floored at 5 and capped at 25;
    players without organic contribution evidence receive a neutral zero prior
    with strength 10
  - forced weight is `ScreenN / (ScreenN + PriorN)`, so a well-sampled
    preliminary player typically receives 61.5% forced weight and a confirmed
    player 80%; sparse organic priors allow more forced weight
  - added visible Ranking Gain and Forced Weight columns and bumped the Target
    cache version
- Verification:
  - deterministic posterior checks covered preliminary, confirmed, unscreened,
    and insufficiently paired cases
  - a complete 400-organic/40-preliminary/60-confirmation run used eight workers
    in all stages and completed in 12.8 seconds
  - all preliminary candidates recorded 40 attempts, all confirmed candidates
    recorded 100, and final rows were correctly ordered by posterior Ranking Gain

## 2026-07-17 - Target Strategic-Pivot Evidence

- Request:
  - smooth the edge case where expensive low-organic-fit RBs dominated the
    Target Board after an elite QB and three RBs were already fixed, without
    imposing a position rule that would suppress a genuinely optimal
    stars-and-scrubs roster
- Implementation:
  - kept preliminary 40-trial discovery and fresh 60-trial confirmation samples
    separate rather than merging them into one selected 100-trial estimate
  - replaced the organic pseudo-count blend with a position-neutral
    leave-one-player-out random-effects prior learned from the other 20-player
    preliminary screens; Organic Gain remains separate because it is a
    selection-weighted policy contribution rather than an always-Buy effect
  - retained the eight independent scenario blocks and now uses block-aware
    standard errors plus small-block interval adjustment
  - requires at least 90% paired coverage; low-fit pivots (`Fit < 5%` or organic
    contribution `N < 10`) need 97.5% posterior support, 90% fresh confirmation
    support, and stable preliminary/confirmation replication
  - confirmation compute now prioritizes the largest posterior effect plus
    uncertainty rather than projection/market round-robin coverage
  - added separate preliminary/confirmation/posterior UI evidence, evidence
    status, replication Z, and the forced-Buy roster position mix, WR/TE spend
    change versus Pass, and common WR/TE completion players
  - bumped the Target cache version to 11
- Edge-case verification:
  - the deterministic 400/40/60 replay used eight workers in every stage and
    completed in roughly 12-17 seconds across repeated runs
  - Derrick Henry replicated at `+14.9` preliminary, `+21.3` confirmation, and
    `+10.6` posterior; Saquon Barkley replicated at `+15.3`, `+14.0`, and
    `+10.2`
  - their Buy rosters averaged 4.9-5.0 RBs and shifted `$34.4-$38.6` away from
    WR/TE versus paired Pass rosters, confirming that the aggressive RB build is
    a real modeled strategy with an explicit stars-and-scrubs cost
  - weaker preliminary-only pivots such as Kyren Williams fell back to Organic
    Gain, and Jonathan Taylor did not survive into the final top 30
  - Python 3.12 compilation, staged synthetic checks, dataframe formatting, and
    focused app whitespace checks passed

## 2026-07-17 - Compact Target Board Output

- Request:
  - reduce the wide Target Board to the fields most useful during a live auction
    without discarding the staged-evidence diagnostics
- Implementation:
  - made the primary board an eight-column decision view: rank, player,
    position, evidence plus posterior positive probability, estimated gain,
    roster fit, combined decision price, and roster impact
  - combined market salary and the static selection reserve into one readable
    price field
  - combined forced-Buy RB count and WR/TE decision-dollar change into one
    roster-impact field
  - moved preliminary, confirmation, posterior, replication, composition, and
    component price fields into a collapsed Evidence Details table
  - bumped the Target cache version to 12 so prior wide cached results cannot
    reach the new formatter
- Verification:
  - synthetic formatter assertions covered confirmed-pivot, reserve-price,
    roster-impact, and organic-fallback rows
  - Python 3.12 compilation and focused whitespace checks passed

## 2026-07-17 - Target Reserve Accounting And Logical Blocks

- Request:
  - correct Target candidate reserve accounting and make forced-stage evidence
    invariant to the selected worker-process count before revisiting ranking and
    analysis-cohort design
- Implementation:
  - candidate Buy/Pass rebasing now subtracts sampled Market `$`, not Market `$`
    plus the personal selection reserve, from remaining league money
  - retained the full Market `$` plus Reserve `$` Decision price in the personal
    salary-cap row
  - separated eight fixed logical scenario blocks from the one-to-eight process
    setting for both 40-trial preliminary and 60-trial confirmation stages
  - bumped the Target Board cache version to 13 and clarified the app docs
- Verification:
  - Python 3.12 compilation and app whitespace checks passed
  - same-seed 100-organic runs with one and eight processes matched exactly for
    all 30 result rows, managed summary values, preliminary shortlist, and
    confirmation shortlist; both retained eight logical blocks
  - the one-process run took 24.4 seconds and the eight-process run 9.1 seconds,
    confirming the setting now changes runtime only
  - the corrected deterministic 400/40/60 edge replay completed in 14.4 seconds
    with eight workers and passed all staged-evidence smoke assertions

## 2026-07-17 - Target Tiered Ranking And Broad Discovery

- Request:
  - reconcile forced, organic, fit, and uncertainty metrics into an actionable
    final order and broaden candidate discovery without multi-minute live runs
- Implementation:
  - replaced the global mixed-estimand rank with tier-local `A`, `B`, and `C`
    ranks for confirmed targets, provisional forced targets, and the organic
    watchlist
  - ranked A/B within tier on forced posterior LCB80 (`mean - 0.842 * SE`) and C
    on Organic Gain; P+, Fit, and conditional contribution remain evidence or
    diagnostic fields rather than extra score multipliers
  - require at least 90% fresh confirmation support and stable replication for
    Tier A; attempted but unsupported confirmation returns to C instead of
    retaining a forced global rank
  - added Organic Pair Coverage and sort under-covered watchlist rows after
    supported rows
  - added two exact shared pilot scenarios over the remaining eligible pool,
    protected all 20 existing heuristic candidates, and appended four fully
    paired discoveries to the
    fresh 24-player/40-trial preliminary cohort
  - discard pilot values before the empirical prior, posterior, evidence status,
    and action score; preliminary and confirmation retain independent seeds
  - when Pass excludes an original Top-N candidate, promote salary rank N+1 in
    the constraint so Buy and Pass retain equal policy breadth
  - bumped the Target Board cache version to 14 and changed compact ranks to
    `A1`, `B1`, and `C1` style labels with explicitly named scores
- Verification:
  - synthetic contracts passed tier ordering, negative-fresh-stage demotion,
    exact LCB80 calculation, pilot isolation, deterministic 20+4 allocation,
    incomplete-pilot exclusion, and dynamic Top-N row replacement
  - deterministic production-sized edge smokes completed in 12.1-12.5 seconds;
    its pilot found four new candidates while retaining all 20 heuristic anchors
  - same-seed 100-organic runs with one and eight processes matched exactly for
    all results, summaries, pilot/preliminary/confirmation cohorts, tiers, and
    tier-local ranks; runtimes were about 12.4-12.6 and 8.3 seconds
  - app-environment formatter checks rendered tier-first `A1/B1/C1` ordering and
    labeled Forced LCB80 versus Organic Gain scores

## 2026-07-17 - Target Market-Anchor Confirmation

- Request:
  - keep likely near-term auction nominations, especially the most expensive
    remaining players, from moving in and out of fresh confirmation because of
    a noisy eight-player preliminary cutoff
- Implementation:
  - preserved all eight existing evidence-priority confirmation candidates and
    appended any missing members of the four highest-market-salary candidates
    with usable 40-pair preliminary evidence
  - deduplicated the union to 8-12 players, used adjusted Market `$` rather than
    the personal selection reserve, and added player-name tie-breaking
  - retained the same 60 fresh pairs and evidence gates for every route; market
    price cannot promote a player to A/B
  - exposed Evidence priority, Market anchor, or both in Evidence Details and
    retained confirmed market anchors in the final 30 rows without changing
    their tier order
  - bumped the Target Board cache version to 15
- Verification:
  - synthetic contracts passed union, overlap, duplicate, unusable-coverage,
    shuffled-row/tie, no-price-promotion, and display-retention cases
  - the production fixed-seed smoke retained all eight evidence candidates,
    added Bijan Robinson, Jonathan Taylor, and Devon Achane around an overlapping
    Ja'Marr Chase anchor, and gave all 11 unique candidates 60 confirmation pairs
  - the expanded run took 13.5 seconds versus 13.6 seconds with market anchors
    disabled in the same code and seed
  - one-worker and eight-worker runs matched exactly, including evidence and
    market confirmation cohorts; runtimes were 12.8 and 8.3 seconds

## 2026-07-17 - Target Continuous LCB And Reallocated Compute

- Request:
  - reduce same-state Target Board churn caused by hard evidence gates, keep
    Organic Gain in discovery rather than the forced score, and shift compute
    from organic fit toward deeper preliminary and confirmation evidence
- Implementation:
  - changed the live allocation to 320 organic trials in eight balanced
    40-trial logical blocks, four pilot scenarios, 24 players by 64 preliminary
    pairs, and 96 confirmation pairs for ten evidence leaders plus up to four
    market anchors
  - retained a fresh root seed for every deliberate live run, recorded that
    seed and the organic logical blocks in the managed summary, and made organic
    block definitions independent of the selected worker count
  - ranked every usable forced screen on posterior LCB80 whether confirmation is
    strong, mixed, negative, conflicting, incomplete, or absent; only
    organic-only rows use the following Organic Gain watchlist
  - replaced A/B/C tier-local rank labels with one numeric board order and
    separate recommendation, evidence-stage, evidence-quality, and low-fit-pivot
    annotations
  - estimated excess preliminary/confirmation variation with the two-study
    method-of-moments variance and added it to both stage variances before the
    posterior, so disagreement raises SE continuously instead of causing a 90%
    gate demotion
  - bumped the Target Board cache version to 16 and updated the compact board,
    Evidence Details, and staged-evidence documentation
- Verification:
  - Python 3.12 compilation, whitespace checks, and synthetic contracts passed
    continuous ranking, exact LCB80, disagreement inflation, pilot isolation,
    top-ten-plus-market confirmation union, balanced blocks, and dynamic Top-N
  - the fixed 20260717 full 320/4/64/96 edge replay used eight organic and later
    workers, confirmed the maximum 14 players, and passed in 19.1 seconds
  - a same-seed 100-organic replay matched exactly across one and eight workers
    for results, summaries, cohorts, labels, disagreement, and rank; serial and
    parallel runtimes were 33.0 and 14.2 seconds

## 2026-07-19 - Organic Target Roster Convergence

- Request:
  - remove the remaining additive managed-value interaction bias by repeating
    the existing exact-scored organic roster swap until local convergence
- Implementation:
  - added `refine_managed_roster_to_convergence` with a positive `max_swaps`
    contract, a default cap of 12, and accepted-swap accounting
  - changed only the organic `run_sim` construction path to call convergence;
    forced pilot/preliminary/confirmation and Current Nomination stay unrefined
  - bumped the Target Board cache version to 17
  - versioned the premium-free annual selection seed as
    `app_target_selection_only_converged_v2` and republished the 2026/beta
    reserve because `selection_only` shares the organic refinement path
  - promoted the scratch verification and benchmark into
    `research/studies/2026-07-19_target_roster_convergence/`
- Verification:
  - Python 3.12 compilation and focused cap-contract checks passed
  - the final 64-trial paired replay raised holdout season EV from 1700.1 to
    1742.1, p10 from 1511.1 to 1577.2, and p90 from 1875.3 to 1930.2
  - convergence averaged 3.78 accepted swaps, maxed at nine, and never hit the
    12-swap cap; final serial runtime was 2.30x the one-swap baseline, while the
    focused 16-trial benchmark measured 1.20x-1.21x
  - the fresh premium-free seed completed 1,000/1,000 rosters, trained through
    2025 on 518 rows, and synchronized 166 premium rows to both databases; the
    expected roster reserve is `$6.76` and RJ Harvey's applied reserve is `$3.79`
  - an eight-worker seed attempt exhausted process memory before persistence;
    the one-worker runbook configuration completed successfully

## 2026-07-19 - Bench Call-Option And Waiver-Hurdle Replay

- Request:
  - test whether raising the waiver threshold can favor bench players who
    meaningfully clear replacement and the lineup, while preserving cheap
    long-lived breakout options instead of enforcing a four-week drop rule
- Implementation:
  - created `research/studies/2026-07-19_bench_option_hurdle/` and reused the
    frozen leakage-safe 2022-2025 origins from the managed-auction replay
  - ran 250 paired trials per origin with 250 independent construction and
    evaluation contexts, five salary draws, Top-12, and 13-player rosters
  - held the evaluation waiver stream constant and tested construction-only
    `+1`/`+2`/`+3` RB/WR/TE hurdles, no P90 bench heuristic, and 0.25/0.50
    sustained-breakout option utilities
  - defined the option with trailing-three-week causal detection, at least
    three qualifying played weeks in the next four, and future points above a
    positional lineup-impact strike; no fixed drop deadline was imposed
- Verification:
  - synthetic checks assigned zero value to a steady 8 PPG player and an
    isolated spike, but positive value to early and late sustained breakouts
  - all 7,000 full-replay solves were optimal, produced 13-player rosters,
    stayed within forecast cap, and retained the current waiver baseline in
    every evaluation
  - `+1` lowered development/2025 forecast EV by 1.4/1.9 and increased bench
    spend by `$2.9`/`$2.4`; `+2` and `+3` produced materially larger forecast
    losses and did not reliably create cheap options
  - `bench0` changed 86% of rosters, reduced bench spend by `$1.4`/`$3.2`, and
    changed forecast EV by `+0.4`/`+2.3`, but did not improve 2025 playoff weeks
  - the 0.25 option improved development/2025 forecast EV by `+13.5`/`+2.3`,
    p10 by `+13.9`/`+1.0`, and playoff points by `+25.7`/`+4.2`; its 2025
    literal sustained-15 hit count nevertheless fell by 0.48 and historical
    affordability worsened
- Decision:
  - reject a higher waiver baseline as the proxy and keep production unchanged
  - next test `bench0` and a stricter, price-aware 0.25 option on current v5
    salaries plus the selection reserve and converged organic construction

## 2026-07-19 - Keeper-Aware Bench Option Replay

- Request:
  - value cheap young bench players as keeper call options rather than using a
    raw next-year-minus-current PPG bonus
  - derive future market value from position-specific projected PPG and salary,
    then backtest current-season strength and identified keeper upside under two
    keepers, annual `+$10`, and a three-year maximum
- Implementation:
  - created `research/studies/2026-07-19_keeper_option_bonus/` and reused frozen,
    leakage-safe 2022-2025 construction and outcome inputs
  - fit trial-specific monotone QB/RB/WR/TE projected-PPG-to-Market-`$` curves,
    transformed origin-frozen next-year draws, and scored positive three-year
    contract surplus at acquisition price plus `$10`/`$20`/`$30`
  - limited option activation to two non-QB, below-impact players with genuine
    next forecasts; exact final identification is restricted to the five
    nominal bench slots
  - compared the current 0.25 P90 bench heuristic, `bench0`, a same-refiner
    zero-option control, and option weights `0.0001`, `0.01`, `1`, and `10`
  - added year-policy checkpoint resume, a same-engine analysis, and keeper
    selection concentration outputs after the first long wrapper interruption
- Verification:
  - mechanics checks passed cheap/expensive contract, lottery expectation, and
    exact top-two utility cases
  - all 7,000 final policy-trial rows were optimal, contained 13 players, fit
    the forecast cap, identified at most two keepers, and selected keeper options
    only from the nominal bench
  - versus the same-engine zero-option control, the tiny tie-break raised
    independent forecast mean by `+8.6`/`+4.4`/`+8.1`/`+10.4` and p10 by
    `+7.5`/`+5.5`/`+8.1`/`+12.1` in 2022-2025
  - realized next-year keeper surplus changed by `-$15.1`, `+$0.9`, and `+$8.8`
    in 2022-2024; the three-origin mean was `$30.37` versus `$32.16` for control,
    and 2024 observed keeper-cost coverage was only 56% versus 97%
  - `0.01` changed only 2%-5% of tie-break rosters; weights `1` and `10` changed
    74%-96% without stable keeper improvement
  - tie-break top-two selection concentration rose from 57% in 2022 to 96% in
    2025, where Jaydon Blue and Cam Skattebo occupied 481/500 option slots
- Decision:
  - retain the production waiver baseline and do not promote a keeper bonus
  - keep `bench0` as the simple current-method candidate; next rebuild the keeper
    signal with current residual calibration, modeled-cost plus observed-cost
    evaluation, lexicographic multi-start selection, and concentration controls

## 2026-07-20 - One-Year, One-Hit Keeper Portfolio Replay

- Request:
  - revise keeper upside around one successful bench hit rather than requiring
    two identified options to work
  - use only first-year keeper value, historical next-model validation residuals,
    and protect the rest of the current roster
- Implementation:
  - created `research/studies/2026-07-20_one_year_keeper_portfolio/`
  - loaded current-method OOS `Model_Validations_Resid` next-horizon point means
    and causal residual quantiles; every donor cutoff ends by `origin - 1`
  - used explicit current-projection plus positional next-residual proxies for
    candidates without a dedicated next-validation row
  - scored expected best `max(future Market $ - acquisition price - $10, 0)`
    across all five bench slots; top-two surplus is supplementary
  - started from the same-engine current-only roster, fixed all eight nominal
    starters, and allowed at most two bench swaps subject to zero- or two-point
    exact construction-mean tolerance
  - evaluated complete counterfactual modeled acquisition cost and retained
    observed historical cost as a coverage-limited audit
- Verification:
  - mechanics checks passed best-one, `$10+` probability, top-two, and expensive
    zero-payoff contracts
  - all 3,000 rows were optimal, contained 13 players and five bench slots,
    retained the eight protected starters, fit the forecast cap, changed at most
    two bench players, and respected current-value tolerances
  - `best1_lex0` improved best realized first-year keeper surplus by `+$17.9`,
    `+$12.5`, and `+$6.4` in 2022-2024 and raised `$20+` hit probability by
    16.4, 3.6, and 3.6 percentage points
  - the policy added Kenneth Walker/Rachaad White to 44/49 more 2022 rosters,
    Devon Achane/Zay Flowers to 48/5 more 2023 rosters, and Chase Brown/Bucky
    Irving to 31/24 more 2024 rosters
  - four-origin forecast mean/p10 effects averaged `+1.1`/`+0.6`; actual season
    points averaged `-2.5` and playoff points `+0.1`, with 2024 the cautionary
    current-season slice
  - average bench spend fell by about `$3.2`; the two-point tolerance added
    little consistent keeper value beyond the zero-loss policy
- Decision:
  - advance `best1_lex0`, not the prior three-year/top-two scalar bonus, to a
    current v5-plus-reserve/converged app-method replay
  - keep production unchanged until current mean/p10 and affordability gates
    pass; improve genuine prospective next-row coverage for rookies/first-year
    players such as Cam Skattebo

## 2026-07-20 - Full-Roster Keeper Reinvestment Sensitivity

- Request:
  - test whether dollars saved by cheap keeper-oriented bench players actually
    transfer to stronger starters
  - compare at most one, two, or three newly keeper-influenced bench slots
- Implementation:
  - created `research/studies/2026-07-20_keeper_reinvestment_sensitivity/`
  - rebuilt the current-only control and every keeper policy on the complete
    cached-bank expected weekly profile to avoid five-context full-roster
    overfitting
  - forced shortlisted option candidates to remain on the nominal bench, then
    re-solved and exact-refined every other roster slot under the `$298` cap
  - retained first-year `price + $10` surplus, causal next-validation residuals,
    expected-best bench portfolio utility, and an independent forecast bank
- Verification:
  - mechanics, syntax, nested-option, forced-bench, cap, and construction
    no-loss checks passed
  - all 4,000 rows were unique and optimal across four origins, 250 trials, and
    control/k1/k2/k3 policies; no roster exceeded `$298`
  - k1 accepted one option in 82.8% of trials, k2 filled both in 66.7%, and k3
    filled all three in 43.6%; 97.5% of k1 forced options had at most two years
    of experience
  - k1 shifted `$5.7` from bench to starter spend, raised starter-only forecast
    mean/p10 `+4.6`/`+3.6`, and changed 1.38 nominal starters on average; total
    spend rose only `$0.1`
  - full-roster k1 mean/p10 moved `+0.6`/`+1.2`, actual season points `+8.1`,
    playoff points `-0.5`, waiver starts `+0.34`, and realized best keeper
    surplus `+$2.1` across the three observable future origins
  - k2 added about `$1.0` more realized keeper surplus than k1 but reduced p10
    about 1.1 points and added 0.4 waiver starts; k3 reduced p10, playoff points,
    and realized keeper value
  - the rebuilt control already selected Kenneth Walker on 249/250 2022
    benches, Achane/Flowers on 211/152 2023 benches, and Chase Brown on 226/250
    2024 benches, explaining the small incremental hit benefit
- Decision:
  - do not treat k1/k2/k3 as hard total lottery-ticket counts; they are
    incremental forced additions, and a young player may also be a usable fill-in
  - do not promote a broad full-roster keeper bonus
  - next give every bench player dual current-fill-in and keeper-option value,
    protect roster mean/p10 plus aggregate bench coverage, and let composition
    emerge without age or designated-slot quotas

## 2026-07-20 - Soft Whole-Bench Keeper Portfolio

- Request:
  - replace hard incremental keeper counts with a bonus available to every
    bench player, allowing usable fill-ins and young upside to overlap
  - let the optimizer determine composition while protecting current-year value
- Implementation:
  - created `research/studies/2026-07-20_soft_keeper_portfolio/`
  - scored expected-best first-year `price + $10` surplus across all five final
    bench players using causal next-validation residual draws
  - applied no age, role, or ticket-count quota; greedy search anchors only
    coordinate full-roster re-solves and must remain on the bench
  - required construction-bank mean and p10 at least equal to the rebuilt
    current-only control and evaluated on a separate 250-context bank
  - measured draw-level option winner concentration, current fill-in marginals,
    starter reinvestment, waiver use, and realized keeper outcomes
- Verification:
  - mechanics and syntax checks passed; all 2,000 rows were unique, optimal,
    under cap, and contained 13 players, eight starters, and five bench players
  - every soft roster passed its 50-context construction mean/p10 gates
  - 49.4% of rosters accepted no incremental search anchor, 24.3% accepted one,
    13.4% accepted two, and 12.9% accepted three or more; average was 0.98
  - control/soft benches averaged 4.07/4.19 effective draw-level options and
    4.62/4.69 players with at least 5% winner share, showing that final option
    concentration—not search anchors—is the useful composition measure
  - predicted expected-best keeper surplus improved `+$4.3`; realized best
    surplus improved `+$3.6`/`+$6.8`/`+$2.8` in 2022-2024
  - starter spend increased `$2.1`, bench spend declined `$1.9`, and independent
    roster mean/p10 averaged `+4.2`/`+2.7`; top-two fill-in value rose `+0.7`
  - only 46.2% of the 506 changed rosters improved both independent mean and
    p10; 2024 averaged `-4.9`/`-3.1` among changed rosters
- Decision:
  - prefer the no-quota expected-best formulation over hard k policies, but keep
    production unchanged because current-year gate generalization is unstable
  - next test all 250 construction contexts or cross-fitted lower-confidence
    gates plus a minimum material keeper-gain threshold, with a 2024 audit

## 2026-07-20 - Production Keeper-Aware Organic Construction

- Request:
  - proceed with the agreed soft keeper portfolio without overfitting three
    realized next seasons; clarify and retain separate current/next residuals
- Changes:
  - removed the live generic 0.25 bench-P90 bonus and raw next-year objective
    blend while preserving the estimated waiver replacement PPG
  - added monotone position-specific current-PPG-to-market-dollar curves and
    positive one-year `next market - acquisition price - $10` surplus draws
  - sampled next residuals independently from the current-season draw with a
    `rho=0.25` common player-market factor
  - added whole-bench expected-best utility with no age, role, or option-count
    quota and a `$1` minimum incremental utility threshold
  - after current ILP plus exact convergence, re-solved the complete roster
    around accumulated nominal-bench anchors and required complete live-bank
    current mean and p10 to remain at least at the baseline
  - kept forced Target Buy/Pass and Current Nomination current-year only; applied
    keeper awareness to organic Target and the annual `selection_only` seed
  - versioned the annual seed as
    `app_target_selection_only_keeper_portfolio_v3` and updated app docs/UI
- Verification:
  - five focused mechanics tests passed for isotonic pooling, keeper-cost math,
    complementary option value, accepted no-loss search, and p10 rejection
  - an eight-roster live 2026 beta Target smoke completed in 3.68 seconds with
    13-player rosters, summary EV/p10, `$25.58` expected-best keeper surplus,
    and 0.125 search-driven keeper additions per roster
  - the fresh v3 annual reserve completed 1,000/1,000 rosters in 133.5 seconds;
    166 seed rates summed to 13, calibration trained through 2025, and the 166
    source/app premium rows matched exactly
  - the republished reserve has `$0.68` selection-weighted player mean, `$8.79`
    expected roster total, and `$3.71` maximum
- Boundary:
  - the database does not store keeper-tenure years; the one-year diagnostic
    assumes a player is eligible to be kept once more, so a player already at
    the league's three-year maximum requires user interpretation
  - the v3 selection reserve is refreshed; keeper-tenure eligibility remains the
    main explicit input limitation

## 2026-07-20 - Isolated NFFC Snake Setup Preview

- Request:
  - test the Snake app's NFFC setup before the NFFC s2 projection run finishes
    by temporarily cloning the completed DK results/templates under an NFFC tag
- Changes:
  - added `Scripts/Modeling/create_snake_nffc_preview.py` to copy the stable
    Snake database into a separate preview file without touching the in-flight
    source `Simulation.sqlite3`
  - cloned DK `Final_Predictions_Resid`, weekly templates, template pools, and
    player map rows to NFFC league keys; remapped template IDs from the DK
    2,000,000 range to the reserved NFFC 3,000,000 range
  - retained the existing 2026 NFFC `Avg_ADPs` slice and stored explicit preview
    provenance/warning metadata
  - enabled the Snake app to use an environment-selected database and offer an
    NFFC selector only when a tagged NFFC prediction slice is available
- Verification:
  - preview build produced 268 final predictions, 5,298 templates, 21,440 pool
    rows, 268 player-map rows, and retained 488 NFFC ADP rows
  - all template-pool references joined to the remapped NFFC template IDs
  - Snake runtime loaded 268 players, all four positions, 268 weekly pools, and
    16 weekly columns; 266 players matched NFFC ADP directly and two deep players
    used the cloned player-map fallback
  - local Streamlit browser smoke switched from DK to NFFC, rendered the player
    grid and setup warning, and reported no browser errors
- Boundary:
  - this is setup-only scaffolding; DK projections and DK weekly scoring remain
    underneath the NFFC tag until the real NFFC s3/s4 outputs replace them

## 2026-07-20 - Sequential Auction Target Stability

- Request:
  - replace hindsight-style target prioritization with a fast sequential policy,
    then broaden and stabilize the final candidate evidence so comparable auction
    tiers appear together
- Changes:
  - added the default history-only Sequential Policy while retaining the old
    staged workflow as `Legacy Oracle`
  - generated hidden noisy salary order/price tapes, revealed only current and
    prior auction history to policy decisions, and scored completed rosters on
    separate weekly outcome contexts
  - expanded 24 heuristic candidates to a 64-player pool using capped
    same-position price/decision-cost/value/PPG peers plus position-relative
    Premium/Mid/Value coverage
  - replaced the additive discovery surrogate with exact 32-path by 16-season
    managed scoring and exposed all 46 unconfirmed rows in a separate watchlist
    with crossed SE, LCB80, tier, peer group, completion, and discovery route
  - confirmed 18 feasible candidates at exact market price; allocated six leader,
    six peer, four position/tier, and two feasible-anchor slots before dedupe/fill,
    capped groups at four and positions at eight, and prevented infeasible
    high-price rows from using anchors
  - reserved low/high max-bid curves for the leading 10 usable confirmations and
    labeled market-only confirmations `Not profiled`
  - separated app/cache versioning from deterministic evidence-bank versioning
    so the broader allocation reuses comparable hidden outcomes
- Verification:
  - all 19 focused app tests passed, including peer caps, position concentration,
    position/salary-tier coverage, feasible anchors, deterministic seeding,
    crossed uncertainty, history-only costs, and completion-gated max bid
  - two exact identical-setting receiver-needy replays produced identical boards
    and curves in 8.77 and 8.72 seconds
  - that replay confirmed all five named peers together: Ladd McConkey, Emeka
    Egbuka, Zay Flowers, DeVonta Smith, and Tee Higgins
  - same-bank broader-pool runs completed in 23.1-29.3 seconds; one confirmation
    set was 7 WR/7 RB/2 QB/2 TE with no peer group above four, and Bijan at `$108`
    retained LCB80 `+14.8`
  - live Streamlit rendering showed both confirmed and discovery tables with 64
    screened, 16 discovery seasons, 18 confirmed, and no browser errors
- Boundary:
  - Current Nomination remains the exact live-price decision tool; nomination
    noise and opponent fallback behavior are still synthetic and should be
    calibrated from historical auction logs

## 2026-07-20 - Sequential Candidate Parallelism

- Request:
  - reduce the now-broader sequential board's latency by using four workers
- Changes:
  - added a persistent Windows-safe spawned process pool for base discovery,
    expanded discovery, market confirmation, and price-curve candidate batches
  - reconstructed isolated simulation/SQLite state per worker and copied the
    live player table so manual salary edits remain active
  - kept discovery and confirmation score caches local to each process, kept all
    price anchors for one candidate in one batch, and restored deterministic
    evaluation order before allocation/ranking
  - exposed a Sequential Workers control defaulting to four while retaining the
    one-worker path; worker count does not enter the evidence-bank seed
  - sorted Counter inputs so display-only fallback/core ties no longer inherit
    per-process hash order
- Verification:
  - 20 focused app tests and syntax checks passed
  - the 320-budget one-worker and four-worker runs matched all 64 result rows,
    all price curves, and all comparable summary fields exactly
  - wall time improved from 25.33 seconds to 10.25 seconds (2.47x), with all four
    requested processes observed
  - live Streamlit health and rendered-page smoke checks passed
- Boundary:
  - parallelism changes runtime only; each candidate still proceeds through its
    nomination path sequentially without access to future prices or outcomes

## 2026-07-20 - Sequential Seed Stability Audit

- Request:
  - explain the implausible Bijan reversal after only marking AJ Brown
    unavailable and determine whether a favorable single seed is trustworthy
- Study:
  - created `research/studies/2026-07-20_sequential_seed_stability/`
  - reconstructed the visible roster with the complete 14-player 2026 beta
    keeper table, entered acquisition prices, and estimated live waiver defaults
  - forced Bijan at market price across 16 independent 48-path by 64-season
    evidence banks, bypassing adaptive confirmation membership
  - compared current pool-shaped sampling with a research-only nested sampler
    that draws the full canonical player universe before masking unavailable
    players
  - separately varied construction, auction-path, and weekly-season roots while
    holding the other components at the production root
- Findings:
  - current matched AJ-on/off gains correlated only 0.25; AJ-off minus AJ-on had
    23.95 SD and ranged from `-35.36` to `+53.36`
  - nested draws raised correlation to 1.00 and reduced AJ-state delta SD to
    1.03 with a `[-2.68, +1.80]` range, confirming that the large reversal was
    random-world remapping rather than an economic AJ effect
  - nested Bijan gain still had 21.07 SD across root seeds and ranged from
    `-48.05` to `+30.61`; the production root was the most favorable of the 16
    nested banks
  - with other components fixed, construction-only gain SD was 16.49,
    weekly-season-only SD 12.69, and auction-path-only SD just 3.95
  - aggregating independent full banks reduced panel-gain SD from 21.07 for one
    bank to 9.13 for four and 5.27 for eight, but multiplying the entire current
    grid is not the most compute-efficient response
- Decision:
  - do not trust current single-bank TARGET signs as seed-robust
  - next implement canonical player-keyed draws, a larger or stratified
    construction bank, and fixed independent evidence blocks with explicit
    block disagreement; first test four 12-path by 64-season confirmation blocks
    because they retain the current 3,072 path-season cells while quadrupling
    independent season coverage
  - keep this as a proposed methodology change until exact parity, runtime, and
    multi-seed stability improve in an app-side prototype

## 2026-07-21 - Sequential Multi-Block Stability Production Update

- Request:
  - implement the agreed stability improvements without averaging away true
    auction-price or weekly-outcome volatility
- Changes:
  - bumped the Sequential Policy/evidence versions and generated projections,
    salary residuals, nomination shocks, construction templates, and validation
    seasons on the canonical full player universe before masking sold players
  - added player-keyed stratified template sampling at mean projected PPG for
    policy construction; every sampled template remains a complete weekly path
  - replaced the single discovery/confirmation crossings with four independent
    policy/evidence blocks: 8 paths by 16 seasons per discovery block and 12
    paths by 64 seasons per confirmation block
  - retained one stochastic clearing price per path and the old 512 discovery /
    3,072 confirmation path-season cell budgets while increasing construction
    contexts from 32 to 128 and confirmation seasons from 64 to 256
  - added random-effects between-block disagreement to the action SE and exposed
    block-positive share/range in both confirmed and discovery tables
  - kept worker count execution-only and made candidate evaluation seeds depend
    on player name/price rather than mutable remaining-pool row position
- Verification:
  - 18 focused sequential tests passed, including canonical tape masking,
    player-keyed balanced templates, and between-block uncertainty
  - exact one-versus-four-worker parity held for all 64 rows and price curves at
    budget 120; the final runtime check improved from 16.51 to 10.20 seconds
  - the app Python 3.9 default-budget benchmark ran in 10.65 seconds after
    eliminating discarded per-block bootstraps; an earlier warm live Streamlit
    board rendered 64 discovery seasons, 256 confirmation seasons, four blocks,
    and the new stability column in 15.2 seconds
  - across the same 16 roots as the audit, AJ-on/off Bijan gain correlation rose
    to 0.993 and the AJ-off delta was +0.37 mean / 0.96 SD; seed SD fell from
    21.07 to 7.99-8.15 and every tested LCB80 remained negative
- Boundary:
  - the fixed panel materially stabilizes action signs but does not eliminate
    all evidence-bank variation; future compute should be added adaptively for
    block sign disagreement or unresolved action boundaries

## 2026-07-21 - Protected Next-Up Salary Anchors

- Request:
  - always include the next three available players by salary in the main
    Sequential Target run
- Changes:
  - identify the three highest raw-market-salary players who are personally
    affordable and legal under the user's current positional maxima
  - force those players into the exact discovery pool and protect them before
    the 18-player confirmation allocation
  - allow protected anchors with weak discovery completion to reach the main
    board as `INCOMPLETE` instead of silently dropping them
  - expose the three protected names in the Sequential Policy summary; they use
    existing confirmation slots and receive low/high curves only if their
    confirmed evidence also earns a curve slot
- Verification:
  - 24 focused app tests passed, including an incomplete top-salary anchor case
  - the default four-worker integration retained 18 confirmed rows and included
    all three empty-roster anchors: Jahmyr Gibbs, Bijan Robinson, and Jonathan
    Taylor

## 2026-07-21 - Unified Sequential Evidence Board

- Request:
  - make confirmed targets and lower-evidence discovery candidates directly
    comparable without implying that discovery is equally actionable
- Changes:
  - merged confirmed and discovery rows into one global block-aware LCB80 order
  - added explicit CONFIRMED/DISCOVERY stage labels, shaded discovery rows, and
    All/Confirmed/Promising Discovery view filters
  - reserved TARGET/WATCH/PASS and Approx Max guidance for confirmed rows;
    discovery rows show `Needs confirmation`
  - kept fresh confirmation as the decision estimate and did not pool it with
    the discovery screen; stage-specific ranks remain in Evidence Details
- Boundary:
  - Signal Rank is a cross-stage scouting comparison, not a confirmed action
    rank; discovery still has one-sixth the path-season cells and broad-search
    winner's-curse risk

## 2026-07-21 - Sequential Keeper Lottery Diagnostics

- Request:
  - bring the ILP app's bench keeper upside into the sequential chain as
    droppable one-year lottery tickets, allowing only a tiny current-year hit
- Changes:
  - factored future market-value draws from acquisition-price surplus so Buy
    and Pass reuse identical next-year outcomes while retaining every path's
    actual paid salaries
  - scored expected-best positive first-year surplus across each completed
    roster's nominal bench and pooled keeper lift with the existing four-block
    paired uncertainty machinery
  - added 1,000-draw keeper-led discovery, guaranteed exact screening for up to
    eight leading prospects, and reserved up to three existing confirmation
    slots for positive screen-level keeper evidence
  - added `LOTTERY`, `KEEPER WATCH`, `CURRENT COST`, and `NO EDGE`
    labels plus keeper LCB80 and full evidence details to the unified board
  - separated roster season-p10 delta from the existing path-lift p10; keeper
    labels allow no more than a two-point current mean or season-p10 loss
  - kept current-season sequential policy, primary rank, and Approx Max Bid
    unchanged; added no age, tenure, or designated-ticket restriction
- Verification:
  - 29 focused keeper/sequential tests and Python syntax checks passed
  - budget-120 one-worker and four-worker outputs matched exactly across all 64
    rows and curves; runtime was 19.50 versus 10.70 seconds (1.82x)
  - the default live Streamlit board completed with four workers in 14.7 seconds
    and rendered the keeper summary, filters, labels, and evidence columns
- Boundary:
  - keeper labels diagnose current acquisition opportunities but do not yet
    cause the rollout policy to prefer keeper upside at later nominations

## 2026-07-21 - Veteran Cliff Calibration

- Request:
  - estimate whether old RB/WR/TE downside is already captured by current and
    next PPG forecasts, weekly templates, and missed-game outcomes before adding
    a soft Sequential Target penalty
- Study:
  - reconstructed uncapped experience for 4,206 historical player-seasons and
    separated current PPG cliffs, extended absence, following-season
    disappearance, and no-useful-following-season outcomes
  - fit position-specific piecewise logistic models with quadratic preseason
    PPG, origin-season effects, player-clustered uncertainty, threshold
    sensitivity, and a draft-relevant starter/elite restriction
  - split threshold-crossing effects from the slope for every later veteran
    year
- Findings:
  - current-year incremental risk was weak for RB and TE; WRs above year nine
    showed the clearest current PPG-cliff signal
  - additional WR/TE veteran years most consistently increased following-year
    disappearance, while RB estimates remained noisy
  - the next-model build forward-fills many missing following-season targets
    with prior PPG, understating above-threshold no-useful rates by 15-24
    percentage points in the validation audit
- Decision:
  - do not deploy a blanket veteran tax; first repair the following-season
    target, explicitly model no appearance as zero keeper value, rebuild the
    next-model calibration, and then estimate only residual age risk
  - keep any RB/TE current-season aversion visibly separate as user preference
    unless stronger evidence emerges

## 2026-07-22 - Current-Season Veteran Value

- Request:
  - test whether aging players are actually poor current-season auction values
    after accounting for both preseason projection and their market discount;
    exclude keeper and next-year value
- Study:
  - reconstructed standalone managed contribution from the same 16 weekly
    profiles used by the auction app, crediting only positive points above the
    position waiver baseline
  - matched above-threshold veterans to up to five same-position, same-season
    younger peers on projection, market, or both
  - used 4,206 seasons of preseason ADP evidence plus 1,049 player-seasons from
    the rolling 2022-2025 v5 auction salary backtest; added player-cluster
    bootstraps, leave-one-season-out ranges, adjusted mean/tail models, and
    quantile sensitivity
- Findings:
  - Top-100 RB matches were mean-neutral, with miss and upside-hit rates both
    modestly lower; this looks like distribution compression rather than worse
    expected value
  - Top-100 WR matches averaged 6.2 fewer managed points and a 10.2-point higher
    miss rate; the sign persisted in every leave-one-season-out slice but the
    player-cluster intervals crossed zero
  - premium TE and recent `$5+` auction veteran cells were too sparse for a
    stable penalty; the broader Top-200 TE survivor cohort was not worse
  - historical projection-matched RB/WR veterans were not uniformly discounted;
    current Henry is about `$4.9` above younger projection peers, while Kittle
    and Kelce are about `$3.0`/`$1.6` below
- Decision:
  - do not alter current PPG or add a blanket age tax
  - if veteran aversion is desired, test it as an explicit ceiling/risk utility
    preference using uncapped experience; separately test a premium-WR weekly
    outcome mixture before treating its warning as predictive calibration

## 2026-07-22 - Uncapped Weekly Template Experience

- Request:
  - remove the template `year_exp` cap so veteran differences and the upper
    matching range are no longer compressed
- Changes:
  - reconstructed historical and current experience from draft year with debut
    fallback, retained the compiled capped value as `source_year_exp`, and
    persisted reconstruction source/delta columns
  - removed the upper clip from `year_exp_scaled`; matching now uses
    `year_exp / 10`, so 13 years maps to 1.3 rather than 1.0
  - added team-aware and explicit Steve Smith/Zach Miller identity handling for
    overlapping historical names
  - rebuilt both retained `beta` and `dk` template/pool slices and synchronized
    the eight generated weekly tables to the auction app and full DB to Snake
- Verification:
  - historical experience now reaches QB 22, TE 17, WR 16, and RB 15 in both
    retained leagues with no null provenance rows
  - current beta inputs are Henry 10, Kamara 9, Kittle 9, and Kelce 13 versus
    compiled source values 8, 8, 9, and 10
  - source, auction-app, and Snake table counts/ranges/named rows matched exactly;
    both live template caches loaded every player with 80 profiles by 16 weeks
  - all 29 auction app mechanics tests and Python syntax checks passed
- Boundary:
  - this changes weekly-template similarity and outcome selection only; it does
    not alter current/next PPG projections or impose a deterministic age tax

## 2026-07-22 - Joint Template Residual and Weekly Outcomes

- Request:
  - improve historical template matching with workload certainty and direct
    observed residual/path outcomes; retain zero-active downside except for the
    non-football Le'Veon Bell 2018 holdout
- Changes:
  - added absolute projected PPG, projection and rank disagreement,
    market/projection gap, and full-universe RB/pass-catcher room rank, gap, and
    concentration features
  - replaced distance-range-normalized 2x weights with position-specific
    absolute-distance kernels, weak-match uniform shrinkage, and a 5% donor cap
  - retained ordinary zero-active templates, added auditable eligibility/reason
    fields, and excluded Bell 2018 as `contract_holdout`
  - changed the managed auction app to add the selected donor's pool-centered
    PPG residual to the calibrated current point forecast and apply that same
    donor's managed weekly trajectory; removed the independent current-year
    model residual and 30% template blend
  - bumped Sequential Target/evidence versions to 6/4 and synchronized the
    rebuilt generated tables to the auction app
- Verification:
  - full beta and DK builders completed in 11.5 and 11.4 seconds; both retained
    5,298-row slices have complete new fields and matching source/app tables
  - all 180 pool probabilities sum to one; donor probability is capped at 5%;
    minimum position ESS ranges from 47.6 to 58.5
  - Bell 2018 has zero pool uses; 61 distinct eligible zero-active donors appear
    in 134 pool rows
  - all new match fields are populated for current beta players
  - all 30 auction app tests passed, including the direct joint-draw test; a
    live database smoke loaded 180 players and sampled finite 4x12x16 contexts
  - budget-120 one-worker/four-worker outputs matched all 64 rows exactly;
    runtime improved from 19.52 to 9.91 seconds (1.97x)
  - Snake loaded all 268 DK players with 80 donors and 16 weekly columns from
    the copied database; its app-specific residual blend was not changed
- Study:
  - `research/studies/2026-07-22_joint_template_outcomes/`

## 2026-07-22 - Joint Template Rolling Validation

- Request:
  - run rolling calibration for the new pool-centered historical residual plus
    matched managed-weekly-path engine and determine whether it finds upside
    profiles
- Study:
  - replayed 1,620 held-out player-seasons from 2017-2025 with every donor
    restricted to a strictly earlier season
  - used causal `Final_Validations_Resid` OOS point forecasts for held-out
    targets while retaining the production template bank's donor residuals
  - compared adaptive centered production, uncentered, uniform, legacy, and
    projection/experience-only matching across 8,100 target-method rows
  - used season-clustered bootstrap comparisons and separated overall event
    calibration from player-level event discrimination
- Findings:
  - recent centered PPG bias was -0.01, managed-contribution bias was +0.89,
    and both nominal P10-P90 intervals covered 80.8%
  - uncentering worsened PPG and contribution CRPS by 0.271 and 2.224 and moved
    recent biases to -1.16 PPG and -10.22 managed points
  - overall `+3`, `+5`, and impact rates were close to calibrated, but their
    ranking AUCs were 0.535, 0.559, and 0.655; impact separation came primarily
    from absolute contribution at RB/WR
  - top impact bins were overconfident, tail lift did not predict realized
    surprise, and young RB/WR residual-upside probabilities were overstated
- Decision:
  - retain pool centering and the joint donor for coherent managed EV/downside
  - do not expose current template tail width as a breakout score or infer an
    age penalty from this replay
  - require a separately roll-validated and recalibrated residual-tail layer
    before adding player-specific upside to the objective or UI
- Output:
  - `research/studies/2026-07-22_joint_template_rolling_validation/results/summary.md`

## 2026-07-23 - Blind Sequential Salary-Bias Replay

- Request:
  - rerun selected-roster salary-bias diagnostics under the current blind
    Sequential Target policy and determine whether the old oracle/static bias
    disappears
- Study:
  - paired 32 construction/order trials at each 2022-2025 rolling origin across
    static full-surface and blind-sequential arms, each with no reserve and the
    strictly prior-origin half reserve
  - used current v5 salary surfaces, causal weekly templates, production noisy
    salary nominations, historical-price replay revealed one nominee at a time,
    and current history-only cached-priority recourse
  - separated initial target-plan bias from final acquired-roster legality
- Findings:
  - static no-reserve rosters spent `$291.8` on their selected sampled salary
    rows, `$306.2` at v5 point prices, and `$323.0` historically
  - the `$31.2` actual-minus-sampled gap decomposed into a `$14.4`
    scenario-shopping discount plus a `$16.8` actual-minus-point residual
  - blind no-reserve initial plans cost `$283.3` at point prices and `$300.6`
    historically; their `$17.3` residual was essentially unchanged from static,
    but the full-surface scenario discount was absent
  - initial-plan historical-cap feasibility rose from 14.1% static to 43.0%
    blind and 52.3% blind with the half reserve
  - blind no-reserve/half completion was 93.8%/95.3%; every completed roster was
    paid-cap legal, but no-reserve left `$45.4` unused on average
- Decision:
  - blindness materially removes oracle scenario shopping, not the
    player-selection residual
  - retain the half reserve as partial protection; do not call the remaining
    salary problem solved
  - next tune sequential ceilings/reinvestment so observed bargains and market
    inflation can use stranded salary without reintroducing future-price access
- Output:
  - `research/studies/2026-07-23_sequential_salary_bias/results/readout.md`

## 2026-07-23 - Auction Weekly Template Comp Explorer

- Request:
  - add a Streamlit view for selecting a player and inspecting the historical
    comps that now drive managed weekly outcomes
- Changes:
  - added a cached exact join across the current player map, weighted template
    pool, and historical template tables
  - added a collapsed explorer below the draft grid with player and top-comp
    controls, pool residual/participation diagnostics, and separate comp/profile
    tabs
  - comp rows show sampling weight, distance, causal preseason PPG, actual active
    PPG, raw residual, played/active games, experience, ADP, and position-aware
    workload share/rank/gap/concentration
  - the target profile exposes the dimensions used in matching and explains
    that production centers the weighted raw residual before applying the
    selected donor's residual and weekly path jointly
- Verification:
  - the current beta database returned 180 target players and 14,400 exact donor
    rows in 0.47 seconds; spot checks covered QB, RB, WR, and TE displays
  - all 35 auction app tests passed, including live-database pool weight/rank
    checks and position-specific formatting tests
  - the local Streamlit server passed its health check; browser click-through
    was unavailable in the current tool session

## 2026-07-23 - Weekly Template Context Rolling Ablation

- Request:
  - test NFL draft value, projected team environment, and historical-season
    recency as additions to the weekly-template matcher
- Study:
  - added
    `research/studies/2026-07-23_template_context_ablation/`
  - reused the production-scale strict rolling framework with 1,620 held-out
    2017-2025 player-seasons and strictly earlier donors
  - reproduced the prior production baseline exactly and evaluated a full
    draft/support/recency factorial plus four- and twelve-season recency
    sensitivities across 16,200 target-method rows
  - represented draft capital as log draft-chart value with a 0.75 match weight
    halving every two target experience years
  - represented team environment as a causal preseason QB1/top-two-RB/top-four-
    WR-or-TE projection index, excluding the target player's own points and
    ranking within season/position
  - applied recency only as a sampling prior; all same-season/future donors
    remained prohibited
- Findings:
  - the predeclared combined method changed recent PPG/contribution CRPS by
    `-0.0011`/`-0.0120` and played-games CRPS by `-0.0057`; only the played
    interval excluded zero in the season-cluster bootstrap
  - the combined recent impact AUC rose from `0.655` to `0.668`, but event gains
    were small and position-mixed
  - recency supplied nearly all stable benefit; eight seasons reduced weighted
    mean donor gap from `7.21` to `5.84` years and 10+-year weight from `32.3%`
    to `20.5%`
  - eight- and twelve-season priors both improved recent played-games CRPS with
    season-bootstrap intervals below zero; twelve was slightly safer in the
    2023-2025 point/event checks, while four was too aggressive
  - draft capital tightened young-player pedigree distance but its outcome
    gains reversed by position; supporting-cast improvement appeared mainly in
    the 2023-2025 contribution slice and did not repeat in development
- Decision:
  - do not promote the full combined matcher or global draft/support weights
  - advance only a light 8-12-season recency prior as the supported production
    candidate; the exact half-life is not resolved, and twelve is the
    conservative option
  - keep production unchanged until that isolated change is approved
  - reserve draft capital for a separately calibrated residual-upside layer and
    keep supporting cast as an auditable diagnostic pending stronger evidence
- Verification:
  - all 1,620 targets produced ten paired methods, all probability vectors
    normalized, no non-prior donor entered a pool, and the study reran in 60.6
    seconds

## 2026-07-23 - Weekly Template Feature Pruning

- Request:
  - test whether removing redundant current matcher features improves the
    template method before locking the final context set
- Study:
  - added `research/studies/2026-07-23_template_feature_pruning/`
  - replayed 30 feature/recency specifications across 1,620 held-out
    2017-2025 player-seasons and 48,600 paired prediction rows
  - protected direct projected PPG, position projection rank, uncapped
    experience, and primary position role signals
  - selected on 2017-2022 only with aggregate calibration/downside guardrails,
    position-level CRPS safety checks, and a paired season-level
    one-standard-error rule
  - audited fixed 2023-2025 holdouts, a nested rolling selector, position
    splits, and season-clustered bootstrap uncertainty
- Findings:
  - `no_exp_interaction__r12` was the position-safe selection: retain direct
    projected PPG and uncapped experience, remove their interaction, and use
    the conservative 12-season recency prior
  - versus production in 2023-2025, PPG/contribution/played-games CRPS changed
    by `-0.00405`/`-0.03245`/`-0.00441`; PPG's clustered interval excluded zero,
    while contribution remained uncertain
  - impact Brier improved by `-0.00066` and impact AUC rose from `0.614` to
    `0.630`; zero-week Brier worsened by only `+0.00010`
  - the nested selector chose the final specification at every 2023-2025
    origin and improved all three nested CRPS summaries versus production
  - dropping component ranks was rejected because its aggregate development
    result hid a `+0.59%` worst-position composite and `+2.05%` worst
    individual-position metric deterioration
  - aggressive compact variants degraded downside and availability precision
- Decision:
  - recommend removing only `projection_x_exp` and adding the 12-season
    recency prior in the next production update
  - keep ADP/market context, disagreement, component ranks, room
    hierarchy/concentration, and pass-catcher team context
  - production remains unchanged until explicitly promoted
- Verification:
  - the current production baseline and isolated 12-season recency arm exactly
    reproduced their prior studies across all checked outputs
  - all 48,600 rows were unique and complete, probability vectors normalized,
    no future donor entered a pool, and the final replay completed in 106.2
    seconds

## 2026-07-23 - Weekly Template Weight Sensitivity

- Request:
  - run one final retained-weight sensitivity check before promoting the
    feature-pruning and recency recommendation
- Study:
  - added `research/studies/2026-07-23_template_weight_sensitivity/`
  - used the recommended no-`projection_x_exp`, 12-season-recency matcher as
    the reference
  - moved 12 retained conceptual feature families independently to 75% and
    125% of reference, then swept all match weights together from 0.01x to
    1.50x while preserving the matched top-80 ordering
  - replayed 36 paired methods over 1,620 strict rolling targets and 58,320
    target-method rows
  - required at least 0.1% development composite improvement, development and
    temporal position safety, non-worse temporal aggregate, and exact selection
    at two of three recent nested origins
- Findings:
  - no individual feature-family move improved development composite by more
    than `0.0121%`; relative retained weights are locally flat
  - near-uniform 0.05x weighting won 2017-2022 by `0.2122%`, but its temporal
    composite worsened `0.0273%`, its worst position metric worsened `1.1017%`,
    and it won only one recent nested origin
  - that winner worsened temporal played-games CRPS by `+0.00284`; the
    season-clustered interval was `+0.00064` to `+0.00445`
  - intermediate 0.25x-0.625x settings modestly improved the temporal aggregate,
    but none repeated as the exact recent nested choice; the selected scale
    drifted from 0.01x to 0.05x to 0.125x as the training origin advanced
  - zero of 35 alternatives cleared every promotion rule
- Decision:
  - retain the reference weights for the pending production update
  - do not bundle a lower sampling-kernel sharpness into the supported
    12-season-recency plus interaction-removal change
  - keep softer within-pool weighting as a separate future calibration
    hypothesis
- Verification:
  - the reference reproduced `no_exp_interaction__r12` exactly across all
    checked distribution, participation, event, ESS, and donor-age outputs
  - all 58,320 rows were unique and complete, no future donor entered a pool,
    and the final replay completed in 125.4 seconds

## 2026-07-23 - Weekly Template Matcher Promotion

- Request:
  - promote the validated feature-pruning and recency specification
- Changes:
  - removed `projection_x_exp` from every position's active match-distance
    weights while retaining the derived column for backward-compatible comp
    diagnostics
  - applied the fixed `0.5 ** (season_gap / 12)` recency multiplier after the
    adaptive distance/local-uniform probabilities, then renormalized and
    reapplied the 5% donor cap
  - persisted donor season gap and recency multiplier on pool rows plus the
    half-life, weighted donor gap, last-three-season weight, and 10+-season
    weight in pool summaries/player maps
  - left all retained feature weights and kernel settings unchanged
  - rebuilt both beta and DK slices and synchronized the generated tables to
    the managed auction and Snake app databases
- Verification:
  - production code compiled and a focused constructed-pool test exactly
    reproduced the rolling-study formula
  - all 35,840 persisted beta/DK pool rows in each source/app database have
    strictly prior donors and exact recency multipliers
  - every pool sums to one within floating-point tolerance and no donor exceeds
    the 5% cap
  - all four auction Template Explorer tests passed against the rebuilt beta
    database

## 2026-07-27 - Projection V2 Milestone 1

- Request:
  - begin the agreed same-repository V2 rebuild with a clean identity and
    outcome foundation while leaving production behavior untouched
- Changes:
  - added `Scripts/V2/` builders for canonical/provisional player identity,
    provider aliases, and exact-season player outcomes
  - used official nflverse player and weekly releases as the canonical played
    data, recording URLs and SHA-256 hashes for downloaded inputs
  - assigned every player a stable internal key so pre-play rookies can enter
    the spine before a GSIS ID exists and keep that key after later promotion
  - replaced shifted/forward-filled games and actuals with direct weekly
    aggregation; retained low-sample seasons and separated the four-game
    `useful_season` flag from target availability
  - added atomic table publication, schema/index contracts, source manifests,
    build history, focused unit tests, and the
    `docs/data_contracts/v2_identity_outcomes.md` contract
  - generated the ignored shadow database
    `Data/Databases/Projection_V2.sqlite3`; no production script or database was
    changed
- Verification:
  - 11 focused tests passed and all V2 modules compiled
  - the shadow build contains 6,456 identities, 37,241 aliases, and 9,922
    outcomes across 2006-2025
  - all outcome and provider-key uniqueness checks passed, all outcome GSIS IDs
    resolve to an identity, scoring components reconcile within floating-point
    tolerance, and no zero-opportunity target row was published
  - the Adrian Peterson same-name records resolve to distinct confirmed players,
    Trey McBride 2023 retains all 16 opportunity games, and Le'Veon Bell 2018
    does not receive a synthetic outcome row
- Next:
  - build the Milestone 2 player-season projection spine with cutoff-aware
    availability, explicit no-appearance rows, and nullable conditional PPG

## 2026-07-27 - Projection V2 Milestone 2

- Request:
  - build the leakage-safe player-season projection spine agreed after
    Milestone 1
- Changes:
  - added allowlisted same-season candidate evidence from projection, ADP,
    market, ranking, and NFL draft tables while explicitly excluding realized
    outcomes and the unreliable legacy rookie college tables from eligibility
  - added auditable `player_season_sources` and unique
    `player_season_spine` tables covering 2006-2026
  - represented completed candidates without a qualifying outcome as
    `appeared=0`, zero games, and zero unconditional points while leaving
    conditional PPG null; pending 2026 rows expose no outcome labels
  - retained observed low-sample PPG but separated target availability from the
    four-game default PPG-training threshold
  - added explicit source-season, NFL-history cutoff, experience-known,
    source-family, conflict, target-availability, foundation-run, and build-run
    provenance fields
  - expanded identity resolution across position groups and used
    season-specific preseason positions when nflverse's canonical group is not
    fantasy eligible; this recovers Travis Hunter's 2025 offensive outcome
  - made a standalone Milestone 1 rebuild invalidate/remove downstream spine
    tables so stale player keys cannot remain publishable
  - documented the contract in
    `docs/data_contracts/v2_projection_spine.md`
- Verification:
  - 18 focused tests pass
  - the final shadow build contains 52,310 source observations and 14,075
    candidate seasons: 8,462 observed opportunity outcomes, 4,862 completed
    non-appearances, and 751 pending 2026 candidates
  - 6,829 observed player-seasons meet the default conditional-PPG training
    threshold; 468 rows retain explicitly unknown experience rather than being
    encoded as veterans
  - uniqueness, candidate-source reconciliation, source-family totals, cutoff,
    pending-label, non-appearance, experience-nullability, identity, and
    lineage checks all pass
  - Travis Hunter retains seven 2025 WR opportunity games; Le'Veon Bell 2018
    and Andrew Luck 2019 remain candidate rows with participation zero and null
    conditional PPG
- Next:
  - build Milestone 3 same-season preseason consensus and strictly prior NFL
    history features on this spine, then audit coverage and redundancy before
    selecting model inputs

## 2026-07-27 - Projection V2 Milestone 3

- Request:
  - build the agreed feature mart before fitting the V2 residual and
    participation models
- Changes:
  - added normalized long-form provider projection and market/rank tables with
    exact source-table/player-key lineage
  - rescored complete provider component projections under the configured
    league rules and retained published point totals only as explicit fallbacks
  - built same-season expert consensus/disagreement, market, role composition,
    team/room hierarchy, lifecycle, and strictly prior NFL-history features
  - made exact-prior-year distinct from last-observed history and prohibited
    forward filling across missing calendar years
  - fixed rank-style positions such as `RB-01`, excluded kicker/defense rows
    after canonical position recovery, and persisted source-resolution audits
  - fixed NFL draft metadata enrichment so confirmed rookies inherit round,
    pick, team, college, and draft year without changing stable player keys
  - capped the reviewed wide mart at 92 features while retaining normalized
    long tables for future challengers
  - published candidate manifests with 30 residual, 20 participation, and 12
    template features plus coverage and within-family correlation audits
  - documented the contract in
    `docs/data_contracts/v2_feature_mart.md`
- Verification:
  - 27 focused V2 tests pass and all V2 modules compile
  - the final shadow build contains 14,075 feature rows, 31,842 projection
    values, 31,821 market values, 92 catalog features, and 47 high-correlation
    within-family pairs
  - feature keys exactly match the spine, no target column enters a manifest,
    all 751 pending rows retain null labels, and feature/spine/foundation
    lineage reconciles
  - aggregate source resolution is 99.4%; positioned sources resolve at or
    near 100%, while the ambiguous positionless FantasyPros best-ball source
    remains explicit at 91.6%
  - all 102 current rookies have explicit no-history status; 97 have expert
    consensus, 79 have ADP, and 80 have draft-capital features
  - no production modeling script or database was changed
- Next:
  - build Milestone 4 rolling-origin baselines comparing expert consensus with
    a shrunk residual challenger and fitting participation separately
  - evaluate provider-era and history-depth slices, then prune correlated
    features through within-family OOS ablations before promotion

## 2026-07-27 - Projection V2 Milestone 4A

- Request:
  - simplify the S1/S2-style model, hyperparameter, feature-selection, PCA, and
    aggregation surface; build the agreed rolling OOF framework and run the
    initial conditional-PPG and participation results
- Changes:
  - wrapped the existing `SciKitModel.time_series_cv` with deterministic
    five-fold assignments in each 2017-2025 season and persisted exact
    `training_through_season` provenance
  - limited the primary search to expert baselines, Ridge/logistic, shallow
    LightGBM, and isolated KBest/PCA/agglomeration challengers
  - added compact/full reviewed feature sets, narrow four-trial searches,
    pooled/season-mean metrics, and position/history/provider-depth/provider-era
    slices
  - added the recent provider active-game PPG field to the conditional manifest
    and used active-game-when-available/team-game-fallback consensus as the
    expert incumbent
  - found and fixed 314 provisional duplicate/truncated aliases that were
    incorrectly labeled participation zero; these are now
    `unresolved_identity` with null targets, and identity confirmation is
    audit-only
  - made source row resolution use column/value zipping rather than relying on
    pandas named tuples, and added a validated active-M2 reuse path for feature
    rebuilds
  - published model runs, fold assignments, exact specifications,
    hyperparameter trials, 95,850 OOF predictions, score/slice summaries, and
    durable research outputs to the ignored V2 database/study folder
  - ran 16 fold-identical feature-family dropouts with season bootstrap
    intervals
- Results:
  - direct shallow LightGBM leads conditional PPG at 3.144 RMSE; position-aware
    consensus recalibration is 3.200, direct full Ridge is 3.173, and the expert
    hybrid is 4.224
  - direct LightGBM beats recalibration in all nine seasons by 0.054 mean RMSE,
    but its advantage over direct Ridge is only 0.029 with an interval crossing
    zero
  - participation LightGBM scores 0.122 Brier versus 0.137 for full logistic
    and beats it in eight of nine seasons; recent-era separation is small
  - KBest, PCA, and agglomeration all lose to raw full pipelines
  - projection level is the only clearly essential conditional Ridge family;
    participation projection level, history, and lifecycle have positive
    season evidence; conditional market and team/role additions are weak
- Verification:
  - 32 focused V2 tests pass and every V2/research module compiles
  - every validation player-season appears in exactly one fold, every season
    appears in all five folds, no OOF row trains on its target season, and no
    final prediction is null
  - the clean run has 3,701 conditional rows, 7,877 participation rows, 18
    specifications, and 95,850 OOF predictions
  - production projection, template, optimizer, and `Model_Inputs.sqlite3`
    outputs were not changed
- Next:
  - fit retained direct Ridge/LightGBM and logistic/LightGBM finalists through
    2025, publish 2026 shadow predictions with exact lineage, and complete the
    final whole-season/calibration check
  - feed OOF model errors and participation probabilities into the existing
    joint weekly-template framework without drawing an independent second
    residual

## 2026-07-28 - Projection V2 Legacy-Inspired Feature Challengers

- Request:
  - add and test the strongest missing concepts from the legacy feature
    pipeline, especially projection versus experience expectations and player
    ADP versus same-position teammates
- Changes:
  - added 12 leakage-safe governed features across experience context,
    self-excluded teammate ADP, and team opportunity shares
  - preserved the original 31-feature incumbent and published the additions in
    `residual_legacy_challenger_v1`
  - rebuilt the shadow mart to 104 features and refreshed M4A on the matching
    feature lineage
  - ran fold-identical Ridge/LightGBM family additions, individual LightGBM
    additions, and a deterministic full-column attribution replay
  - added future M3 invalidation of stale complete M4 model runs
- Results:
  - adding all 12 worsens Ridge by 0.0131 RMSE and the original shallow
    LightGBM by 0.0080
  - deterministic family point estimates are -0.0035 for teammate ADP,
    -0.0038 for opportunity share, and +0.0015 for experience context; every
    season-bootstrap interval crosses zero
  - `expert_ppg_exp_diff`, the direct analogue of the remembered legacy
    feature, changes deterministic LightGBM RMSE by only -0.0002
  - the strongest individual point estimate is self-excluded mean teammate ADP
    gap at -0.0039 RMSE, but its interval crosses zero and it is neutral in
    2023-2025
  - projected target share is unavailable through 2023 and has only one
    learnable validation season, so it cannot be promoted
  - the first stochastic individual replay produced false tiny gains because
    adding even an unavailable column changed LightGBM's 80% column sample;
    full-column deterministic attribution removed that artifact
- Verification:
  - the focused tests and research modules compile
  - the feature build contains 14,075 rows, 104 features, correct
    31/12/19/12 manifest counts, 50 high-correlation pairs, and SQLite
    integrity `ok`
  - refreshed M4A exactly reproduces 3.1443 conditional-PPG RMSE, 0.1222
    participation Brier, 3,701/7,877 target rows, and 95,850 OOF predictions
  - production projections, templates, optimizers, and the pre-existing
    modified `Model_Inputs.sqlite3` were not changed
- Decision:
  - promote none of the 12 features; retain them only in the separate research
    manifest and keep the 31-feature incumbent unchanged
- Next:
  - proceed to finalist fitting, 2026 shadow predictions, whole-season
    robustness/calibration, and joint weekly-template integration

## 2026-07-28 - Projection V2 Regularized Linear Sparsity

- Request:
  - test whether Lasso or Elastic Net can improve the direct conditional-PPG
    model and determine whether a smaller effective feature set is preferable
- Changes:
  - added Lasso and Elastic Net execution support to the standardized V2 linear
    pipeline while leaving the default 18-model M4A surface unchanged
  - ran direct Ridge, Lasso, and Elastic Net on identical five-fold 2017-2025
    rolling OOF splits for the 31-feature incumbent and incumbent plus all 12
    legacy-inspired challengers
  - used exhaustive coverage of the seven-value Lasso and four-value Ridge
    grids within the existing 20-draw search; Elastic Net sampled 15-18 of 35
    narrow discrete combinations per outer fold
  - refit all 270 model/season/fold populations using the selected outer-fold
    parameters and persisted raw plus missing-indicator coefficient selection
    frequencies
- Results:
  - incumbent Lasso scores 3.1656 RMSE versus 3.1747 for fold-identical Ridge,
    a 0.0091 gain with five of nine season wins and interval
    `[-0.0238, +0.0027]`
  - expanded Lasso is the best linear point estimate at 3.1615, but its 0.0041
    gain over incumbent Lasso has interval `[-0.0233, +0.0159]`; it worsens
    rookie and second-year slices
  - Elastic Net is slightly worse than Lasso and every selected incumbent
    penalty uses `l1_ratio` 0.7 or 0.9
  - incumbent Lasso selects a mean 23.6 of 35 raw features, with 19 selected in
    at least 80% of the 45 exact fits; it also selects a mean 8.6 missingness
    indicators, so static pruning must preserve no-history/source-availability
    information
  - direct shallow LightGBM remains better at 3.1443 RMSE
- Verification:
  - all 22,206 coefficient-refit predictions reproduce stored OOF predictions;
    maximum absolute difference is `3.55e-15`
  - focused modeling tests pass and the study/core modules compile
  - the study writes only under
    `research/studies/2026-07-28_v2_regularized_linear_sparsity/`; the V2
    database, production projections, templates, and optimizers are unchanged
- Decision:
  - retain incumbent Lasso as the preferred sparse linear diagnostic, but do
    not promote it over LightGBM, do not promote Elastic Net or the expanded
    manifest, and do not delete low-frequency features without a nested-origin
    static-subset confirmation
- Next:
  - include Lasso alongside Ridge and LightGBM in finalist fitting and 2026
    shadow comparison, then complete whole-season calibration and joint-template
    integration

## 2026-07-28 - Projection V2 Standardized Provider Challengers

- Request:
  - ensure projection inputs are scored apples-to-apples from provider raw
    stats, preserve a path to learn historical provider differences without
    upweighting one-year sources, and test compact rate, opportunity, and
    disagreement additions
- Changes:
  - removed provider-published fantasy totals and raw PPG from modeled
    consensus, provider columns, and provider room/team context
  - reconstructed every eligible provider point estimate under configured DK
    base scoring
  - added a guarded imputation: fill exactly one missing required component
    only from the player-season median of at least two other providers;
    otherwise leave the provider unscored
  - repaired 4,010 FFA rows, primarily the historically missing receptions
    field, while retaining explicit imputation provenance
  - masked learned provider-specific columns until the provider has three
    prior projection seasons; new providers still enter equal-weight consensus
  - published 10 projection-shape/rate, eight component-disagreement, and
    eight provider-specific PPG features in the separate
    `residual_projection_challenger_v1` manifest
  - ran fold-identical 2017-2025 Lasso and deterministic full-column LightGBM
    family tests plus individual-provider tree attribution
- Results:
  - deterministic LightGBM improves from 3.1230 to 3.1145 RMSE with all eight
    gated provider columns, a -0.0085 delta with six of nine season wins and
    interval `[-0.0198, +0.0021]`
  - FFToday supplies most of the exploratory provider result at -0.0073 RMSE;
    its unadjusted individual season interval is `[-0.0136, -0.0004]`, driven
    mainly by QB and RB
  - the provider family worsens Lasso by 0.0275; projection shape is neutral
    for Lasso and worse for LightGBM; disagreement worsens both
  - FantasyPoints, FFF, FanDuel, and PFF produce exactly zero provider-specific
    OOF change because the three-prior-season gate masks them
  - provider/rate additions do not improve rookies or other no-history players
- Verification:
  - 42 focused V2 tests pass and all touched modules compile
  - the final shadow mart has 14,075 rows, 131 features, correct
    31/12/26/19/12 manifest counts, 59 high-correlation pairs, and one active
    feature run lineage
  - the study uses 3,696 OOF conditional-PPG rows; removing published-total
    fallback costs only five historical OOF rows and seven 2026 consensus rows
  - production projections, templates, optimizers, and the pre-existing
    modified `Model_Inputs.sqlite3` remain unchanged
- Decision:
  - keep the 31-feature incumbent unchanged and retain all 26 additions only
    as a governed research manifest
  - carry the gated provider family/FFToday candidate into nested/static
    confirmation rather than manually weighting a provider from the same OOF
    evidence
  - treat configured scoring and guarded imputation as correctness fixes
    independent of performance
- Next:
  - run the nested/static provider confirmation, then proceed to finalist
    fitting, 2026 shadow predictions, whole-season calibration, and joint
    weekly-template integration

## 2026-07-28 - Projection V2 Consensus Ladder

- Request:
  - run the final projection-only tests covering learned provider consensus,
    provider-relative information, room disagreement, and active-PPG alignment
- Changes:
  - built an isolated study with projection-only Lasso and deterministic
    LightGBM ladders plus fold-identical additions to the 31-feature full model
  - estimated global and position-specific nonnegative provider weights using
    only earlier-season outcomes, three-season provider eligibility, an
    equal-weight-median shrinkage component, and inner-origin penalty selection
  - added a strictly causal position-by-history router diagnostic that selects
    between projection-core and full forecasts from earlier OOF errors
  - changed no production code, database, manifest, projection, template, or
    optimizer output
- Results:
  - full deterministic LightGBM remains best at 3.1230 RMSE; projection-only
    consensus core is close at 3.1326 with essentially identical MAE
  - every new projection family worsens the full model by 0.0020-0.0103 RMSE
  - the global provider stack improves raw realized team-game PPG by 0.0096
    RMSE versus the configured median, but wins five of nine seasons and has
    interval `[-0.0318, +0.0117]`; its conditional-model features worsen RMSE
  - active alignment and room disagreement have tiny unstable projection-only
    gains and no full-model transfer; active data begins only in 2024-2025
  - a causal position-by-history router improves the full-model point estimate
    by 0.0055-0.0097 RMSE across 25/50/100-row gates, but all intervals cross
    zero
  - the 2026 route from the 25/50-row rules uses projection-only for
    limited-history QB/TE/WR and the full model for limited-history RB and all
    veterans
- Verification:
  - all 20 specifications contain 3,696 OOF rows over the exact 2017-2025
    rolling population; the provider stack has 7,618 evaluable rows
  - analysis outputs persist the feature coverage, provider weights,
    fold-identical comparisons, history-router choices, and 2026 route
  - focused V2 tests and study compilation pass; production and the
    pre-existing modified `Model_Inputs.sqlite3` remain untouched
- Decision:
  - close the broad projection feature search, keep the configured median and
    31-feature incumbent, and promote no new family
  - carry projection core plus the causal history router as an unpromoted
    finalist into whole-season/calibration validation and template integration
- Next:
  - fit the retained full and projection-core finalists through 2025, publish
    2026 shadow component forecasts, and perform the final router/calibration
    replay before integrating with joint weekly templates

## 2026-07-28 - Projection V2 Position-Specific Models

- Request:
  - test separate position models because the relevant features and
    relationships may differ materially for QB, RB, WR, and TE
- Changes:
  - fit deterministic shallow LightGBM projection-core and full models as four
    independent QB/RB/WR/TE components
  - added the proposed three-role QB/RB/WR+TE structure to preserve shared
    receiving information
  - used position/component-specific five-fold hyperparameter selection while
    preserving strict prior-season training and the exact 3,696-row OOF
    evaluation population
- Results:
  - four-position projection core worsens RMSE from 3.1326 to 3.1752, with
    interval `[+0.0192, +0.0693]` and only one of nine season wins
  - four-position full worsens RMSE from 3.1230 to 3.1389
  - independent full QB/RB improve only 0.0029/0.0034 RMSE, while independent
    WR/TE worsen 0.0352/0.0184
  - the three-role full model scores 3.1231, effectively identical to pooled
    full; the three-role projection model remains 0.0132 worse than pooled
  - a two-model QB/RB+WR+TE full split also ties at 3.1237 and is worse by a
    mean 0.0101 RMSE per season in 2023-2025; projection core is 0.0058 worse
  - the three-role full model improves 2023-2025 RMSE from 3.1024 to 3.0792,
    but wins 2023/2025 and loses 2024, so the short recent window is not a
    promotion result
- Verification:
  - 14 component fits create four complete 3,696-row stitched prediction sets
  - all 14,784 assignment rows satisfy
    `training_through_season < season`
  - study and core modules compile, 42 focused V2 tests pass, and production
    outputs remain unchanged
- Decision:
  - reject four fully separate position models and retain pooled models as
    primary
  - carry only the three-role full model as an unpromoted temporal-robustness
    finalist
- Next:
  - include three-role full in the pending whole-season/calibration replay
    alongside pooled full, projection core, and the causal history router

## 2026-07-28 - Projection V2 Position-Aware Feature Families

- Request:
  - test whether projection-versus-experience, teammate ADP, opportunity, and
    room-ambiguity features are more useful within individual positions and
    young-player groups
- Changes:
  - added four prespecified leakage-safe families separately to the governed
    full QB, RB, WR, and TE models
  - tailored opportunity inputs by role and excluded `team_target_share`
    because its 2024 start leaves only one learnable OOF season
  - tested richer QB competition, positional room clarity, and WR/TE
    pass-catcher room context
  - applied exact season sign-flip tests and BH correction across 16 primary
    comparisons; combined-family variants are secondary
- Results:
  - no favorable primary family survives correction or has a season interval
    below zero
  - QB room clarity is directionally strongest: -0.0126 RMSE versus separate
    QB and -0.0155 versus pooled full QB, with interval
    `[-0.0508, +0.0171]`
  - RB room clarity is flat; opportunity shares worsen QB/RB/WR mean PPG and
    improve TE by only 0.0036 with a wide interval
  - teammate ADP improves separate TE by 0.0042 but remains 0.0143 worse than
    pooled TE; WR experience improves separate WR by 0.0060 but remains 0.0293
    worse than pooled WR
  - exploratory same-evidence slices show -0.0973 rookie-QB RMSE for room
    clarity, -0.0342 rookie-WR RMSE for experience context, and -0.0277
    rookie-TE RMSE for teammate ADP
- Verification:
  - 20 position-family fits cover the exact 3,696-row OOF population
  - every assignment trains strictly before its target season
  - focused V2 tests and both study modules compile; production is unchanged
- Decision:
  - promote no point feature; retain the 31-feature incumbent
  - carry the young-player patterns only as whole-season and joint-template
    distribution hypotheses requiring independent confirmation
- Next:
  - finish whole-season finalist/calibration work, then test whether these
    fields improve template CRPS, coverage, upside, or missed-games behavior
    rather than conditional mean RMSE

## 2026-07-28 - Projection V2 KNN and Random Forest

- Request:
  - cover KNN and random forest before closing the conditional-PPG model-family
    search
- Changes:
  - added research-only KNN scaling support without changing the default M4A
    model surface
  - ran scaled KNN across a complete 12-setting neighbor/distance grid and
    random forest across an eight-setting 250-tree depth/leaf/feature grid
  - tested projection core and governed full features on the exact 3,696-row
    2017-2025 OOF population
  - prespecified fixed 50/50 averages with matching LightGBM predictions as
    untuned diversity diagnostics
- Results:
  - projection/full KNN score 3.2438/3.3194, worsening LightGBM by
    0.1111/0.1964 with intervals entirely above zero
  - projection/full RF score 3.1423/3.1242; full RF is effectively tied with
    full LightGBM and improves the recent mean season delta by 0.0094
  - projection RF/LightGBM average scores 3.1296, a -0.0031 point estimate
  - full RF/LightGBM average leads at 3.1143, a -0.0086 delta with five of nine
    season wins and interval `[-0.0182, +0.0005]`
  - the full blend improves QB/RB/WR by 0.0170/0.0061/0.0124 RMSE, worsens TE
    by only 0.0013, and improves limited-history players by 0.0204
- Verification:
  - all four fitted models cover the exact OOF population and every assignment
    trains strictly before the target season
  - 43 focused V2 tests pass and touched modules compile
  - production projections, manifests, templates, optimizers, and databases
    remain unchanged
- Decision:
  - reject KNN as a point model and ensemble component
  - carry full RF and the untuned RF/LightGBM average as unpromoted finalists
- Next:
  - fit RF and LightGBM through 2025, preserve their 2026 component forecasts,
    and test the equal blend against history routing and whole-season
    calibration/template guardrails

## 2026-07-28 - Projection V2 Linear and Tree Blends

- Request:
  - test whether the best linear model adds value when blended with LightGBM,
    random forest, or their average
- Changes:
  - refit governed full Lasso on the final provider-scoring feature lineage and
    exact 3,696-row OOF population used by the tree models
  - tested prespecified 50/50 Lasso/tree and equal-third
    Lasso/RF/LightGBM averages
  - fit causal convex blend weights at each origin using only earlier OOF
    seasons, with declared tree defaults before two prior seasons exist
- Results:
  - standalone full Lasso scores 3.1584 versus 3.1230 for LightGBM and 3.1242
    for RF
  - fixed Lasso/LightGBM scores 3.1094 and fixed Lasso/RF scores 3.1028
  - fixed full Lasso/RF/LightGBM equal thirds scores 3.1000 versus 3.1143 for
    the tree average, wins all nine seasons, and has interval
    `[-0.0225, -0.0075]`
  - causal full Lasso/tree-average scores 3.1027 with interval
    `[-0.0196, -0.0049]`; the learned Lasso share rises from 24.5% in 2019 to
    35.1% in 2025
  - the fixed full blend improves all four positions and second-year/veteran
    groups, but is 0.0128 worse than the tree average for rookies
  - projection-plus-active Lasso equal thirds has the lowest point estimate at
    3.0997 and better rookie behavior, but active inputs exist only in
    2024-2025 and its negligible edge was discovered on the same evidence
- Verification:
  - every component covers the same target rows and trains strictly before the
    target season
  - fixed blends require no fitted weights; every causal origin uses only
    earlier OOF seasons
  - production projections, templates, optimizers, manifests, and databases
    remain unchanged
- Decision:
  - advance fixed full Lasso/RF/LightGBM equal thirds as the primary simple
    blend finalist and the causal full Lasso/tree blend as a robustness
    finalist
  - retain projection-plus-active blending only as a sparse-history challenger
- Next:
  - fit and preserve all component predictions through 2025, publish 2026
    shadow forecasts, and run whole-season, calibration, and joint-template
    validation before promotion

## 2026-07-28 - Projection-Anchored History Gaps

- Request:
  - replace misleading pooled-median fills for unavailable historical PPG with
    a player-specific current-projection baseline
- Changes:
  - added a 13-feature `residual_history_gap_challenger_v1` manifest
  - expressed prior-year, prior-three-year, and career PPG as gaps from current
    active-game expert consensus, falling back to team-game consensus
  - assigned exactly zero gap to unavailable history and retained explicit
    availability, log opportunity games, neutral prior residual, and recency
  - added a secondary `games / (games + 8)` reliability-shrunk construction
  - refit incumbent/raw-gap/shrunken-gap Lasso, RF, and deterministic LightGBM
    on identical 3,696-row rolling OOF folds and recomputed fixed/causal blends
- Results:
  - all validation gap/reliability fields are complete; 631 no-career rows
    have exactly zero career adjustment
  - raw/shrunken Lasso score 3.1602/3.1611 versus 3.1585 incumbent
  - raw-gap LightGBM scores 3.1134 versus 3.1230, but wins four of nine seasons
    and is 0.0036 worse in mean 2023-2025 season RMSE
  - raw-gap equal thirds scores 3.0972 versus 3.1001, but the interval is
    `[-0.0091, +0.0035]` and recent mean delta is +0.0058
  - the raw equal-third gap model improves rookies by 0.0105 and non-rookie
    no-career players by 0.0422, but worsens prior 1-3 game, prior 4-7 game,
    and returning-after-gap groups
- Verification:
  - nine complete models each produce exactly 3,696 predictions
  - every fold assignment trains strictly before the target season
  - feature construction/modeling tests pass and production outputs are
    unchanged
- Decision:
  - use projection-anchored gaps as the governed sparse-history challenger
    representation, not pooled-median historical PPG
  - do not globally replace the 31-feature incumbent because the overall gain
    is small, temporally unstable, and absent for Lasso
- Next:
  - include a prespecified genuinely-no-history gap option in the final
    whole-season/history router and joint-template replay

## 2026-07-28 - Projection Trajectory and Logged ADP

- Request:
  - test this year's preseason projection versus the same player's prior-year
    projection, longer projection history, and logged ADP
- Changes:
  - added five preseason-only trajectory fields: exact one-year change,
    recency-weighted three-year change, exact-prior availability, prior
    projection count, and prior projection volatility
  - assigned neutral zero change plus explicit zero availability/count when a
    player has no prior projection; no realized PPG, games, or injuries enter
  - published trajectory and `log1p(ADP)` in separate research manifests
  - ran fold-identical incumbent, trajectory, logged-ADP, combined, and
    one-year/three-year ablation models across Lasso, RF, and LightGBM
- Results:
  - exact one-year trajectory alone improves the equal-third blend by 0.0016
    RMSE; its tree effects are neutral and recent mean is 0.0004 worse
  - three-year context improves the blend by 0.0041; the full five-field
    trajectory family improves it by 0.0051 to 3.0949 and is slightly better
    in 2023-2025
  - logged ADP improves Lasso by 0.0319 RMSE with eight of nine season wins,
    but is neutral for RF and slightly worse for LightGBM
  - the pooled-best combined blend scores 3.0930 versus 3.1001, but is 0.0025
    worse in the 2023-2025 mean and weakens missing-ADP and selected
    sparse-history slices
- Verification:
  - all models cover the exact 3,696-row 2017-2025 OOF population with
    fold/target/baseline lineage equal to the incumbent study
  - the rebuilt shadow mart contains 14,075 rows, 150 cataloged features, and
    separate five-feature trajectory and one-feature ADP-transform manifests
  - production projections, templates, optimizers, and source model-input
    database remain unchanged
- Decision:
  - retain last-year change alongside the stronger longer-history context;
    do not promote it alone
  - carry the raw-ADP full trajectory blend and model-specific logged-ADP
    Lasso into whole-season/calibration and joint-template validation

## 2026-07-28 - Projection V2 Team Environment and QB Style

- Request:
  - test calculable overall-team quality and separate QB passing from rushing
    context for RBs and pass catchers
- Changes:
  - added an 11-feature `residual_team_environment_challenger_v1` manifest
  - separated QB1 passing/rushing yards and TDs plus QB rushing fantasy-point
    share
  - capped skill context at top 2 RB, top 3 WR, and top TE; added raw/percentile
    team strength and self-excluded supporting-cast points
  - defined team rushing from the core plus QB1 and offensive TDs as QB1 pass
    TDs plus rushing TDs, avoiding duplicated receiving TDs
  - excluded projected targets because broad historical coverage begins only
    in 2024
  - fit six prespecified family variants across Lasso, RF, and LightGBM in
    resumable fresh-process batches on the exact trajectory-reference folds
- Results:
  - all new fields cover 98.0% of the 3,696 OOF rows
  - QB yardage is exactly flat, QB TDs worsen 0.0028, supporting cast worsens
    0.0020, team rushing/scoring is flat, and all 11 fields improve only 0.0003
  - QB rushing share is best at 3.0928 versus 3.0949, a 0.0021 gain and 0.0089
    recent gain, but wins five seasons and does not survive six-family
    correction
  - QB rushing share improves WR/TE by 0.0055/0.0068, is neutral for RB, and
    worsens QB by 0.0108
  - a same-evidence WR/TE-only route scores 3.0916, wins seven seasons, improves
    2023-2025 by 0.0085, and has interval `[-0.0070, +0.0002]`
- Verification:
  - 18 challenger models and three lineage-bridged references each contain all
    3,696 predictions
  - the rebuilt shadow mart contains 14,075 rows, 160 features, the 11-feature
    team-environment manifest, and 85 high-correlation audit pairs
  - focused feature tests pass; production projections/templates/optimizers
    remain unchanged
- Decision:
  - do not expand the global point model with broad team quality
  - carry only QB1 rushing share as a prespecified pass-catcher
    whole-season/template finalist; do not use it as an RB penalty yet

## 2026-07-29 - Projection V2 Locked Whole-Season and 2026 Shadow

- Request:
  - run the four pre-production lock, whole-season validation, 2026 shadow,
    and weekly-template handoff steps
- Changes:
  - added an exact versioned feature/model lock with hashes, compact grids,
    deterministic seeds, fixed equal-third blend weights, and prespecified
    secondary routes
  - replayed 2017-2025 as complete forecast seasons with all fitting,
    hyperparameter selection, routing, calibration, and residual intervals
    restricted to prior seasons
  - fit through 2025 and published 751 unique 2026 shadow predictions
  - published an explicit one-residual joint-template handoff and production
    name-key audit without changing production tables
  - fixed inferred-draft-year identity resolution so current Frank Gore Jr.
    evidence does not attach to retired Frank Gore, and reconciled redundant
    provisional Michael Thomas rows
- Results:
  - primary conditional PPG scores 3.0941 RMSE versus 3.1793 for expert
    recalibration, winning 9/9 seasons with mean-season delta -0.0847
  - no-history gap routing scores 3.0905 and wins 9/9, but its absolute gain is
    only 0.0036 RMSE
  - participation LightGBM scores 0.1215 Brier versus 0.1355 logistic
  - every tested strictly-prior point calibration worsens pooled RMSE
  - P25-P75/P10-P90 residual coverage is 0.497/0.793
  - joint donor residual reconstruction error is below 1.8e-15; an independent
    second residual would double variance
  - temporary name-key coverage is 99.07% for historical production templates
    and about 98.9% for current maps; name changes and one legitimate
    same-name collision prove the join is not production-safe
- Verification:
  - 51 V2 tests pass; all lock/study scripts compile
  - the corrected spine has 14,068 rows and the 2026 shadow has 751 distinct
    player keys, 720 conditional centers, and no missing participation scores
  - production projections, weekly templates, and optimizer inputs are
    unchanged
- Decision:
  - freeze the primary as a DK shadow candidate with the no-history gap route
    retained only as a locked secondary component
  - do not apply a point calibration overlay or post-hoc combine secondary
    routes
  - before production, propagate canonical player IDs through the weekly
    handoff and fit or validate a beta-scored center rather than reusing DK

## 2026-07-29 - Canonical Weekly Keys and Beta V2 Lock

- Request:
  - add canonical player keys to weekly templates and run the full beta V2 data
    and locked model lineage
- Changes:
  - added a governed alias/career-window resolver used by the weekly builder
    and a one-time backfill workflow
  - added required `player_key` and `player_key_match_method` columns to
    `Best_Ball_Weekly_Templates` and `Best_Ball_Weekly_Player_Map`
  - made generated app export fail closed on missing/null canonical keys
  - synchronized the source, auction-app, and Snake Simulation databases
  - created separate `Projection_V2_beta.sqlite3` data/model lineage and
    parameterized the locked replay, calibration, and template audit by
    scoring objective/output path
- Results:
  - all 10,596 historical beta+DK templates and all 448 current map rows have
    canonical keys; current map includes stable provisional rookie keys
  - DK and beta audits join 100% of their historical/current production rows
    by key with no unmatched rows
  - beta and DK share exactly 6,559 identities and 14,068 feature keys; beta
    historical PPG differs from DK by 1.170 MAE and provider points by 10.275
    MAE, confirming the need for a full separate rebuild
  - beta primary scores 2.9109 versus 2.9759 expert and wins 9/9 seasons
  - beta participation LightGBM scores 0.1216 Brier
  - every beta secondary-route interval crosses zero and every prior-only point
    calibration worsens pooled RMSE
  - beta target versus beta weekly-template active PPG differs by 0.240 MAE;
    donor residual reconstruction error remains below 1.8e-15
- Verification:
  - 54 V2 tests pass and all changed scripts compile
  - source/app/Snake tables have identical row counts, complete keys, and no
    duplicate canonical player-season rows within league
  - three historical converted-position labels remain explicit: Cordarrelle
    Patterson 2019/2021 and Ty Montgomery 2022; current positions agree 100%
- Decision:
  - lock the same primary model architecture for beta, fitted independently
  - promote no beta secondary route and no point calibration
  - keep DK and beta V2 databases separate and join any future production
    center by canonical key plus league

## 2026-07-29 - Projection V2 Following-Season Shadow

- Request:
  - build next-year predictions for use alongside weekly templates, using the
    residual versus the expert projection to keep the target simple
- Changes:
  - added a versioned `t+1` target that subtracts the origin-season expert
    team-game PPG consensus from following-season conditional PPG
  - separated following-season appearance probability from conditional PPG
  - imposed a one-origin outcome embargo on fitting and hyperparameter
    selection, so origin `t` uses labeled origins only through `t-2`
  - fit compact Lasso, random forest, and LightGBM residual models plus
    logistic/LightGBM appearance models for DK and beta
  - published canonical-key historical handoffs and 2027 shadow tables
  - replayed residual rank and appearance probability as incremental
    weekly-template match fields over 1,620 strict rolling targets
- Results:
  - DK blend RMSE is 3.9003 versus 5.2070 expert carry, with 8/8 origin wins;
    beta is 3.6718 versus 4.6685, also 8/8
  - appearance LightGBM Brier is 0.1604 DK and 0.1623 beta
  - each league publishes 751 2027 candidates, 720 conditional centers, and
    751 appearance probabilities with identical canonical key coverage
  - template residual rank improves weekly-PPG CRPS by only about 0.001-0.002;
    contribution CRPS is generally worse, including every tested beta addition
- Verification:
  - strict target reconstruction, no-appearance/null-PPG, unresolved-identity,
    incomplete-target, duplicate-key, training-cutoff, probability-bound, and
    league/key checks pass
  - full V2 tests pass; new scripts compile
  - production projections, weekly templates, Simulation tables, and
    optimizers are unchanged
- Decision:
  - initially retain the next-year outputs as a two-part shadow forecast
  - do not add next-year fields to weekly template matching
  - require an explicit appearance/availability mixture before production
    optimizer use; the production handoff below supersedes shadow status after
    that mixture was implemented and tested

## 2026-07-29 - Projection V2 Production Handoff

- Published the locked current and following-season forecasts into
  `Final_Predictions_Resid` for 268 DK and 180 beta players using canonical
  league/key joins with fail-closed coverage and scoring checks.
- Set current residual quantiles to zero. Auction and Snake now use one matched
  donor's centered active-PPG residual together with that donor's weekly path;
  no independent current residual is sampled or used for scaling.
- Rebuilt 10,596 historical templates and 448 current player-map rows, then
  synchronized the source, auction, and Snake databases.
- Rejected historical V2 donor recentering after it worsened PPG CRPS by
  0.0057 DK and 0.0051 beta; both player-cluster 95% intervals were above zero.
  Production retains validated legacy OOS centers and stores V2 centers only
  as diagnostics with `v2_recenter_promoted = 0`.
- Auction keeper value now draws conditional next-year PPG/residuals and then
  applies `pred_appear_ny`; no appearance sets future market value to zero.
- Verification:
  - final-policy template replay exactly matches the validated baseline for all
    1,620 rows in each league
  - 60 V2 tests, 38 auction tests, and 3 Snake V2 tests pass
  - live auction and Snake weekly/optimizer smoke paths complete
  - source/app/Snake handoff, template, and player-map invariants match
- Created recoverable pre-cutover database backups:
  `Data/Databases/Simulation_pre_v2_production_20260729.sqlite3` and
  `Fantasy_Football_App/app/Simulation_pre_v2_production_20260729.sqlite3`.

## 2026-07-29 - Weekly-Template Projection Weight Sensitivity

- Replayed eight fixed projection-weight specifications on the same 1,620
  strict rolling player-season targets in DK and beta.
- Tested 50%/100% absolute-PPG increases, isolated component-rank and raw
  component magnitudes, and moderate/aggressive combined variants.
- Every global PPG/raw-component bump worsened pooled PPG, contribution, and
  played-games CRPS in both leagues; recent-period results also worsened.
- Component ranks alone were approximately neutral globally. Raising only QB
  rushing/passing component ranks was favorable across all three metrics and
  both leagues, but clustered intervals crossed zero and DK PPG won only 4/9
  seasons.
- Decision: retain production weights and keep the QB-only component-rank bump
  as an exploratory future challenger.

## 2026-07-29 - Projection V2 Identity, Source-Season, and Beta Scoring Correction

- Request:
  - fix Tetairoa McMillan and Amon-Ra St. Brown identity splits, remove the
    extra truncated identity, include sacks in beta scoring, audit other major
    scoring omissions, and confirm whether actual PPG includes yardage bonuses
- Changes:
  - added a narrow governed alias ledger and stable Tet production-key
    migration; expanded reviewed truncations to Equanimeous St. Brown and
    recent provider name variants
  - made direct draft/entry contradictions and pre-entry seasons hard identity
    filters while using `last_season` only as a namesake tie-breaker, so
    returning players such as Zack Kuntz and Michael Gallup remain confirmed
  - governed FantasyPros WR stored 2016 as effective 2018 and stored 2020 as
    effective 2021, with override ID/reason/archive provenance and native-row
    collision rejection in both identity and value loaders
  - versioned provider points as
    `core_offensive_season_components_v1`; required sacks for beta QB rows,
    constrained donors to the same player/season/position, allowed the sole
    one-donor exception for beta sacks, and persisted component/donor lineage
  - made the production handoff refresh-safe when the prior production slice
    already contains canonical key and V2 metadata columns
- Results:
  - each league now has 6,655 identities, 55,964 aliases, 52,526 source
    observations, 9,934 outcomes, 31,848 projection values, 31,834 market
    values, and 13,909 feature rows with exact DK/beta identity parity
  - all 43 Tet aliases resolve to confirmed key
    `c16a5e67-fff0-57b9-838c-c8df91df7b9d`; his 2025 outcome is present and
    `has_prior_outcome=1` in 2026
  - all six `Amon Ra St`, five `Equanimeous St`, and three `Brown St` aliases
    resolve to their confirmed identities; obsolete provisional keys are absent
  - exactly 253 FantasyPros WR rows move 2016->2018 and 263 move 2020->2021
    with complete provenance; no affected row remains effective in 2016/2020
  - beta contains 4,993 QB-provider rows: 3,326 complete, 2,180 sack-imputed
    (1,588 one-donor and 592 two-donor), and 1,667 explicitly insufficient;
    zero complete beta QB rows have null sacks
  - corrected locked RMSE is 3.1092 DK versus 3.1939 expert and 2.8993 beta
    versus 2.9765 expert, both 9/9 season wins
  - corrected next-year RMSE is 3.9118 DK versus 5.2297 expert carry and
    3.5902 beta versus 4.4274, both 8/8 origin wins
- Actual scoring answer:
  - yes, actual conditional PPG includes configured weekly pass 300/400,
    rush 100/200, and receiving 100/200 bonuses, plus sacks suffered and lost
    fumbles; the denominator is qualifying offensive opportunity games, not
    conventional games active
  - provider season-total projections intentionally exclude weekly bonuses
    because threshold-game counts are not identifiable from season yards
  - projected fumbles remain deferred for source-quality work; actual
    two-point and special-teams fields remain zero until explicit league
    coefficients are confirmed
- Verification:
  - 108 V2 tests pass; touched modules compile; `git diff --check` passes
  - both V2 and all three Simulation databases pass SQLite integrity and
    foreign-key checks; no orphan or duplicate identity/value/production keys
  - calibration retains the unadjusted primary centers; template audits join
    100% of 268 DK and 180 beta production rows
  - a copied production publish succeeds twice, with the second run producing
    zero point deltas; all 11 generated app tables match source and the Snake
    database is byte-identical
- Decision:
  - accept the corrected lineage and supersede the earlier 2026-07-29 lock and
    next-year metrics; retain the existing primary architectures and joint
    weekly-path uncertainty contract
  - retain fringe no-sack-donor beta rows as missing instead of silently
    scoring sacks at zero; current ADP-at-or-before-200 QB coverage is complete
  - durable evidence lives in
    `research/studies/2026-07-29_v2_identity_scoring_revalidation/`

## 2026-07-29 - QB Passing/Rushing Target Decomposition

- Confirmed the prior V2 tests used passing/rushing projection levels and
  shares as features but never fit separate passing- and rushing-point targets.
- Built a research-only strict rolling comparison on the exact locked QB
  populations: 380 DK and 343 beta player-seasons across 2017-2025.
- Fit identical compact Lasso/RF/LightGBM grids to total conditional PPG and
  independently to realized passing and rushing PPG. Reported both the literal
  component sum and a strictly-prior mean adjustment for receiving, fumble,
  two-point, and special-teams points.
- Results:
  - DK component-plus-other RMSE is 3.4996 versus 3.4581 for the same-model
    direct-total blend, a +0.0414 delta with interval
    `[-0.0442, +0.1265]`
  - beta component-plus-other RMSE is 3.9706 versus 3.9976, a -0.0270 delta
    with interval `[-0.1261, +0.0630]`
  - the component route wins 4/9 DK and 5/9 beta seasons and worsens the
    2023-2025 window in both leagues
  - rookie-QB and beta high-rush slices are directionally favorable but were
    diagnosed after the aggregate test
- Decision: keep total conditional PPG as the primary QB target. Preserve
  component forecasts as diagnostics or for a future prespecified router;
  production models, projections, templates, and apps are unchanged.

## 2026-07-29 - Weekly League Scoring and FFToday Vintage Correction

- Review finding:
  - the beta weekly template slice was labeled beta but the weekly position
    scorers obtained the default DK scoring dictionary
  - the 50 FFToday QB rows stored under 2018 match the provider's official 2019
    projection archive; native 2019 rows already exist, so those rows leaked a
    future projection vintage and beta sack evidence into 2018
- Changes:
  - passed explicit league through QB/RB/WR/TE weekly scoring, added a transient
    `scoring_league` marker, and made template construction reject mixed or
    mismatched markers before setting league and template-ID offset
  - added `--league`, `--simulation-db`, `--v2-db`, and `--no-app-sync`; staged
    Simulation builds require a staged V2 database, disable app sync, and
    reject a locked scoring objective for the wrong league
  - added governed quarantine
    `fftoday_qb_stored_2018_2019_vintage_quarantine_v1` before identity,
    effective-season, candidate, value, feature, and template-key paths
  - retained the raw 50 stored-2018 rows and all 50 native 2019 provider rows;
    no invalid row is relabeled, deleted, used as a beta sack donor, or filled
    with a DK/zero-sack assumption
  - added exclusion ID/reason/reference and counts to feature-source audits,
    a Milestone 3 quarantine receipt, and a policy-hash receipt that
    `--reuse-foundation` must match
- Staged results:
  - DK/beta remain at 6,655 identities and 13,909 feature rows each; aliases
    fall to 55,914, source observations to 52,476, and normalized projection
    values to 31,798 after the exact 50-row quarantine
  - locked current RMSE is 3.1078 DK versus 3.1951 expert and 2.8845 beta
    versus 2.9600, both 9/9 season wins
  - beta locked validation falls from 3,659 to 3,622 rows; the 37 removed rows
    are 2018 QBs whose only apparent sack donor was the leaked FFToday vintage
  - next-year RMSE is 3.9137 DK versus 5.2351 expert carry and 3.5538 beta
    versus 4.3653, both 8/8 origin wins
  - every tested prior-only calibration overlay remains worse; no secondary
    route or calibration policy is promoted
  - both weekly slices rebuild to 5,298 templates; 5,120 paired `active_ppg`
    values differ with mean absolute delta 1.3508, and 5,147 normalized weekly
    paths differ with mean L1 delta 1.1407
  - Amon-Ra St. Brown's 2024 beta line is 256.7 points/17.1133 PPG versus
    302.2/20.1467 DK; Josh Allen's 14 sacks cost exactly 14 beta points
  - beta V2 diagnostic centers cover 2,657/2,696 historical rows; the 39
    unavailable 2018 QB diagnostics retain the validated legacy active center
    only with a joined unavailable marker and current quarantine proof
  - the corrected 1,620-target/league projection-weight replay retains current
    weights: DK's 2.25 PPG weight worsens PPG CRPS by 0.003261 with interval
    above zero; beta's 0.000351 PPG improvement is uncertain and comes with a
    0.002868 played-games CRPS loss whose interval is above zero
  - the corrected next-year template replay also keeps those fields outside
    matching: DK's 0.002006 PPG-CRPS gain comes with a 0.031825 contribution
    loss and both intervals exclude zero; beta's PPG/contribution changes are
    both uncertain
  - 118 V2 tests and 22 weekly modeling tests pass; touched modules compile
- Scoring interpretation:
  - realized weekly `active_ppg` and template upside include configured
    yardage bonuses because each player-week is scored before aggregation
  - provider season-total projections still exclude weekly bonuses because
    season yardage cannot identify weekly threshold crossings
- Publication:
  - a second staged handoff produced exactly zero current/next-year point
    deltas; the staged DK V2, beta V2, and Simulation databases were then
    copied byte-for-byte to production
  - all five live/app databases pass SQLite integrity with zero foreign-key
    errors; all 11 managed-auction generated tables match source while the
    app-owned non-generated-content hash remains unchanged
  - the Snake database SHA256 exactly matches source Simulation; do not broaden
    the narrowly proven beta 2018 QB fallback
- Durable evidence:
  - `research/studies/2026-07-29_v2_weekly_fftoday_correction/`
