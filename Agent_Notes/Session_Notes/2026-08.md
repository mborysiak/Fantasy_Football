# August 2026 Session Notes

## 2026-08-14 - Refreshed NV Production Stage

- Completed governed stage
  `20260814T153232Z_d5feb5a5` (run `20260814T153233Z_2a4365dd`) after the fresh
  Notebook 1 inputs. All 30 registered steps, release validation, Auction
  candidate preparation, Snake candidate preparation, and both app smoke tests
  are complete. The release was explicitly promoted at
  `2026-08-14T17:31:42Z`.
- Final current populations are 344 DK, 381 NFFC, 326 beta, and 326 NV. Every
  weekly map has exact projection-key parity, exactly 80 donors per player, and
  probability sums within floating tolerance of one. Template audits reported
  zero unmatched production rows.
- The full NV salary stage completed on its governed second attempt after one
  classified Windows/XGBoost heap-corruption exit. It wrote 326 unique
  `nvpred` rows with zero missing values and exact 326/326 projection parity,
  saved 680 salary validation-residual rows and 1,218 backtest rows, retained
  zero NV keepers, and normalized the top 156 offensive slots to exactly
  `$3,576` (12 times `$298`). Beta retained 14 keepers and 326 salary rows; its
  beta-only premium published 312 unique non-keeper rows from 1,000/1,000
  successful trials.
- Native isolation also recovered one DK mart access violation, one beta locked
  LightGBM access violation, and two independent NV locked LightGBM batch
  violations. Every recovered step restored its pristine input before retrying.
- The first app-parity passes exposed transient Windows SQLite row-read
  corruption because the digest helper's connection context committed but did
  not explicitly close. A scoped resume using the identical digest with
  deterministic connection closure passed exact Auction generated-table and
  full Snake parity. Auction smoke exercised beta and NV with zero
  errors/exceptions; Snake smoke exercised DK and NFFC with zero
  errors/exceptions.
- The live baseline correctly lacked NV, so the first-introduction release
  report was generated with a scoped baseline-only NV-empty allowance. It
  records NV as 0 old / 326 new / 326 added and remains hash-bound to the
  prior live and staged Simulation files. All 11 installed artifacts match the
  staged SHA-256 receipts, SQLite quick checks pass with zero foreign-key
  violations, and no cleanup warnings were recorded. Durable pre-refresh
  backups and the archived release report are under
  `Data/Production_Refresh_Backups/20260814T153233Z_2a4365dd/`. Harden digest
  connection closure before the next new refresh.

## 2026-08-13 - NV Pipeline and Auction App Registration

- Registered NV as the fourth 2026 production league with an independent
  `Projection_V2_nv.sqlite3`, locked current and next-year model versions,
  handoff eligibility, 16-week weekly build/audit, and release lineage. NV uses
  the core population plus top-180 ETR overall ranks and any NV keepers, with
  production floors of 300 total and QB/RB/WR/TE 40/75/105/38.
- Confirmed the scoring contract matches beta except for passing touchdowns:
  beta is 5 points and NV is 4. Both retain the same sack rule, including the
  governed one-donor historical sack imputation and donor-ineligible 2018 QB
  quarantine. NV weekly matching uses exact
  `v2_nv_scoring_matched_preseason` context and
  `nv_scored_expert_consensus` for context-available historical centers.
- Added fixed-200 governance for `salaries_2026_nv.csv` and parameterized the
  v6 salary build by `FF_AUCTION_LEAGUE` and an explicit league V2 database.
  Refresh now writes and validates separate `betapred` and `nvpred` slices with
  exact projection/weekly/salary key parity. The absent NV keeper file is an
  explicit 2026 empty-slice exception; adding the file later requires no code
  change. Selection premium remains beta-only and is neutral for NV.
- Bumped the refresh manifest to schema 6 and expanded it to 30 steps, adding
  NV foundation/lock/next/weekly/audit stages, an NV V2 promotion artifact,
  both auction salary/keeper runs, four-league release reporting, and an
  Auction smoke requirement for beta plus NV. The dry run completed and printed
  the full registered contract without live writes.
- Corrected the Auction defaults to NV 2 QB/2 RB/2 WR/1 TE/1 FLEX, five bench
  slots, 13 offensive players, and a `$298` cap. The managed scorer now exposes
  two synthetic QB replacements so the two-QB lineup passes its own feasibility
  gate. Beta remains 1 QB/2 RB/2 WR/1 TE/2 FLEX with the same roster/cap.
- Verification: 226 focused V2/weekly/salary/orchestration tests pass, all 69
  Auction app tests pass, affected modules compile, the schema-6 production dry
  run passes, and both repositories pass `git diff --check`. No full staged
  model build or promotion was run; the live databases remain pre-NV.

## 2026-08-08 - Pre-Promotion Prediction and Template Change Report

- Added a saved live-versus-candidate release report after the complete staged
  pipeline and before any promotion write. It prints population additions and
  drops, the top ten published PPG increases/decreases, and the top ten
  positive/negative weighted-template residuals with their prior values and
  deltas. The residual uses the apps' exact donor expectation:
  `sum(template_sample_prob * active_ppg_resid)` after the governed
  probabilities are verified to sum to one.
- The JSON/Markdown artifacts are hash-bound to both Simulation databases,
  recorded in the manifest, reprinted before promotion, and copied beside the
  durable `*.pre_refresh.sqlite3` rollback files. Missing/stale reports,
  projection/template population disagreement, non-finite values, duplicate
  keys/donors, a donor count other than 80, or probability drift fail closed.
- Generated a retrospective copy for promoted release
  `20260808T171956Z_710ed569`. It records 9 added and 12 dropped league/player
  rows (DK 348 to 345; NFFC 382 to 382; beta 326 to 326), including Pearsall's
  reviewed DK/NFFC drops. The largest projection movement is Stefon Diggs DK
  at +1.319 PPG; the largest decrease is Antonio Williams DK at -1.192 PPG.
  Durable files are `release_change_report.json` and
  `release_change_report.md` in that release backup directory.
- Verification passes 65 focused report/orchestrator tests and all 292 V2
  tests; the only suite warning is the existing `pkg_resources` namespace
  deprecation. Both changed modules compile and `git diff --check` is clean.

## 2026-08-08 - Refreshed Projection Candidate and Injury Governance

- The first governed rebuild caught an incomplete FFToday scrape before
  release: the stored 2026 slice had QB/RB/WR/TE counts 50/95/50/0 even though
  the live provider pages still exposed the full tables. Restored the source
  atomically to 50/95/131/50 (326 rows). Notebook 1 now retries each position,
  locates the projection table by shape instead of a fixed page index, rejects
  any position below 40/80/100/40, verifies the exact post-write position
  counts, and prints a save confirmation only after commit.
- Ricky Pearsall has no current or next V2 projection center while recent ADP
  still places him at 176.70 DK and 117.61 NFFC. Per the season-ending PCL
  injury update, registered an explicit 2026 DK/NFFC exclusion with the reason
  `season_ending_pcl_injury_adp_lag_without_current_projection_center`; no
  legacy or fabricated projection is substituted. NFFC now reviews the first
  363 offensive ADP candidates so Pearsall plus unprojected market-only Khalil
  Herbert and Haynes King can be excluded without truncating the 360-pick
  draft surface. Brandon Aiyuk is not excluded: after the FFToday repair he is
  a complete core row in all three leagues.
- Fresh non-promoting stage `20260808T171956Z_9604d0b4` completed all 25
  governed steps. The 13,880-row, 160-feature mart produced 345 DK, 382 NFFC,
  and 326 beta handoffs with zero selected incomplete rows; weekly maps match
  those populations with 80 donors/player and horizons 16/17/16. Salary
  publication has 326 predictions, and 312 selection premiums came from
  1,000/1,000 successful trials with an expected roster reserve of `$8.20`.
- One beta foundation access violation and one beta weekly heap-corruption
  exit were classified and recovered on their governed second attempts. The
  first Auction generated-table parity check transiently named
  `Best_Ball_Weekly_Templates`; direct schema, 12,105-row multiset, and digest
  comparison were identical, and an unchanged resume passed Auction parity,
  full Snake parity, both size/integrity gates, and both app smokes with zero
  errors/exceptions.
- Explicit promotion completed at `2026-08-08T19:20:34Z`. All ten installed
  model/app hashes match the staged manifest, SQLite quick/foreign-key checks
  pass, and all ten durable pre-refresh backups exist under
  `Data/Production_Refresh_Backups/20260808T171956Z_710ed569/`. Windows retained
  one temporary Model Inputs rollback duplicate after installation; its durable
  backup was already verified, and the exact temporary file was removed after
  the promotion process released it. Focused FantasyPoints/handoff tests pass
  40/40, and the changed ingestion/handoff modules compile.

## 2026-08-08 - FantasyPoints Projection Export Schema

- Updated notebook 1 for the new literal FantasyPoints filename
  `projections.season.csv` and its grouped-row/field-row header layout. Added a
  validated normalizer that maps `RANK`, `NAME`, `Position`, `GP`, and the
  repeated passing/rushing/receiving columns to the unchanged
  `FantasyPoints_Projections` table contract while retaining legacy-schema
  normalization for regression checks.
- The archived new file normalizes to 575 unique rows (72 QB, 145 RB, 217 WR,
  141 TE) and its 21 output columns exactly match the live SQLite table. It has
  zero missing/non-numeric required values and zero duplicate cleaned
  player-position keys. The prior archive has 550 rows; 545 cleaned keys
  overlap, with 30 new and five removed provider rows.
- Added fail-closed schema, numeric, rank/ADP, uniqueness, total-depth, and
  position-depth checks plus focused current/legacy/malformed/partial tests.

## 2026-08-08 - Notebook ADP Save Confirmations

- Added explicit post-commit confirmations to
  `Scripts/Data_Generation/1_Update_Projections.py` for FantasyPros redraft,
  NFFC, and DraftKings ADP refreshes. Each message is emitted only after its
  validated atomic replacement returns and reports saved row counts; the
  provider messages also summarize position or governed-feed counts.

## 2026-08-04 - Canonical ADP Governance and Promoted Refresh

- Added policy-hashed, family-level ADP governance. MFL stops after 2024;
  recent consensus uses FantasyPros redraft, FantasyPros best-ball `AVG`,
  direct DK, and one NFFC family vote. Raw provider/component rows remain
  available for audit and challengers without receiving extra model votes.
- Simplified NFFC manual ingest to `ADP.tsv` = Best Ball Overall and
  `ADP (1).tsv` = Best Ball $25/$50. The notebook archives semantic filenames,
  validates both files before an atomic replacement, retains older
  Rotowire/Cutline raw history, and rebuilds 2025+ aggregates. Current pair
  coverage is 505/482 rows; the common top-240 set has 248 players, Spearman
  0.987859, and median absolute ADP gap 4.45. The aggregate has 482 paired and
  23 single-feed 2026 players; single-feed `feed_gap` is null rather than zero.
- DraftKings retains its direct center and now derives labeled synthetic
  bounds/SD from the NFFC pair. Canonical Model Inputs use an observed-family
  median and source count without row-imputing missing providers or exposing
  correlated FantasyPros component columns as separate model features.
- Beta weekly-template matching now prefers V2 canonical ADP, then canonical
  Model Inputs, with ETR only as last resort. ETR still governs beta eligibility.
  Updated the model-side best-ball contract and the Snake app contract/runbook.
- The handoff fail-closed review removed stale annual exclusions and added four
  current NFFC-only market players without valid projection centers: Kenny
  McIntosh, Jalin Hyatt, Jaydn Ott, and J'Mari Taylor. No projection was
  fabricated. Taysom Hill remains a reviewed NFFC exclusion but is no longer
  a DK exclusion because he falls outside the top-280 DK eligibility set.
- Fresh non-promoting stage `20260804T193416Z_4136d153` completed every
  governed step. Final populations are 348 DK, 382 NFFC, and 326 beta; all
  weekly maps have 80 donors/player and complete identity/center coverage.
  The next-season primary model beat expert carry-forward in all eight held-out
  origins for every scoring system (pooled RMSE DK 3.9106 vs 5.2351, NFFC
  3.9706 vs 5.5962, beta 3.5571 vs 4.3653). Salary and 1,000/1,000 reserve
  trials completed; consolidated validation, Auction generated-table parity,
  Snake full-copy parity, size/integrity gates, and both app smokes passed.
  A transient Auction table-copy digest mismatch disappeared under direct
  schema/row/SQL-EXCEPT/digest comparison and passed on unchanged resume.
- Verification: affected modules compile; focused ADP/handoff tests pass
  40/40; complete V2/Data Generation/best-ball scoring suites pass 345 tests
  with 12 existing deprecation/future warnings.
- Explicit promotion completed at `2026-08-04T20:27:28Z`. All ten live model,
  Simulation, Auction, and Snake artifacts matched the staged manifest SHA-256
  digests at installation; all ten durable pre-refresh backups exist under
  `Data/Production_Refresh_Backups/20260804T193416Z_b6992d72/`; and there were
  no rollback or cleanup warnings. The repository Simulation physical digest
  later changed only through the separately receipted vacuum below.
- Added a governed `compact_simulation` step after all Simulation writers and
  before validation/app preparation. It records integrity, foreign-key, file,
  page, and reclaimed-byte receipts and requires `freelist_count=0`. The
  already-promoted repository Simulation database was backed up and compacted
  from 114,450,432 to 62,353,408 bytes (52,097,024 reclaimed); all 26 table
  digests and the table inventory match the pre-vacuum copy. Focused refresh
  verification passes 61 tests.

## 2026-08-04 - Logged Expert-Rank Disagreement Test

- Froze a common-scale disagreement feature before testing: each published
  source rank is converted to a season-position percentile, mapped to the V2
  feature universe's common position depth, transformed with `log1p`, and
  summarized by MAD. Players with fewer than two sources retain null
  disagreement. Consensus rank level, observed source count, and publication
  coverage remain explicit controls.
- Replayed the locked Lasso/RF/LightGBM point models at every 2017-2025 origin
  in DK and beta. The primary full-column-forest comparison is incremental to
  normalized rank level. Logged disagreement worsens pooled RMSE by 0.008028
  DK and 0.003259 beta, recent RMSE by 0.043602/0.032392, and wins only 4/9
  seasons in each league. Only RB improves; QB/WR/TE worsen in both. The
  production-surface deltas are also unfavorable at +0.007206/+0.001337.
- Tested uncertainty separately with the identical rank-level point center and
  a fixed Ridge trained only on earlier OOF seasons to predict absolute-error
  scale. Logged disagreement worsens Gaussian CRPS by 0.001247 DK (+0.073%)
  and 0.000407 beta (+0.026%). Spearman correlation with absolute point error
  is 0.027 DK and -0.009 beta. A strictly-prior excess-over-expected-MAD
  sensitivity does not rescue the point or scale result.
- Both cross-league advancement decisions fail. Keep normalized rank level as
  the unpromoted challenger and reject broad logged disagreement for modeling
  and residual/template uncertainty. Treat the repeated RB-only point gain as
  post-hoc; it needs a separately frozen football rationale and new test before
  any follow-up.
- The study used read-only SQLite connections and matching database hashes
  before/after. No production feature, lock, template, or table changed.
  Focused feature/causality tests pass 4/4. Durable study:
  `research/studies/2026-08-04_v2_logged_rank_disagreement/`.

## 2026-08-04 - Logged Expert-Rank Level Confirmation

- Replayed a frozen direct comparison on the current promoted lineages between
  the normalized within-source season-position percentile median and
  `log1p(median raw overall expert rank)`. Both representations use identical
  scoring-specific provider rows and have zero player-season missingness
  mismatches.
- DK favors normalized rank: raw log is +0.000764 controlled RMSE with 5/9
  wins, only QB/TE nonworse, and intervals crossing zero; the production
  surface is +0.001744 with only 2/9 wins and recent worsening.
- Beta favors raw log descriptively: -0.001618 controlled and -0.002490
  production RMSE, with all four positions favorable. It still wins only 5/9
  seasons, and its controlled season/player intervals cross zero.
- The scoring systems disagree and raw log fails the cross-league gates.
  Retain normalized rank as the unpromoted challenger because its semantics
  are robust to provider depth and overall QB placement. No production model,
  template, or database changed; database hashes matched before and after.
  Durable study:
  `research/studies/2026-08-04_v2_log_expert_rank_confirmation/`.

## 2026-08-04 - Normalized Expert-Rank Promotion Decision

- Added only the scoring-specific within-source season-position percentile
  median, then reselected the complete locked Lasso, RandomForest, and
  LightGBM grids at every 2017-2026 forecast origin using strictly prior whole
  seasons. Production databases remained read-only with matching hashes.
- DK improves pooled/recent RMSE by 0.001813/0.003699 and player CRPS by
  0.001120, but its player-cluster interval is
  `[-0.004521, +0.000873]`; the recent aggregate gain comes entirely from
  2025 after 2023 and 2024 worsen.
- Beta worsens pooled/recent RMSE by 0.000077/0.000039, worsens player CRPS by
  0.000425, and worsens QB RMSE by 0.012799. Its player-cluster interval also
  crosses zero.
- Only 3/6 recent league-season cells improve. Nested reselection changes 8/30
  DK and 13/30 beta component-origin selections, so the earlier fixed-model
  attribution result does not replicate consistently on the final selection
  surface.
- Stage A fails. The frozen protocol therefore does not authorize Stage B
  template or roster transport; no production model, cache, template, table,
  or app artifact changed. Durable study:
  `research/studies/2026-08-04_v2_normalized_rank_promotion/`.

## 2026-08-04 - Local Checkout Migration Cleanup

- Updated the repo-local Claude permissions from the retired OneDrive checkout
  to `C:/Users/borys/GitHub`, including the Auction and Snake sibling repos.
- Re-registered the user-level `fantasy-football-312` Jupyter kernel against
  `C:/Users/borys/GitHub/Fantasy_Football/.venv_ff_312/Scripts/python.exe`.
  A kernel smoke check reported both that executable and the migrated repo as
  its working directory. No registered user kernel retains an OneDrive path.
- Pinned the VS Code workspace interpreter to that same `.venv_ff_312`
  executable. The first attempted local interactive run had attached to global
  Python 3.12 instead, where the editable sibling `ff` package was unavailable.
- Reconciled an accidental morning run of `1_Update_Projections.py` from the
  OneDrive checkout. Copied 11 canonical raw exports into the local checkout:
  three ETR files, FantasyPros ADP and best-ball ADP, FFA projections, four
  NFFC feeds, and PFF projections. Every copied hash matches its OneDrive
  source. The newer-timestamp FFA raw-stats file was byte-identical and was not
  copied; `-MarkPC`, cache, Optuna, research-conflict, code, and database files
  were not copied.
- The accidental OneDrive `Season_Stats_New.sqlite3` was deliberately not
  promoted. Release `20260804T131926Z_57a25fb4` used local source hash
  `073f2700...`; the accidental old-checkout database is `0a1a89cc...`.
  Although its SQLite `quick_check` passes, the stale OneDrive script left
  only 40 current FantasyPros projection rows versus 519 locally.
  Re-run the projection ingest locally from the recovered raw files before the
  next governed production refresh.
- Left archived code, comments, and research provenance unchanged. They are
  historical references rather than active migration configuration.

## 2026-08-04 - Post-Migration Projection Ingest Check

- The local ingest updated `Season_Stats_New.sqlite3` at 13:06 from the
  intended `.venv_ff_312` kernel. SQLite `quick_check` is `ok`, foreign-key
  violations are zero, all 39 prior tables remain, and no schema changed.
- Eleven current-year source slices changed relative to the immutable
  pre-refresh snapshot. FantasyPros projections contain 521 rows
  (QB/RB/WR/TE = 82/131/189/119) with no duplicate player-position keys;
  provider-scoped `ADP_Ranks` contains 886 rows with no duplicate keys.
- The governed source-market check fails only
  `nffc_rotowire_online`: the refreshed raw `2026ADP.tsv` and stored slice both
  contain 355 rows versus the locked 400-row floor. Follow-up confirmed this is
  the complete 20-round export: ranks are contiguous and unique, max pick is
  240, and all 121 players absent versus the prior 474-row/360-pick file had
  prior ADP above 244. The other NFFC feeds pass at 505, 482, and 325 rows
  against 400/400/250 floors; DK, total NFFC, and ETR pass at 421/300,
  1,667/360, and 242/180.
- Updated the annual contract: Rotowire Online now requires at least 300 rows
  and exact `MAX(max_pick)=240`; Best Ball Overall, Best Ball $25/$50, and
  Cutline retain 400/400/250 floors and require exact 360/360/260 pick
  boundaries. The current database passes the governed validator with source
  counts 355/505/482/325 and boundaries 240/360/360/260. Focused cycle/refresh
  tests pass `66 passed`; the full V2 suite passes `279 passed` with one
  existing `pkg_resources` deprecation warning. Use a fresh stage because the
  contract hash changed.

## 2026-08-04 - Full Staged Refresh After ESPN Salary Source Update

- Replaced the active 2026 beta ESPN salary export's fixed 200-row gate with a
  variable-length terminal-`$0` boundary. Frozen 2025 beta/nv exports remain
  exact at 200/160. The current file parsed 180 markers into 180 unique rows,
  ended at `$0`, and retained its exact manifest hash throughout the run.
- Focused verification passed: salary parser/transaction suite `38 passed`,
  refresh-orchestrator suite `59 passed`, syntax compilation, and dry run.
- Fresh schema-5 stage `20260804T131926Z_57a25fb4` at
  `C:/Users/borys/AppData/Local/Temp/fantasy-football-production-refresh/20260804T131925Z_10a06ae7`
  completed all 24 steps on the first attempt with no retries. It produced 350
  DK, 383 NFFC, and 328 beta players, 80 donors/player, league horizons
  16/17/16, 328 keyed salary estimates, and 314 selection premiums from
  1,000/1,000 successful trials.
- The projection-defined salary population stayed complete despite the shorter
  ESPN source: 189 players sit at the `$1` output floor and the top 142
  non-keepers normalize exactly to `$3,071`. Versus live, 128 salary values
  changed with `$0.24` mean and `$3.94` maximum absolute movement; the expected
  roster reserve moved from `$8.81` to `$9.21`.
- Current/next PPG and production player-key sets were identical to the prior
  live release for all three leagues. Consolidated database integrity,
  weekly/ADP gates, app content parity, 100 MiB size gates, and Auction plus
  DK/NFFC Snake smokes passed.
- Explicit promotion installed all ten model/app artifacts with durable backups
  under `Data/Production_Refresh_Backups/20260804T131926Z_57a25fb4/`. Every live
  SHA-256 and size matches the receipt, and all ten post-promotion SQLite
  integrity and foreign-key checks pass.

## 2026-08-03 - NFFC Draft-Slot Preference

- Ran a corrected ex-ante comparison of all 12 positions for the Snake app's
  NFFC offense-only 3RR adapter. Every room removes the `slot - 1` players
  drafted before the user's first turn, then follows the stack-aware Sequential
  rollout policy for all 20 user selections.
- All 3,072 drafts completed legally across 256 matched noisy-ADP rooms per
  slot. Completed rosters were scored on 512 common held-out 17-week seasons.
- The current point-estimate preference is
  `3,4,2,5,6,7,1,8,11,9,10,12`. Slot 3 led at 2,355.16 points; slot 4 was only
  0.79 lower with a paired interval spanning zero. Slot 2 remained a plausible
  top-tier alternative. Slot 1 ranked seventh, and slots 8-12 formed the
  weakest broad tier.
- Rejected and overwrote an initial invalid diagnostic that forced the first
  candidate before simulating earlier opponent picks, which had incorrectly
  left elite players available at late slots.
- Treat the order as a current projection/ADP structural preference, not
  historical ROI, championship-win evidence, or full official 30-round NFFC
  support. Re-run after material input updates. Durable study:
  `research/studies/2026-08-03_nffc_draft_slot_order/`.
- Gibbs-tier sensitivity explains the counterintuitive slot-1 result. The live
  model places Gibbs at 19.705 PPG versus Puka at 19.311, only a 0.394 edge.
  Holding policy paths fixed, a +1.00 Gibbs PPG adjustment puts slot 1 within
  0.63 points of slot 3 and 1.13 above slot 4; +1.25 makes slot 1 the leader.
  The KDS order is therefore materially conditional on whether Gibbs is truly
  a standalone tier.

## 2026-08-03 - Snake DK D256 Historical Approval

- The adjacent Snake app promoted nested D256 for DK Sequential Preview while
  retaining D128 for NFFC offense-only. This changed no model inputs,
  projections, weekly tables, or source `Simulation.sqlite3` content.
- The leakage-safe replay used this repo's maintained Python 3.12 environment,
  strictly prior origin databases, and governed held-out 2017-2025 outcomes.
  It froze all 81 states before outcome access and sealed 3,888 D128/D256
  arm-room scores.
- D256 averaged 2,079.373 configured-DK all-played points versus 2,078.467 for
  D128: +0.907 points (+0.044%), season-clustered 95% CI
  [-3.160, 5.439] points, above the -5.196-point (-0.25%) noninferiority
  margin. Every frozen design, strict-prior, source, roster, isolation,
  exact-prefix, target-receipt, and scoring gate passed.
- Durable boundary: Sequential is the production methodology. GLPK/Legacy is a
  fallback/diagnostic and is not the D256 approval oracle.

## 2026-08-03 - Ridge and HistGBM Exact-Surface Screen

- Added a preregistered seven-alpha Ridge search and ten-candidate HistGBM
  schedule/L2 search on the exact locked 40-feature DK and beta conditional-PPG
  populations. All selection used rolling 2013-2022 origins; fixed comparisons
  reused the exact current 2023-2025 Lasso/RF/LightGBM predictions.
- Both scorings selected Ridge `alpha=10`, already inside the older 1/10/100
  range. The expanded 0.001-0.1 and 1,000 boundaries were materially worse.
- Replacing Lasso with Ridge improves RMSE from 3.090995 to 3.088552 DK (delta
  -0.002443; player interval [-0.005467,+0.000617]) and from 2.892611 to
  2.886263 beta (-0.006348; [-0.011263,-0.001338]). It wins all six season
  cells, improves MAE, and reduces positive bias, with essentially neutral
  rank correlation.
- Ridge is only a research shadow: Lasso/Ridge errors correlate at 0.9986 DK
  and 0.9961 beta, RB RMSE worsens by 0.0037/0.0050, DK uncertainty crosses
  zero, and the confirmation block has been reused.
- HistGBM equal-four worsens RMSE by 0.003646 DK and 0.001022 beta. Replacing
  LightGBM improves pooled RMSE by 0.002536/0.002357 but wins only three of six
  season cells and all player intervals cross zero. Reject HistGBM.
- Decision: keep production Lasso/RF/LightGBM unchanged. Do not combine Ridge
  with Extra Trees or tune weights post-hoc on 2023-2025. Durable study:
  `research/studies/2026-08-03_ridge_histgbm_ensembles/`.

## 2026-08-03 - Ridge Swap Downstream Decision

- Froze `Ridge(alpha=10) + RandomForest + LightGBM` at equal thirds against the
  active corrected-lineage `Lasso + RandomForest + LightGBM` blend. Ridge used
  the exact locked 40 features and only seasons before each 2017-2026 origin;
  RF/LightGBM forecasts came from the live read-only locked runs.
- Annual 2017-2025 refits reduce pooled RMSE from 3.105127 to 3.103726 DK and
  from 2.878165 to 2.876902 beta. Both player-cluster intervals cross zero. The
  swap wins 2023 and 2024 but loses 2025 in both scorings, so the full rolling
  test has 4/6 recent season wins rather than the earlier fixed-fit 6/6.
  Recent RB RMSE worsens by 0.003168 DK and 0.005368 beta.
- Strict-prior 2018-2025 residual distributions slightly improve player CRPS
  in both leagues with calibrated 50%/80% coverage. On frozen production top-N
  cohorts, all eight core/depth league-period PPG, contribution, and played
  CRPS cells remain inside the +0.25% transport margin.
- The 384-scenario, 12-room, fixed-20-player-roster replay is unfavorable. DK
  development improves score CRPS by 0.772% but DK 2023-2025 worsens 0.311%;
  beta development worsens 0.378% and beta 2023-2025 worsens 0.527%, just
  outside the +0.5% guardrail. Only one of four league-period cells improves.
  Beta roster replay covers 2019-2025 because the active beta locked table has
  no 2018 QB forecasts; beta player/template evidence retains 2018.
- Decision: reject Ridge replacing Lasso for the 2026 production center and
  retain equal-third Lasso/RF/LightGBM. The point-season and fixed-roster gates
  fail; the preregistered Auction shadow was therefore not run. Verification
  passed 21/21 pairing, lineage, strict-prior, and scenario checks. No model or
  app database changed. Durable study:
  `research/studies/2026-08-03_ridge_swap_downstream/`.

## 2026-08-02 - Boosting Grid Expansion

- Expanded both boosting searches from eight to 16 candidates while retaining
  every incumbent candidate. LightGBM added paired learning-rate/tree schedules
  from 0.01/500 through 0.10/50 under two structures. CatBoost added paired
  0.01/900 through 0.075/120 schedules plus deeper and less-regularized
  boundary candidates. All selection remained on rolling 2013-2022 forecasts.
- DK retained the exact incumbent LightGBM candidate: learning rate 0.05, 100
  trees, depth 3, seven leaves, minimum child 20, and L2 1. Its holdout and
  Extra Trees ensemble predictions are therefore bit-identical.
- Beta selected the new 0.01/500 LightGBM schedule with the incumbent shallow
  structure, improving the pre-2023 mean seasonal selection score by 0.001897
  and winning 6/10 origins versus 0.05/100. It did not transfer: the 2023-2025
  equal-third blend worsened 0.000583 RMSE (player interval
  [-0.001296,+0.002558]), improving 2023-2024 slightly and worsening 2025. It
  also weakened the prior Extra Trees blend by 0.000414 RMSE.
- Both DK and beta retained the original CatBoost 0.03/300, depth-5, L2-5,
  random-strength-0.5 candidate. Expanded tuning therefore reproduces the
  earlier -0.000686 DK / +0.002758 beta equal-four result and does not rescue
  CatBoost.
- Decision: retain the existing LightGBM grids and selected parameters, retain
  CatBoost's rejection, and keep Extra Trees as the only model-family shadow
  candidate. No production changes. Durable study:
  `research/studies/2026-08-02_boosting_grid_expansion/`

## 2026-08-02 - Beta Scoring-Context Challenger

- Added explicit beta V2 scoring-context support for historical and current
  weekly-template matching, including signed beta component shares, beta-scored
  provider-rank dispersion, team-QB passing context, exact locked-run lineage,
  governed hybrid-position audits, and fail-closed 2018 QB unavailability.
- A staged replay used beta feature run
  `milestone_3_20260803T000028Z_ccb9082c` and lock
  `v2_locked_final_beta_20260803T000628Z_84ddd0e9`. The stage was not promoted;
  an external Snake code-fingerprint change also prevents it from serving as a
  resumable release stage.
- The original full beta arm passed all 2,608-target player gates but failed
  development roster CRPS at `+0.9061%` versus the `0.5%` limit. Targeted
  alternatives isolated the instability to the beta PPG/rank representation,
  not signed components or donor residual centering.
- Absolute beta PPG without rank/market gap passed roster validation but failed
  the depth played-games player component (`+1.3506%` versus `1%`). Adding a
  small `0.50` beta rank weight cleared all player gates but failed roster
  development (`+0.8456%`). The context-only arm passes roster validation but
  retains the pre-2017 DK preseason PPG anchor, so it is not a complete fix.
- Decision: no promotion, no production refresh, and no production-cycle
  change. Beta expert fallback recentering remains explicit research opt-in.
  Focused beta/NFFC scoring tests pass (`63 passed`). Durable evidence is in
  `research/studies/2026-08-02_beta_scoring_context/`.

## 2026-08-03 - Beta Scoring-Context Correction Promotion

- User governance superseded the original no-promotion decision for data
  correctness: a small measured regression is acceptable to eliminate the
  known DK/beta unit mixture. This does not reinterpret the study as a
  predictive win.
- Activated exact `v2_beta_scoring_matched_preseason` matcher context and
  `beta_scored_expert_fallback` in the registered 2026 cycle. Validated legacy
  OOS centers remain for 2,696 templates; 2,602 fallback templates now use the
  beta-scored expert center instead of DK preseason units.
- The accepted validation tradeoff is development roster CRPS `+0.9061%`
  versus the `0.5%` gate and 2023-2025 `+0.3790%`. All 2,608-target player
  gates passed. No route weight or validation-era exception was introduced.
- The 39 beta 2018 QB rows without a valid sack-aware context remain in audit
  tables but are donor-ineligible with the exact FFToday quarantine reason.
  Together with the Le'Veon Bell holdout, the beta table has 40 ineligible
  templates and 5,258 eligible templates.
- Fresh immutable stage `ff_beta_context_promotion_20260802_03`, run
  `20260803T040708Z_2075ac47`, completed all 24 steps. Locked current and
  next-year DK/NFFC/beta validation, three weekly audits, salary fitting,
  1,000/1,000 reserve trials, consolidated release checks, and both app smokes
  passed. Live maps are 350 DK, 383 NFFC, and 328 beta, with 80 donors each;
  beta has 5,259 scoring-matched historical contexts and 314 current premiums.
- Atomic promotion installed ten artifacts with durable backups under
  `Data/Production_Refresh_Backups/20260803T040708Z_2075ac47/`. Every installed
  SHA-256 matches the manifest; live source/Snake/Auction SQLite integrity is
  `ok`; Snake full-table and Auction generated-table content parity pass.
- Verification: full `Tests/V2` passed (`277 passed`); the focused beta weekly,
  feature-mart, production-cycle, and refresh suites passed (`128 passed`);
  affected modules compile successfully.

## 2026-08-02 - Extra Trees and CatBoost Ensemble Screen

- Added eight compact Extra Trees and eight CatBoost candidates to the locked
  40-feature conditional-PPG pipeline. Each family was selected only on rolling
  2013-2022 origins, refit through 2022, and evaluated as a fixed equal-weight
  fourth member against the exact current Lasso/RF/LightGBM 2023-2025
  predictions in DK and beta.
- Extra Trees improved pooled RMSE from 3.090995 to 3.087534 in DK (delta
  -0.003461; player-cluster interval [-0.009328,+0.002527]) and from 2.892611
  to 2.886341 in beta (-0.006270; [-0.011945,-0.000534]). It won all six
  scoring-system/season cells and every non-QB position cell. All ten refits
  across five estimator seeds per scoring system remained favorable.
- Both scorings selected the same conservative Extra Trees configuration: 400
  trees, depth 10, minimum leaf 10, all features, no bootstrap. The challenger
  is highly correlated with the incumbent, but its non-QB blend still improves
  by 0.002808 DK and 0.002394 beta RMSE.
- CatBoost added only -0.000686 RMSE in DK and worsened beta by +0.002758. It
  therefore fails as an equal-weight fourth member. Reject the secondary
  equal-fifths blend because both challengers did not pass independently.
- Decision: advance Extra Trees as a research shadow candidate, not a
  production change. The improvement is small, DK uncertainty crosses zero,
  and DK/beta are alternate scoring views of substantially the same reused
  2023-2025 player-seasons. Durable study:
  `research/studies/2026-08-02_extra_trees_cb_ensembles/`

## 2026-08-02 - SKM Fold/Seed Ensemble Sealed Holdout

- Sealed 2023-2025 from every fit, fold assignment, and hyperparameter choice,
  then compared the locked single-fit method with deployment-matched
  five-member SKM-style bags using identical V2 primary features,
  preprocessing, compact grids, and equal-third Lasso/RF/LightGBM blending.
- The full fold-parameter plus seed bag is effectively tied in DK: 3.091207
  versus 3.090995 RMSE, delta +0.000213 with player-cluster interval
  [-0.002066,+0.002491]. It is materially worse in beta: 2.897024 versus
  2.892611, delta +0.004413 with interval [+0.000917,+0.007850].
- Five-seed averaging around the current selected parameters is neutral:
  -0.000515 DK and +0.000028 beta, with both intervals crossing zero. Under
  the frozen pipeline only RF changes with estimator seed; Lasso and
  deterministic full-sampling LightGBM are exact controls.
- Fold-parameter RF improves standalone RMSE by 0.006530 DK and 0.002783 beta,
  but fold Lasso worsens 0.001439/0.015941. One beta fold selected alpha 0.1,
  so parameter instability overwhelms the RF benefit in the full bag.
- A post-hoc hybrid retaining current Lasso/LightGBM and fold-bagging only RF
  improves the point estimate by 0.001193 DK and 0.000516 beta. DK's interval
  ends narrowly below zero while beta crosses; because the arm was discovered
  after component inspection, retain it only as a future prespecified test.
- Decision: production remains unchanged and the current single-fit method is
  retained. Durable study:
  `research/studies/2026-08-02_skm_fold_ensemble_holdout/`

## 2026-08-02 - Historical Team and Salary Source Integrity

- Audited the V2 source lineage and found three provider archives whose
  historical values were usable but whose team fields had been mutably
  backfilled: `FFA_RawStats`, `FFA_Projections`, and
  `FantasyPros_Best_Ball_ADP`. Added policy-hashed rules that discard only
  those historical team labels through 2025, retain the rows and values,
  canonicalize `TEAM_MAP` aliases before consensus, and leave true trusted
  ties unresolved.
- Applied the policy at identity, projection-spine, value, and feature-context
  boundaries. The first diagnostic stage exposed 25 NFFC joins lost to
  equivalent aliases; canonicalization restored all 25. The final DK/NFFC/beta
  foundations agree on 6,665 identities, 56,162 aliases, and 13,824 feature
  keys, with one matching three-rule trust receipt in each database.
- Replaced the ESPN salary name-length heuristic with a structural one-record-
  per-`$` parser. It validates governed team/status/position layout, whole-dollar
  salaries, exact marker and parsed counts, case-insensitive uniqueness, and
  post-cleaning uniqueness. Production now parses all governed exports before
  a staged-only atomic repair of 2025 beta, 2025 nv, and 2026 beta at exact
  200/160/200 counts while preserving the `Salaries` schema and indexes.
- A second diagnostic stage exposed two legitimate current V2 rows whose team
  remains null after trusted-source conflict: Stefon Diggs and Deebo Samuel Sr.
  Team is not a salary-model feature, so the salary fallback now permits only a
  cycle-reviewed subset of those two keys, requires `team_conflict=1`, and
  keeps every actual model input fail-closed under policy
  `v2_nullable_team_conflict_v1`. The final receipt observed exactly both rows.
- The final staged salaries preserve Bo Nix at `$5` in all three governed
  slices and Isaiah Likely at `$2` in 2026 beta. `Salaries_Pred` has 328 unique
  canonical keys with zero projection-only or salary-only rows; 326 are direct
  `ProjOnly` rows and two are governed V2 fallbacks.
- Governed run `20260802T174943Z_5b3cff69` completed all 24 stages and promoted
  all ten model/app databases. Release evidence includes exact corrected 2019
  teams (Hopkins HOU, Cooks LAR, Diggs MIN, Winston TB), identical Christian
  Kirk/Trevor Lawrence JAC QB context in 2022-2023, 350/383/328 DK/NFFC/beta
  weekly maps, 314 unique beta non-keeper premiums, 1,000/1,000 successful
  selection trials, and zero Auction/Snake smoke errors.
- Post-promotion SHA-256, size, `quick_check`, full `integrity_check`, and
  foreign-key checks passed for all ten installed files. Main focused suites
  passed 391 tests; the promoted Auction and Snake repositories passed 51 and
  17 tests. Durable pre-refresh backups are under
  `Data/Production_Refresh_Backups/20260802T174943Z_5b3cff69/`.
- Operational caveat: append-only logs retain one initial beta-foundation
  pandas/NumPy `TypeError` before a successful immutable-stage resume, one NFFC
  feature-mart heap-corruption retry, and three isolated NFFC LightGBM worker
  access violations recovered by the existing batch retry. The rebuilt
  artifacts passed every independent data, parity, hash, and app gate; do not
  describe the Windows native runtime as fully stable.
- The final Git check found one corrupt loose object for the indexed
  pre-refresh `Simulation.sqlite3`. Its durable promotion backup hashed to the
  exact required Git object ID, so the damaged compressed object was moved to
  the run's `git_object_quarantine/` directory and regenerated from that backup
  without changing the index, history, or working database. `git fsck` and
  `git diff --check` then passed.

## 2026-08-02 - Prior-Season PFF Advanced-Stat Screen

- Built a strictly lagged 2017-2025 screen from raw `PFF_Rec_Stats` and
  `PFF_Rush_Stats`, joined by numeric PFF ID through canonical
  `player_identity`. Season `t` uses only `t-1`; rookies and missing histories
  stay in the pool with a position-season neutral value and zero reliability.
- Screened 26 receiving/rushing metrics across their applicable positions.
  Fixed shrinkage reflects routes, targets, receptions, or attempts. Every
  rate is compared with an availability/opportunity-only control.
- Used the locked production PPG blend as the primary baseline, so expert
  projection, ADP, experience, prior production, projected role, room context,
  and trajectory are already included. Ran rolling corrections and q90
  classifiers in DK with beta replication; test seasons use only earlier OOS
  residual seasons for fitting.
- TE missed tackles per reception was the only DK position-metric row with
  season-bootstrap intervals below zero for both PPG RMSE (-0.0196 versus the
  opportunity control) and q90 Brier (-0.0013). It improved both outcomes in
  beta (-0.0133 and -0.0016), won 6/8 seasons for each objective and format,
  and improved both in every 2023-2025 season.
- The TE signal has moderate consecutive-season persistence (Spearman 0.330
  over 832 qualifying pairs) and little correlation with selected existing
  baseline features (maximum absolute Spearman 0.094). TE YAC/route is a
  weaker second-tier candidate whose DK intervals cross zero.
- YPRR, first downs/route, targets/route, and route grade form a highly
  collinear cluster; for WRs, YPRR correlates 0.891 with first downs/route,
  0.814 with targets/route, and 0.913 with route grade. Neither YPRR nor
  first downs/route earned a projection promotion from this screen.
- Decision: production remains unchanged. Freeze TE missed tackles per
  reception as the primary candidate for a full locked-model and
  template-matcher validation, with TE YAC/route as a separate sensitivity.
- Durable study:
  `research/studies/2026-08-02_pff_advanced_stat_screen/`

## 2026-08-02 - PFF TE Full Confirmation

- Refit the complete locked conditional-PPG Lasso/RF/LightGBM blend at every
  2017-2025 origin with production-selected hyperparameters. Challengers used
  strictly prior PFF TE data and were compared with both production and a
  PFF-availability/routes/receptions opportunity control.
- Routed the challenger blend only to TE rows, preserving exact QB/RB/WR
  production predictions. Avoided tackles per reception improved TE RMSE and
  q90 Brier in DK and beta during both 2017-2022 and 2023-2025; its nine-season
  RMSE comparison with the opportunity control won 8/9 DK and 6/9 beta seasons,
  with both season-bootstrap intervals below zero. YAC/route also passed as a
  separate projection sensitivity.
- Replayed 2,647 weekly-template targets per league with exact non-TE parity.
  The primary 0.25 avoided-tackles arm worsened development TE PPG CRPS and q90
  Brier in both leagues. The YAC/route arm passed the mechanical player-level
  screen, so it advanced to the frozen 108-room roster/championship replay.
- YAC/route failed roster transport: DK score CRPS, championship Brier, and
  championship log loss all worsened in development and 2023-2025; beta
  improved score CRPS but recent championship Brier/log loss worsened.
- Decision: no production changes. Retain avoided tackles per reception as a
  TE-routed projection-only implementation candidate; reject both PFF features
  for template matching. Screening and confirmation reuse the same historical
  origins, so a promotion still needs explicit governance or new-origin
  evidence.
- Durable study:
  `research/studies/2026-08-02_pff_te_confirmation/`

## 2026-08-02 - ADP_Ranks Source-Slice Repair

- Confirmed that the MFL loop deleted by year/position without a source filter,
  while FantasyPros appended without replacement. The live effects were zero
  2025 FantasyPros QBs, four 2026 DST snapshots per team, and accumulated
  historical `DS`/`K1`-`K9` rows.
- Added a validated atomic `ADP_Ranks` slice replacer. MFL now replaces only its
  year/position slice; FantasyPros replaces its complete annual source slice
  after allowed-position, row-depth, finite-pick, and uniqueness checks.
- Saved the byte-identical pre-repair source database as
  `Data/Databases/DB_Versioning/Season_Stats_New__2026_08_02_adp_ranks_pre_repair.sqlite3`.
- Rebuilt the live FantasyPros slices in one transaction: 313 valid 2024
  offense rows; 302 valid 2025 offense rows using 42 QBs from the identical
  June 8 versioned backups; and 466 rows from the archived 2026 export,
  including exactly 32 DST teams. The legacy best-ball CSV was not used because
  its population and ranks do not match the lost half-PPR snapshot.
- Final checks found zero duplicate provider keys, zero nonstandard FantasyPros
  positions, exact recovery of all 42 backup QBs, preserved non-FantasyPros
  rows, and SQLite `quick_check=ok` plus `integrity_check=ok`. All 12 focused
  data-generation tests passed. DK, beta, and NFFC V2 databases remain
  unchanged and still lack the corrected 2025 `adp_fpros` QB vote pending a
  fresh governed refresh.

## 2026-08-02 - App Database Compaction

- Diagnosed the promoted Snake database at 103.89 MiB: 47.01 MiB was SQLite
  freelist space, while the same 26-table logical database occupied about
  56.88 MiB when compact. The oversized blob existed only in the Snake working
  tree; the tracked HEAD blob was 73,826,304 bytes.
- Confirmed the Snake runtime queries five tables and that a runtime-only copy
  would be about 43.55 MiB, but retained the full source-copy contract to avoid
  dropping audit and Auction tables solely for storage management.
- `prepare_apps` now vacuums both staged app candidates before post-copy parity,
  smoke tests, hash capture, and promotion. It records page/file receipts,
  requires zero remaining freelist pages, and rejects artifacts over GitHub's
  100 MiB limit. Focused refresh verification passed 57 tests and the complete
  V2 suite passed 255 tests.

## 2026-08-01 - Annual Parameter Cache and RF Workers

- Raised annual current and next-season random forests from one to four joblib
  workers without changing seeds, trees, folds, grids, or predictions.
- Added the staged/promoted `V2_Parameter_Cache.sqlite3`, keyed by season,
  league, runner, and model. Every entry fingerprints the relevant training
  rows, targets, features, grid, origins/embargo, metric, and seed. A mismatch
  retunes only that model; a hit skips optimization but still refits and
  predicts every selected origin.
- Bootstrapped 36 validated 2026 entries from the completed failed-stage
  selections by recomputing their fingerprints against that stage's exact
  training frames. A fresh rebuild may use them only if it reproduces those
  fingerprints.
- A disposable DK warm-cache replay hit all seven current and five next-season
  model entries. Both 660-row shadow tables matched the saved one-worker output
  in every non-run-ID field within `1e-12`; SQLite integrity was `ok`.
- Manifest schema 5 snapshots, validates, backs up, and promotes the cache with
  all model and app databases. Focused syntax and 104-test verification passed
  before starting the new governed refresh.
- Isolated each V2 Milestone 2 foundation build from the Milestone 3
  feature-source pass in separate Python processes. A direct DK replay of the
  supported `--reuse-foundation` path completed after two combined-process
  attempts died in Windows native heap code; the split preserves the same
  foundation and feature contracts.
- Raised the outer retry ceiling for the explicitly classified Windows access
  violation and heap-corruption exit codes from two to five attempts. The
  failed beta feature pass completed unchanged on the next independent replay;
  ordinary Python failures still fail immediately and every attempt remains in
  the manifest.
- Fixed weekly current-team context for canonical market-only additions. The
  keyed `Avg_ADPs.team` (itself sourced from `player_identity.latest_team`) now
  fills only an unassigned Model Inputs/V2 team and publishes
  `current_team_source`. This resolved JuJu Smith-Schuster's DK ADP-226 row as
  NYG without a name join or blanket fringe exclusion; DK, NFFC, and beta
  weekly builds all passed on the completed stage.
- Fixed the auction selection-premium refresh so its deliberately premium-free
  Target seed first clears only the staged active year/league premium slice.
  This prevents stale generated players from contaminating constructor-time
  joins while preserving other seasons and leagues. The failed stage preflight
  cleared 314 stale beta rows, completed 1,000/1,000 trials, and republished
  314 current rows without touching live databases.
- Completed and promoted governed run `20260801T194518Z_0473c7e7`. All 24
  stages passed; every annual selection was a cache hit (7 current plus 5 next
  for each league), the auction reserve completed 1,000/1,000 trials, and both
  app smokes reported zero errors/exceptions. The first promotion attempt was
  cleanly rolled back because DB Browser held the Auction database; resuming
  after the handle was closed skipped all builds and promoted the validated
  stage atomically with no cleanup warnings.
- Post-promotion checks found matching staged/live hashes for all ten promoted
  databases and SQLite `quick_check=ok` everywhere. Live weekly maps contain
  350 DK, 383 NFFC, and 328 beta players; the premium has 314 unique beta keys,
  no keys outside the beta map, and 1,000 successful seed trials. JuJu
  Smith-Schuster is NYG via `canonical_avg_adps`.

## 2026-08-01 - Fringe Market Handoff Policy

- Replayed the failed production handoff against the completed staged DK,
  NFFC, and beta model databases. J'Mari Taylor was DK market-only at rank 243
  and ADP 225.97 with no valid current/next PPG center. Kenny McIntosh, Jordan
  Watkins, and Jaydn Ott were NFFC market-only at ADPs 316-317 with the same
  completeness gap.
- Added exclusion policy `v2_market_only_incomplete_buffer_exclusion_v3`:
  core players and keepers always fail closed; market-only players fail closed
  through the first five-sixths of the draft; later incomplete market-only rows
  are omitted and audited only if the remaining complete population still
  covers all 240 DK, 360 NFFC, or 180 beta draft slots. Canonical `Avg_ADPs`
  remains complete and legacy point estimates are never used as a fallback.
- Removed Najee Harris from the explicit 2026 NFFC exclusion list after the
  replay proved both handoffs are now complete. Stale explicit exclusions
  continue to fail the build.
- The disposable full handoff passed with 350 DK, 383 NFFC, and 328 beta
  projections. Focused handoff tests and syntax checks passed. Because the code
  fingerprint changed, rerun the governed refresh from a fresh stage before
  promotion.

## 2026-08-01 - League-Winner Upside Objective Audit

- Audited the existing objectives before changing behavior:
  - player templates use ordinary PPG, contribution, and played-game proper
    scores plus event diagnostics
  - Auction ranks paired Buy-minus-Pass mean managed-season score with an
    LCB80 uncertainty penalty
  - Snake ranks forced current-pick candidates by expected final best-ball
    score
- Froze a causal player league-winner event: active PPG at least 5.0 above the
  held-out preseason projection and managed contribution above the
  position-specific q90 of comparable preseason-ranked players from the five
  strictly prior seasons. q95 is the severity sensitivity. No target-season
  result enters a threshold.
- Replayed six matchers on 2,647 held-out targets per league. The
  `wr_ppg225_both025` arm alone improved primary q90 Brier, log loss,
  continuous tail-utility CRPS, and contribution CRPS in all four
  league-by-period core cells while staying inside the 0.25% PPG guardrail.
  The gains were small and most season-bootstrap intervals crossed zero.
- The audit exposed absolute DK tail undercalibration. Production predicted
  q90 league-winner rates of 7.45% versus 16.39% realized in 2017-2022 core
  players and 6.02% versus 14.17% in 2023-2025. Beta was much closer at
  7.72% versus 7.50% and 6.35% versus 6.94%.
- Reran the exact 384-scenario, 12-room-per-origin Phase-C banks and converted
  each room's scenario wins to championship probability. The promising WR arm
  did not transport: championship Brier/log loss worsened in DK development,
  DK recent, and beta development, improving only recent beta. Season
  intervals were wide. The prior flatter-distance finalist remained
  inconsistent as well.
- Rescored the saved nflfastR receiver and RB role experiments on +5/legacy
  impact Brier and average precision. None showed joint league/period
  replication that changes their prior rejection.
- Decision: no production matcher or Auction/Snake objective change. Add the
  rare-upside and championship scores to future validation. The next app-level
  test should preserve calibrated common scenario draws, require expected
  roster score within 0.25% of the best action, and use paired championship
  probability LCB only as a lexicographic tie-break. Causally calibrate the DK
  tail event before using absolute probabilities.
- Durable study:
  `research/studies/2026-08-01_upside_objective_audit/`
