# September 2026 Session Notes

## 2026-09-06 - Ignore Nested Research Replay Databases

- Extended the research staging SQLite ignore rule to all directory depths.
  The prior rule missed the nine files over 100 MiB in the 2022/2023/2024
  stages of `2026-08-27_auction_excess_multi_origin`. All were untracked and
  remain intact locally; no database or Git-history deletion was needed.
- Confirmed the main Model_Inputs database is already ignored and its removal
  from the Git index remains staged. Source stats and the compacted production
  Simulation remain tracked. Research code, manifests, and result reports are
  still eligible for tracking.

## 2026-09-05 - Retire Display-Only Breakout Production Tables

- Removed the Auction breakout display/import and the production refresh's
  paired-breakout build and validation requirements. Schema 8 replaces 7;
  inherited source/Auction breakout tables are removed before compaction and
  release validation rejects reintroduction. The generated Auction registry
  returns to 20 tables; app-owned tables remain preserved.
- Retained the research builder and legacy offline formatters. The builder
  reads Simulation only, requires a separate `--output-db`, rejects production
  destinations, and no longer supports `--sync-app`. The historical contract is
  preserved under the original paired-breakout study.
- Backed up source, Auction, and Snake, removed exactly four diagnostic tables
  on separate candidates, and installed the candidates after validation. The
  installed files are 84,561,920 / 84,557,824 / 84,533,248 bytes respectively
  (about 80.64 / 80.64 / 80.62 MiB), with 26 retained tables each. All retained
  table digests and schemas match each database's own snapshot. No source
  expert/stat database or weekly scoring table changed.
- Verification: all 71 focused refresh/builder tests and all 120 Auction tests
  pass; Auction Beta/NV and Snake DK/NFFC AppTest show zero exceptions/errors.
  SQLite integrity/foreign keys pass, freelists are zero, and installed hashes
  match the verified candidates. No full model rebuild was needed.
- A separate Beta research rebuild produced 273 player profiles and 21,840
  donor-pool rows successfully; production hashes stayed unchanged.
- Complete snapshots, code backups, compaction/digest receipts, installation
  hashes, and test logs:
  `Data/Production_Refresh_Backups/20260906_breakout_retirement/`.
  No commit, push, historical Git rewrite, LFS change, or external backup was
  performed. Durable policy: `docs/runbooks/database_storage.md`.


## 2026-09-05 - Database Storage and Generated Git Tracking

- Audited all 30 Simulation tables using read-only source queries and
  per-table page measurements on an in-memory backup. The live source is
  138.54 MiB; paired-breakout pools occupy 50.07 MiB, weekly pools 35.88 MiB,
  weekly templates 20.50 MiB, and paired templates 7.26 MiB. App consumers and
  the governed refresh still use the large tables.
- A `VACUUM INTO` copy saved only 4,096 bytes. Integrity, complete schema,
  and every table's ordered row values match; the source file hash is unchanged.
  The copy and full JSON receipt are retained under
  `Data/Production_Refresh_Backups/simulation_storage_audit_20260906T012145Z/`.
- Consolidated generated-database ignore rules and added staged SQLite copies
  plus transient sidecars. Removed only Model_Inputs and Validations from the
  Git index with `git rm --cached`; both working files remain intact. Source
  stats databases, provider/manual inputs, and Simulation remain tracked.
- No live Simulation table, app database, or source/stat database was changed.
  No commit, push, historical Git rewrite, LFS migration, or external backup
  was performed. Durable retention policy and storage results:
  `docs/runbooks/database_storage.md`.
